export type TodaySessionStep = 'scene' | 'matchPairs' | 'build' | 'type' | 'speak' | 'complete'

export const TODAY_SESSION_STEPS: TodaySessionStep[] = [
  'scene',
  'matchPairs',
  'build',
  'type',
  'speak',
  'complete',
]
