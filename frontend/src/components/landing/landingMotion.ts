// Shared motion grammar for the landing page.
//
// One signature easing — the "tide curve": slow in, decisive out — used for
// every section-level reveal so the whole page speaks with a single voice.
export const TIDE_EASE = [0.66, 0, 0.01, 1] as const
