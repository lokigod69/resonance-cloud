# Session Log
Newest first. Append-only — entries are never rewritten.
When this file exceeds ~300 lines, move the oldest half to `archive/log-2026.md`.

## 2026-07-12 (2) — Content fix batch (191 findings) + guided TTS batch EXECUTED: 3,744 clips across all 10 languages, verified end to end
- **Owner session brief (voice):** commit the language batch, close the gaps, bright-only TTS on quality model, mixed voices but ONE generation per text ("per ten lessons same voice, then bomb — different voice"), Codex executes / Fable orchestrates+reviews; A2 design next. Content-fix job explicitly pinned to gpt-5.6-sol high/xhigh (already the `~/.codex/config.toml` default — verified in the job log).
- **Commits pushed:** `96b04391` language batch + `b098cb0f` brain save (owner call), `27388103` content fixes + TTS pipeline, `dc89cc70` batch results + verify tooling.
- **Discovery that dissolved decision 1:** prod probe (`scripts/probe_guided_tts_state.py`) showed the guided TTS SQL was ALREADY applied — all 4 tables, playback view, `guided-tts` bucket live, plus the English flash-v2.5 canary assets (368) and 447 usages. The rescued-draft migration was never needed. it/pt/pl voices also already in `public.voices` (plan doc was stale on both).
- **Content fix batch:** Codex (task-mrgppwky-nlrswt, 1h10m) applied 191/193 findings — the 2 skips are the intentional target-equals-base locale fallbacks (contract narrowed, no authoring of 5,115 en + 1,846 de fields). Codex deviations all reviewed and accepted (P5L1 `Sorry, I'm late.`, PL impersonal constructions, PT `Estou aqui. Obrigado.`). **Round 2 was Fable personally** (Codex hit its OpenAI usage cap): the review's own suggestions had collapsed 10 English wistful/sharp variants into bright-duplicates (hedge-strip Levenshtein 1.000) → rewrote each in-voice; trophy collisions/dupes fixed (en P5 double-`tomorrow`, P6 allergy/better, P9 double-`set`, ceb P10 salamat/pwede → nagpasalamat/okey, pt nome/aqui/próxima → qual/estou/até); 5 low-value typeRecall answers restructured; PT diacritic acceptedAnswers verified present; ID P8 checkout: kept the loanword phrase (linguistically right), trophy → `sekarang`, `keluar` item restored, leakage test exempts exactly that lesson. Stale test baselines regenerated programmatically (EN arc titles now German, PR6 phrase lists, trophy sequences, tts-inventory char counts 236→202). All green: 9 suites (data 9250/9250, cross-vibe 0 hard fails 0 collisions), typecheck, i18n 1497/1497, eslint, build, pytest 52/52.
- **TTS pipeline changes:** `generate.py --language` flag (target_language_code was stuck at en-US); provider omits `language_code` for models without enforcement (multilingual v2 400s it — new test); `db.load_existing_assets_by_cache_key` batched (PostgREST URL-length 400 at >~100 keys); `scripts/seed_guided_bright_rotation.py` (91 per-path bright profiles, `{lang}_a1_bright_p{n}_multiv2_v1`, specificity 12 beats vibe-level 8 and static 0); `scripts/run_guided_bright_batch.py` = dry-run + guardrails + commit in ONE process (profile-pattern check, 8k/lang + 60k/batch caps) instead of circular EXPECTED_SCOPES entries.
- **Batch results (eleven_multilingual_v2, bright, corePhrase+chunks+trophyWord):** en 405 / es 417 / it 405 / fr 405 / pt 410 / de 424 / ceb 408+1retry / id 408 / pl 416 / ko 45 = **3,744 clips, 47,024 chars** (~16% of owner's monthly credits). One transient ElevenLabs read-timeout (ceb P1 core), idempotent re-run regenerated just it. **Verified:** 0 failed assets, 3,970 bright playback rows (424–453/lang, ko 50), 0 missing URLs, 13/13 audio HEAD spot-checks (200/audio-mpeg). Guided audio now plays real voices on lingwave.ai for every A1 language once deployed.
- **Permission-gate lesson:** the auto-mode classifier blocks prod Supabase writes unless the user named them → one AskUserQuestion naming tables/spend cleared both seeding and generation.
- **Known leftovers:** English wistful/sharp clips remain old flash-v2.5 single-voice audio; texts changed by the fix batch have stale audio in those vibes only (bright fully regenerated; vibes are discontinuation-bound). Owner has NOT yet made the Japanese call. A2 design is the owner-directed next step.
- **Owner direction (voice transcript):** unify language offering (home must add any offered language), build out A1 guided for more languages (Korean wanted, Japanese maybe), only bright vibe for new content (vibes being discontinued as content-variants), TTS for all A1 paths after languages land (Codex as workhorse), A2 design later. ≥20 languages long-term target.
- **Wizard unification SHIPPED (uncommitted):** Polish registered in `lib/languages.ts` (pl/Polski/#e11d48, wizard-only — NOT isSpeak, api has no 'pl') + isWizard flipped on Portuguese/Dutch/Hindi/Arabic → wizard = 15 languages; `langName.Polish`+`.pl` ×3 locales (i18n 1497/1497); PL flag in FlagIcon; pt/nl into supported-Latin font set (pl deliberately on fallback stack like Cyrillic); Lens prompt: Hindi transliteration + pt/nl articles. Verified: typecheck ✓ api tsc ✓ eslint ✓ tsx round-trips 5/5 ✓. Polish-active Speak degrades to language grid (Speak.tsx:674 verified). Report: `investigations/LANGUAGE_SURFACE_UNIFICATION_2026_07_12.md`.
- **Guided speak-step transcription was BROKEN for 8/9 languages (found + FIXED, uncommitted):** `api/guided-transcribe.ts` 400'd every non-English locale AND hardcoded `language='en'` to Whisper, while `useGuidedSpeechRecognition.ts:112` always sends the lesson locale. Now: `GUIDED_LOCALE_TO_WHISPER_LANGUAGE` accepts all guided locales incl. ko-KR, proper Whisper hints, ceb-PH → auto-detect (Cebuano not Whisper-supported). api tsc ✓, paid-api suite ✓. English-only testing is why this went unnoticed.
- **Codex CLI WORKS now** (v0.144.0 + gpt-5.6-sol smoke-tested) — the 2026-07-10 "too old / rescue unavailable" note is obsolete. Companion runtime used for 2 long tasks this session; status/result via `codex-companion.mjs status|result <task-id>` from repo root.
- **Guided A1 content quality review (Codex, 17min):** all 9 languages = 10 paths × 10 lessons (900 lessons, 1,100 variants, ALL contentStatus 'draft'); wistful/sharp variants exist ONLY for English → owner's "vibes are cosmetic/EN-only" belief confirmed for other languages. 193 findings: 31 HIGH / 131 MED / 31 LOW. Systemic: EN path/lesson metadata `.de` fields hold English (~310), Cebuano+Indonesian `sceneCaption.de` hold target-language text (200 lessons), placeholder captions `.de` English (~180). Real linguistic HIGHs: pt "plataforma do ônibus"→"ponto de ônibus", es/id unidiomatic hotel checkout, it "Sono tardi" (also fails typeRecall compose), pl missing reflexive "się". Report: `investigations/GUIDED_A1_CONTENT_QUALITY_REVIEW_2026_07_12.md` (53KB). Fix batch = next session's prime candidate.
- **Guided TTS batch plan written** (`docs/Product/FABLE_GUIDED_TTS_BATCH_PLAN.md`): frontend playback (`lib/guidedAudio.ts` → `guided_tts_playback` view) and Python ElevenLabs pipeline (`orchestrator/src/services/guided_tts/`, dry-run default) already EXIST; gaps: guided SQL only in rescued-drafts (owner-gated), Python/SQL surface-enum drift (chunks/speak vs chunk/speakTarget), `EXPECTED_SCOPES` en-only, no voice picks for it/pt/pl. ~5k chars/lang/voice → ~90k chars for 9 langs × 2 voices. 4 owner decisions listed in the plan.
- **Korean guided pilot (A1 Practical 1, 10 lessons) SHIPPED, uncommitted** (Codex authored, Fable reviewed every lesson + verified): NEW per-language module pattern `src/data/guided/koreanA1.ts` (648 lines, type-only imports; the 65k-line guidedLessons.ts stops growing) + surgical integration (union, ko-KR, speak-locale map, path options, todayLanguage). Natural 해요체, Korea-native scenes (편의점/삼각김밥/Hongdae), bright-only. All checks green incl. 9 guided suites (data 9250/9250) + prod build (guided chunk 2,874kB, no dashboard leak).
- **Korean-blocker speech fix (Fable, during review):** `guidedSpeechCheck.ts` + `speechRecognition.ts` normalizers stripped all non-ASCII → Hangul transcripts became empty → speak step would always grade Korean 'incorrect'. Now Unicode-aware (`[^\p{L}\p{N}\s]/gu`); also stops splitting umlauts/diacritics into token fragments for existing languages. Speech-check suite ✓.
- Architecture doc updated with a dated delta block (15 registry entries, transcribe fix, tooling-on-main, articleDisplay gone, vibe reality).
- **Owner answers applied:** all 11 leftover worktrees removed (5× C-drive static-thematic — `C:\Users\micha\.config\superpowers\worktrees\` deleted entirely —, orchestrator-id-static-tts, ResonanceTEST-polish-polish, tmp/phase2a×2, tmp/voxtral×2) + 10 orphaned local branches deleted after per-branch origin-containment proof (incl. `static-thematic-ceb-animals-yumi-raw-v1`, contained in origin/static-thematic-id-...); stale copy `orchestrator-origin-main-check` (223MB) deleted; `ltx-worker\.venv` (4.9GB) deleted; `_archive\` (~1.9GB incl. the storage-cleanup git bundle — local ref `backup/storage-cleanup-workflow-4525de38` still exists) deleted AFTER the clean review verdict; `content\`+`curriculum\`+`brand\` media left in place per owner, never pushed.
- **`refactor` branch dropped deliberately (Fable decision, owner delegated):** its 2 "ahead of origin/refactor" commits `e816565a`+`c45dbd43` (April job_runner decomposition + cloud_engines/direct-dispatch) are ancestors of main — only the remote ref was stale. `git branch -D` after merged-to-HEAD proof. origin/refactor left as-is (no pushes).
- **SQL rescue before archive deletion:** 5 files existed ONLY in `_archive` → copied to `frontend/supabase/rescued-drafts/` + README. `20260517010000_guided_tts_v1.sql` kept on owner decision (raw material for future guided-path listen-to-phrases audio; plan-first, no implementation). The 4 April root drafts (study_mode, cost_tracking×3) were probably applied manually to Supabase (typed mode + costs dashboard live in prod) — kept as the only schema record instead of permanent delete.
- **Codex adversarial review of the 4 cleanup commits (task 10m29s, read-only):** checks 1/3/4/5 HOLD (zero deleted-module refs in tree/deploy config; Dockerfile.cloud runs only start_cloud.py; 114 moved doc paths unreferenced; guided-TTS tooling imports clean); check 6 env-blocked in Codex sandbox (tsbuildinfo EPERM — known limitation; both projects pass read-only, and Fable's local typecheck is clean). **1 LOW finding, fixed by Fable:** 3 stale `returnTo` fallbacks to the deleted `/games` hub (GameShell.tsx:16, SlicerGame.tsx:59, DeckPicker.tsx:44) → `/dashboard`. typecheck ✓ eslint 0 ✓. UNCOMMITTED (with rescued-drafts/) — owner commit call.
- Noted, not acted on: `D:\CODING\ResonanceTEST\tmp\` still holds ~35 loose scratch files (logs/patches/screenshots, worktrees now gone) — not on the decide list.

## 2026-07-11 (4) — Repo-wide cleanup pass EXECUTED (5 analysis agents + 2 executors, Fable verifying): 4 commits pushed, ~900MB archived, DAW deleted, worktree jungle cleared
- **Full report:** `investigations/CLEANUP_PASS_2026_07_11_REPORT.md`. Policy archive-first: un-versioned → `D:\CODING\ResonanceTEST\_archive\` (manifests); tracked → `git rm` in revertable commits.
- **Commits (all pushed):** `c16e83d7` frontend dead code (26 zero-importer files ~1,850 LOC + orphaned /games hub + wordLibrary.* i18n family); `be208ef6` DAW chain deleted (app.py, routers/, state/csv_import/presets/voices, start*.bat, song_engine/language.py) + 64 setc one-off scripts/tests archived, seed_guided_voice_profiles.py now tracked; `8a1c20a8` docs restructure (Infrastructure tracked — was memory-cited but untracked!, historical families → docs/archive/, README refresh); `9067e188` pricing contract test repointed at live wizard pages + investigation/ baselines.
- **Verified by Fable:** typecheck ✓ i18n 1495/1495 ✓ build 3.97s ✓ pytest collect 689/0 errors ✓ 140 live backend tests ✓. **14 pre-existing stale frontend-contract test failures documented** (verified failing at pre-cleanup commit too — music_lyrics 6, profile_base_language 2, gpt_premium 2, admin_gpt_image 1, duration_fields 1, phase1b_retry 1, concept_lyric 1+env) — backlogged.
- **Worktrees/branches:** 19 clean worktrees removed + 37 merged branches deleted (git -d self-check). Crown jewels rescued FIRST: push-speak-scroll-fix + french-a1-p1-final/pilot pushed; diverged codex/unify → origin `archive/codex-unify-generation-loaders-local-20260711`; backup/storage-cleanup (workflow-scope push block) → verified bundle in _archive/git-bundles + local ref kept. Dirty-worktree scratch salvaged to _archive/worktree-salvage. **Memory correction: static-TTS multi-language tooling (61f3c245) IS on origin** (ancestor of origin/static-thematic-id-...) — the "unpushed worktree branches" memory was stale.
- **Root:** ~895MB archived (90 loose handoffs/investigations, 25 dirs incl. engines/, migration/, test outputs); LINGWAVE_LEARNING_STATE.md rescued to investigations/. content/ (13.2GB) + curriculum/ (15.1GB) + brand/ left in place — owner call.
- **Owner decide-list (also in NEXT_STEP):** 11 leftover worktrees need explicit naming (permission classifier), orchestrator-origin-main-check stale copy (223MB), content/curriculum/brand fate, _archive deletion after review, ltx-worker/.venv (4.9GB), refactor branch (2 unreconciled commits), root supabase/ + guided_tts_v1 sql drafts (verify never applied).

## 2026-07-11 (3) — Nav-restructure batch COMMITTED+PUSHED `a1068fc9` on owner call; docs/dead-code cleanup program kicked off
- Owner gave the commit call → 24-file nav batch committed as `a1068fc9` and pushed with the brain save. Old untracked `frontend/scripts/test-daily-habits.ts` (mtime 2026-06-23) noticed during staging — pre-existing clutter, deliberately left out; feeds the cleanup task.
- Owner kicked off the next big task: repo-wide documentation + dead-code cleanup ahead of TestFlight (Codex analyzes, Opus/Sonnet executors, Fable decides). Hard constraint: the video pipeline stays — it's unexposed but shares Supabase table/JobRunner infrastructure the product needs. Temporary breakage acceptable (no users), but end state must work.

## 2026-07-11 (2) — Nav restructure BUILT: 6-tab IA (Media/Library/Lens in, Study out) + slicer due-words mode (Fable + Codex investigate/review; 24 files UNCOMMITTED)
- **Owner confirmed the IA over chat:** merge Cards+Music → Media tab, drop the Study tab (home is the study entry), Lens gets a tab, **Library gets its own tab too** (owner's addition — "library is for exploring, not for the homepage where you study"), kill the three home quick-practice tiles, playful Dive-in goes straight into due words. Owner also gave the commit call for the previous 14-file batch → committed+pushed as `40ef3aff` before this work started.
- **New nav (single config `primaryNav.ts`, feeds mobile bottom nav + both desktop headers):** Home(/dashboard), Today(/today), Media(/decks; activePaths /deck,/generate,/music; icon Layers), Library(/categories; reuses `nav.categories`), Lens(/lens; only when `VISUAL_LENS_ENABLED`), Speak(/speak). `MobileBottomNav` grid columns now derived from item count (5 when Lens flag is off). Prefetch list (`getPrimaryNavRouteImports`) updated: adds categoryList + lens (flag-gated), keeps studyModeSelector warm (Dive-in/mastered pill land there). New keys ×3 locales: `nav.media`, `nav.lens`.
- **Media = segmented switch, not a new route:** new `src/components/media/MediaSegments.tsx` (Cards|Music pill, theme-chip-active, safe-top padded via `pt-[max(0.25rem,var(--app-safe-top))]`) inserted at the top of Decks/DecksPG/Music/MusicPG. All deep links (/decks,/deck/:id,/generate,/music) unchanged. **Gotcha found by Codex re-review: `cn()` is tailwind-merge — caller `pt-*` classes silently strip the component's safe-top padding; callers must only pass pb-/mb-.**
- **Home:** glassy DashboardPG dropped the whole action grid (3 SrsActionTiles + Library tile + Lens tile); Word Tide + Dive-in + preset chip + mastered pill remain. Classic Dashboard keeps mission card + 3 tiles (its only study entry — no tide there), drops only Library/Lens tiles. Dead `.dashboard-library-*` CSS removed from index.css (also out of the @supports/reduced-transparency selector lists). `SrsActionTile` still used by classic.
- **Slicer due-words mode (`/games/slicer?queue=due&lang=X&returnTo=/dashboard`):** playful Dive-in now skips the DeckPicker. `useWordStates` gained a `fetched` flag (set inside the async load per query-key; distinguishes "empty pool" from "not fetched yet" — the hook's initial loading:false lies). Shared `isLemmaDueNow` exported from useWordStates (WordTide now uses it too). SlicerGame derives `dueChoice` (one word id per due lemma = wordIds[0], no duplicate cards) and `activeDeck = selectedDeck ?? dueChoice` — NO setState-in-effect (first attempt failed the react-hooks/set-state-in-effect lint; derived state is the right shape anyway). Mode = audio_to_image if any due row has an image (scene text-falls-back per card) else audio_to_text. Empty pool → DeckPicker fallback; **`inDueSession = !selectedDeck && dueChoice !== null` gates the row filter/mode/exit — Codex HIGH finding: gating on dueLanguage alone made the fallback picker's decks unplayable (empty due-set filtered all rows).** Due session exit navigates returnTo with `slicer.dueWords.exit`. Keys ×3: `slicer.dueWords.title/exit`, `slicer.loadingDeck` (both loading branches now t()'d).
- **Study SessionComplete label truth-fix:** all 7 study modes navigate('/dashboard') but said "Back to Decks"/"Cards" — now `study.backHome` ×3 locales.
- **Contract test** `scripts/test-mobile-bottom-nav-contract.ts` rewritten to the new tab set + flag-conditional Lens expectations (was already stale: asserted the glass classes deleted in `1834646f` and the removed dashboardLibraryHref).
- **Codex declined-finding record:** did NOT add `image_url`/`image_urls` to useGameDeck's WORD_SELECT — no migration/backend evidence those columns exist on `public.words`, a bad column 400s every slicer load, and pickImageUrl falls back to thumbnail_url (selected, canonical). Codex re-review accepted the risk call.
- **Verified:** typecheck ✓, eslint 0 on all changed files ✓, check:i18n 1502/1502 ✓, nav contract test ✓, prod build ✓ (3.4s). Codex adversarial review (gpt-5.6-sol via upgraded CLI — rescue works again) → 5 findings → 1 HIGH + 3 fixed, 1 declined with rationale; re-review verdict no-ship on the tailwind-merge padding regression → fixed (callers' pt- removed) → all checks green again. NOT committed — owner call + iPhone re-test pending. Pre-existing orphan noted: /games hub has no inbound links left (was true before this batch).
- **Owner iPhone-tested the deployed Word Tide (browser, lingwave.ai) and gave a voice feedback dump.** Straightforward breakage fixed this session; layout/IA questions deferred to discussion (bottom-nav composition, Today-as-hub, quick-practice tiles' right to exist, audio-on-image in Write mode).
- **Tide practice sheet was broken on mobile — root causes found, not patched around:** (1) the glassy layout's `<main>` is a `z-10` stacking context while `MobileBottomNav` is `fixed z-50`, so the in-place `z-50` sheet could NEVER cover the bottom nav; (2) `autoFocus` on the answer input popped the iOS keyboard at open, panning the layout viewport — fixed overlay rides up, unblurred home shows above, sheet lands half off-screen. Fix: `TidePracticeSheet` now `createPortal`s to `document.body` at `z-[60]`, centered (`items-center p-4`, `max-h-[calc(100dvh-2rem)] overflow-y-auto`), `useBodyScrollLock` while open (existing hook; its touchmove guard matters because the page scroller is `.app-shell`, not body), autoFocus removed (owner: "I should see the thing and then I can type").
- **Home presets now gated on real assets:** DashboardPG runs the same two limit-1 probes as the /study configurator (thumbnails / suno urls) over the active language's deck ids. `listening` hidden from the picker with zero songs (owner's dead end: picked listening with no songs → "Dive in" → "no audio available"); `visual` likewise on images; a STORED preset whose asset probe returns false falls back to the picker (`effectivePreset=null`) instead of dead-ending the CTA. Probe `null` (in flight) keeps the stored preset to avoid picker flash.
- **Deck dropdown hidden in home-launched sessions:** new `deckScopeLocked` from `useStudyUI` (`queue` param present or `lang` param present = the entry point fixed the pool) — dropdown suppressed in all 7 study pages (Type/ImagePG/Audio/Flashcard/Image/PG/Study). Organic /study-configurator entries carry neither param and keep the dropdown.
- **/study/type page layout:** root is now a fixed-height flex column (`h-[calc(100dvh-var(--glassy-content-top-offset)-var(--glassy-main-bottom-padding))]`, vars are :root-defined so classic resolves too) — prompt container flexes (`flex-1 min-h-[8rem]`, image `h-full object-contain` instead of `aspect-video`), input/actions `shrink-0`, only the OrbDock (already `max-height:220px; overflow-y:auto` under `.skin-glassy`) scrolls; root falls back to page scroll on tiny viewports. Input `autoFocus` now only on `(pointer: fine)` devices (module const `AUTOFOCUS_ANSWER`).
- **Slicer back fixed:** `DeckPicker`'s back button hardcoded `navigate('/study')`; now honors `?returnTo` (`|| '/games'`) like GameShell — home dive-in (playful) passes `/dashboard` so back goes home.
- **Streak into the header:** `HomeAccountStrip` gained `streak` prop → compact flame+count chip in the strip's empty left grid column (aria reuses `dashboard.streak.aria`); `.dashboard-streak-row` display:none under 768px (strip is `md:hidden`, so desktop keeps the standalone row). Wired in BOTH dashboards (PG + classic).
- **Verified:** typecheck ✓, eslint 0 errors on all 13 lintable changed files ✓ (removed one unused disable directive it flagged), check:i18n 1496/1496 ✓ (no new keys — all strings reuse existing ones). NOT visually verified on device — owner re-test needed. NOT committed — commit call pending.
- **Deliberately NOT done (discussion first):** bottom-nav restructure (owner floated: merge Cards+Music→Media, drop Study tab, Lens tab or Today-as-hub); removing home quick-practice tiles; tap-image-to-hear in Write mode (owner talked himself both ways — recall-purity vs audio need); slicer still asks for a deck on playful dive-in (needs a "play the due pool" concept, not a nav fix).

## 2026-07-10 (3) — Owner UX batch: typed-recall mode + library viewer nav + Train/Audio pool fix (Fable + Explore agent; UNCOMMITTED)
- **Owner session brief (voice):** home page is "no fun" — wants learning already happening on load (floating words, zero-config dive-in), per-language experience presets, a typed-answer study mode ("look at the image, type mixed nuts, cool animation"), library card viewer prev/next (had to close/reopen per word), and an explanation for "pressed Train, got a completely different word". Home redesign gated on owner green light (concept presented, 3 variants); the rest was implemented immediately.
- **Typed-recall mode BUILT:** new `/study/type` route both skins → `src/pages/StudyType.tsx`; prompt = thumbnail if present else translation; forgiving matcher `src/lib/typedAnswer.ts` (NFC normalize, lowercase, punctuation→space, apostrophe unification, parenthetical/variant splitting on `/,;`, leading-article drop de/en/fr/es/it/pt, OSA Damerau-Levenshtein tolerance 0 (<5 chars) / 1 (<10) / 2 (≥10); diacritics NEVER stripped — "schon" vs "schön" = 'close', shows proper spelling, still counts remembered). Verdicts: correct/close → recordAttempt(true) with success burst (spring pop + expanding ring, reduced-motion collapses to fade) + `playPronunciation` TTS, auto-advance 1.0s/1.9s; wrong → shake + retry allowed; "Show answer" reveal → Continue = `handleReviewLater` (records false + retry pocket). `study_mode='typed'` (recall_attempts CHECK constraint was dropped in 20260510130000; compute_word_states counts every non-runner mode). StudyMode union extended; Write tile (✍️ emblem) added to StudyModeSelector; keys `study.mode.type*` + `study.typed.*` en/de/fr. Matcher verified: 17-case tsx behavioral run ALL PASS.
- **Library card viewer navigation:** `CardDetailModal` gained `navigation` prop (chevrons overlaid on image, ArrowLeft/Right keys, horizontal swipe ≥56px with 1.5× dominance, "n / N" counter in the close row) — wired into BOTH LevelDetailPage paths (static `selectedItems` by conceptId, curriculum `level.entries` by term). Static word modal now `centered: true` layout (title/translation/Play centered under image — owner's styling gripe) and its hardcoded EN "Play"/aria became `categories.modal.play`/`playAria` (+`previous`/`next`) ×3 locales.
- **Train/Audio mismatch ROOT CAUSE + fix:** Train = queue `strengthen` = ALL `state='learning'` lemmas language-wide (no due gate), heat-sorted hot-first — so older failed words legitimately outrank the just-studied ones (by design, explained to owner). The actual BUG-feel: mode pages further filter by asset (audio → suno songs only), so "Train → Audio" could serve entirely different words than the tile count advertised. Fix: StudyModeSelector's Image/Audio existence probes now scope to the queue's word ids when a queue param is present (≤150 ids, else scope fallback; empty queue hides both). Lens words confirmed IN the pools (deck_type-agnostic RPC + fetch), only invisible to image/audio modes for lack of assets.
- **Verified:** typecheck ✓, check:i18n 1485/1485 de+fr ✓, eslint 0 errors on all 10 touched files (1 warning in LevelDetailPage pre-exists on HEAD — proven by stash A/B). NOT committed — owner call pending alongside green-light answer.
- **Codex support NOT available this session:** `gpt-5.6-codex` invalid for ChatGPT accounts and config default `gpt-5.6-sol` rejected ("requires a newer version of Codex") — the installed Codex CLI is too old for its own configured model; owner should upgrade the CLI. Investigation was done by a Claude Explore agent instead (full flow map incl. compute_word_states state machine: new→learning until 3 consecutive correct→reviewing→mastered ≥5).
- **Home redesign: owner GREEN-LIT V1 "Word Tide" (with refinements) and it was BUILT same session.** Owner refinements honored: NO persistent "Welcome" heading on the populated home (stays on first-run), NO Today's-Lesson card on home (guided lives on the Today tab — `TodayMissionCard`/`useTodayMission` removed from DashboardPG, a home-perf win since that hook did deck+RPC work), orb cap with backfill (MAX_ORBS=8, new orbs wash in as others clear), orbs vanish when graded (wrong ones too — SRS brings them back), image-cards get a tiny image coating on the orb, text-cards stay abstract colorful orbs (no spoiler text; hue = lemmaKey hash).
- **Word Tide implementation:** `src/components/dashboard/WordTide.tsx` — 8 slot-anchored drifting orbs (transform-only framer keyframes, static under reduced motion) over the existing wave; pool = same buckets as a no-queue session (new&due + learning + due reviews) minus session-cleared keys; slot assignment is state reconciled in an effect (react-hooks/refs rule forbids ref writes in render — first attempt failed lint, rewritten); thumbnails/TTS via one batched `words` query per newly-visible set, cached. Tap → `TidePracticeSheet` (bottom-sheet/popover): image or translation prompt + typed input (same `evaluateTypedAnswer` matcher), success burst + TTS + auto-grade(true) 0.95/1.7s, Show answer → Continue = grade(false); inserts `recall_attempts` with `study_mode='tide'` then `wordStates.refetch()`. All-clear state (gold check) when the pool empties after ≥1 clear.
- **Experience preset:** `src/lib/experiencePreset.ts` — per user+language localStorage (`visual|listening|writing|playful` → `/study/image|audio|type` | slicer); one-time picker card on home (4 emoji chips + hint), then a "Dive in — N to go" CTA (N = newDue+totalDue, no queue param = all-due session) + small preset chip to change. Quick-practice tiles now deep-link into the preset's mode (`SrsActionTile` gained `modeRoute`; playful/unset → configurator as before). i18n `home.tide.*`/`home.preset.*` ×3 (1496 keys).
- **Word Tide verification:** typecheck ✓, eslint 0 on all 5 files ✓, check:i18n 1496/1496 ✓, production build ✓ (7.9s, only the pre-existing >500kB chunk warning). NOT visually verified — owner must judge orb feel/looks on the deploy. Classic-skin Dashboard.tsx untouched (glassy-first per skin decision).

## 2026-07-10 (2) — Tier 2 item 3 shipped: nav !bg-black/40 overrides deleted (Fable solo; 1 commit pushed)
- **Owner approved the visible nav change.** `1834646f`: removed `!backdrop-blur-3xl !backdrop-saturate-150 !bg-black/40` from the three nav shells (`PolishGlassLayout.tsx:38`, `AppHeader.tsx:62`, `MobileBottomNav.tsx:14`) — class-string-only diff, 3 lines. The `.app-topnav`/`.app-bottomnav` theme-contract rules now paint uncontested: per-skin blur (glassy 48px/44px), `--nav-bg` tint per theme, and the `@supports`/reduced-transparency fallbacks (incl. Tier 1's) finally reach the nav. Warm-linen gets its linen nav; every dark theme gets its tinted nav instead of flat black.
- **Verified:** typecheck ✓, eslint 0 problems on the 3 files ✓ (no i18n delta — class strings only). Visual verdict is the owner's on the deploy — this changes the nav look on every screen by design.

## 2026-07-10 — Apple-design Tier 1 committed + Tier 2 SRS loop shipped: 280ms lockout gone, swipe-to-grade with velocity physics (Fable designing/implementing, Sonnet recon + Codex adversarial review; 2 commits pushed)
- **Tier 1 committed as `6cec01ef`** (work from the 2026-07-09/10 apple-design audit session, verified then, committed now on owner's "push everything" call): press states on `button.variants.ts`/`MobileBottomNav`/dashboard tiles/SRS grading circles, reduced-motion fallbacks for flip + Radix surfaces, app-wide `prefers-reduced-transparency` + no-backdrop-filter glass fallback, error toasts 7s. Audit + tiered backlog: `investigations/APPLE_DESIGN_SKILL_AUDIT_2026_07_10.md`.
- **Tier 2 (owner approved items: lockout removal + swipe-to-grade) shipped as `526dfbaa`:** `StudyFlashcard.tsx` now grades and advances in the same tick — `FEEDBACK_ADVANCE_DELAY_MS` (280ms input lockout) deleted; the radial pulse is decorative (seq-keyed, 700ms self-clear) and never gates input; the per-card ring glow died (can't ride the outgoing card once advance is immediate), replaced by direction-aware exits (remembered → drifts right, review later → left — teaches the gesture). New `src/components/study/SwipeGradeCard.tsx`: 1:1 `drag="x"`, Apple momentum projection (`(v/1000)·d/(1−d)`, d=0.998) decides commit vs spring-back so velocity sign wins over position, release velocity handed into all springs, commit threshold `min(42% width, 200px)`, Tinder-style stamps + green/red glows via `useTransform`, reduced-motion keeps drag but drops fling/rotation. Buttons + 1/2 keys remain the accessible path.
- **Codex adversarial review (no blockers) drove 3 hardening fixes pre-commit:** repeat guard raised 160→260ms (must outlast the 220ms exit or held keys grade unseen cards), 1/2 handler got `e.repeat` + focused-control guards (mirrors useStudyUI), hardcoded English aria-labels replaced with i18n (3 new `study.*Aria` keys en/de/fr; grade buttons reuse `study.reviewLater`/`study.rememberedAction`). Accepted as-is: last-card swipe jumps straight to SessionComplete (immediacy over ceremony).
- **Also fixed while in there:** framer `scale` motion value was clobbering Tailwind `-translate-y-1/2` on the stamps (moved translate into the motion style as `y: '-50%'`).
- **Verified:** typecheck ✓, eslint 0 problems on all changed files ✓, check:i18n 1471/1471 de+fr ✓, Codex review clean of framer-12 API misuse (checked against framer source). Owner iPhone re-test of the deployed feel pending — springs tuned by theory, not device.
- **Open:** Tier 2 item 3 (nav `!bg-black/40` override removal — visible nav change) awaits explicit owner call; Tier 2 items 6–8 + Tier 3 (water carousel physics, card viewer modal, bottom sheets, nav continuity) in the audit backlog. — Uncommitted batch COMMITTED + PUSHED as two scoped commits; bundle-slimming phase 2 DONE (Fable solo; 3 commits pushed)
- **Owner said "commit and push + do phase 2".** The two-pass uncommitted batch went out scoped as promised: `7ac30d21` (night batch: StudyCardFrame + SessionComplete across 6 study pages, reticle `min(90vw,28rem)`, generate-layout test repair) and `26255d8f` (slimming phase 1: publicAssetUrl + strip dist/curriculum). `StudyAudio.tsx` carried hunks of BOTH passes — split via stage-all + `git apply --cached -R` of a hand-built patch holding only the phase-1 hunks (import + thumbnail wrap), so each commit is pure.
- **Phase 2 implemented per plan §Phase 2, committed `aeb8cf2b`:** guidedVibes 3 emblem urls + guidedTrophySongs `audioPublicUrl`/`defineAudioCandidates` publicUrls wrap in `publicAssetUrl` at the data source (one wrap covers all render sites); `strip-ios-bundle.ts` also removes `dist/guided/trophy-songs` + `dist/guided/vibes`; contract test asserts the 3 strip targets + the funnel in both data files. **Deliberate deviation from plan step 8:** TodayPathOverview/GuidedSpeechPrompt/SegmentTrophyTile literals (`/guided/today|reviews|trophies|lesson-numbers`) NOT wrapped — those trees stay bundled (small, offline-capable); wrapping would force network fetches and dead-weight the bundled copies.
- **Measured:** strip frees **623.3 MiB** total; `dist` **77.7 MiB** after (target was ~76 MiB). Web build keeps curriculum + guided intact.
- **Verified:** typecheck ✓, capacitor contract ✓, guided suites vibes 99 / trophy-songs 216 / segment-reviews 531 / trophy-cloze 41 / fallback-matrix 1572 / cross-vibe 300 pairs / tts-inventory 38 — all 0 failed ✓, eslint changed files ✓, check:i18n 1468/1468 ✓, web build ✓, ios-mode build + strip ✓.
- **Found (pre-existing, not fixed):** `test-guided-today-path-overview.ts` has 6 stale source-string asserts (303 passed, 6 failed) against the evolved TodayPathOverview UI — identical failures with this session's changes stashed. It sits mid-chain in `test:guided-today` (`&&`), so the 5 scripts after it don't run via npm; ran them individually. Same class as the generate-layout repair — candidate for the same treatment.
- **Open:** native device smoke of both strips pending first iOS build; owner-side TestFlight items unchanged (team signing, Supabase redirect URLs, bundle-id decision, usage-cost migration).

## 2026-07-08 (late night) — Bundle-slimming phase 1 IMPLEMENTED: publicAssetUrl + strip dist/curriculum from the iOS bundle (Fable solo; UNCOMMITTED, on top of the night batch)
- **Implemented `investigations/CURRICULUM_BUNDLE_SLIMMING_PLAN_2026_07_08.md` phase 1** (resume default — no owner answers in the paste). New `publicAssetUrl()` in `src/lib/publicOrigins.ts` (overloaded nullable signature; passthrough for nullish + `https?:`/`data:`/`blob:`/protocol-relative/`capacitor:`/`resonance:`; web no-op; native prefixes `getPublicWebOrigin()` = lingwave.ai). Funnel wraps: the 4 `imageUrls.ts` helpers; builders `curriculumEntryImagePath`/`curriculumCategoryHeroPath`/`generatedCategoryEntryImagePath`/`resolveCurriculumImageSetAsset`; raw sites `StudyAudio.tsx` thumb, `ObservabilityWordDetail.tsx` CardImage, `useVideoVersion.ts` activeThumbnailUrl; `CategoryListPage.tsx` hero literal now uses `curriculumCategoryHeroPath`. **Plan drift found:** the 5 study canvases and the slicer `deckAdapter` already funnel through `getCardFullUrl`/`getCardThumbUrl` (not raw as planned) — covered by the helper wrap, no edits needed. `landingData.ts` deliberately untouched (LingwaveWaves same-origin rule).
- **Strip mechanism:** new `scripts/strip-ios-bundle.ts` (deletes `dist/curriculum`, fails loud if missing, logs freed bytes); `build:ios` = `vite build --mode ios && tsx scripts/strip-ios-bundle.ts && cap sync ios`. Web `build` untouched → Vercel still ships full curriculum. Contract test extended: `publicAssetUrl` token + build:ios-must-strip assert.
- **Side effect accepted (per plan):** on native, `curriculumDeckBridge` import payloads would write absolute `https://lingwave.ai/curriculum/...` into `words.thumbnail_url` (valid everywhere; publicAssetUrl is idempotent on them).
- **Verified:** typecheck ✓; capacitor contract test ✓; curriculumImagePath/generatedCategoryImages/curriculumImageSets unit tests ✓ (relative outputs hold — `isNativeApp()` false under Node); eslint on all 11 changed files ✓ (0 problems); check:i18n 1468/1468 ✓ (no new keys); `npm run build` ✓ with `dist/curriculum` present, then strip script run against it: **removed 460.8 MiB, dist 240.1 MiB after**. The plan's ~130–150 MB phase-1 projection was optimistic vs its own numbers — remaining bulk is `guided/` 169 MB (= phase 2, projected ~76 MiB dist after).
- **Open:** UNCOMMITTED (joins the night batch — owner commit call now covers both). Phase 2 (guided literals funnel + strip trophy-songs/vibes) not started. Native smoke (thumbnails fetch from lingwave.ai) needs a device build.

## 2026-07-08 (night) — Reticle enlarged; StudyCardFrame + SessionComplete extracted; bundle-slimming plan written; stale generate-layout test repaired (Fable orchestrating 2 Opus + 1 Sonnet agents; UNCOMMITTED)
- **Owner decisions this session:** Lens reticle bigger (done); glassy default confirmed (no-op — already `a2ee4e16`). Bundle id STILL undecided.
- **Reticle:** `src/index.css` `min(74vw,23rem)` → `min(90vw,28rem)`, vignette hole widened `40%/76%` → `47%/82%`. Scan crop follows automatically (`reticleRegion` reads the live DOM rect) — no Lens.tsx change needed.
- **StudyCardFrame + SessionComplete extraction (Opus agent, plan Phases 1+3 of `FABLE_STUDY_AND_CARD_RENDERING_COHERENCE_PLAN.md`):** new `src/components/study/StudyCardFrame.tsx` (skin-aware chrome: `.theme-card`/`.pg-glass` via `useSkin()`) + `src/components/study/SessionComplete.tsx` (single completion screen, preserves each skin's exact palette); wired into all 6 study surfaces (Study, StudyPG, StudyImage, StudyImagePG, StudyFlashcard, StudyAudio). Intended behavior change: StudyFlashcard/StudyAudio were skin-blind and now render glassy chrome under the glassy skin; classic unchanged. No new i18n keys (existing `study.*`/`nav.*` reused). Image-study conversion deliberately deferred to the plan's Phase 4 consolidation.
- **Curriculum bundle slimming plan (read-only Opus agent) persisted to `investigations/CURRICULUM_BUNDLE_SLIMMING_PLAN_2026_07_08.md`:** measured `public/curriculum` **471M** + `public/guided` **169M** (trophy-songs 149M — the earlier "guided ~71MB" figure is stale). Design: `publicAssetUrl()` in `publicOrigins.ts` (nullish/absolute/data/blob passthrough, web no-op, native prefixes `getPublicWebOrigin()`); funnel = the 4 `imageUrls.ts` helpers + 3 static path builders + ~10 raw render sites (StudyAudio, 5 canvases, slicer adapter, useVideoVersion, ObservabilityWordDetail, CategoryListPage literal); strip via new `scripts/strip-ios-bundle.ts` post-build step in `build:ios` (web/Vercel build untouched). Projected IPA ~130–150MB after phase 1 (curriculum), ~85–100MB after phase 2 (guided). Explicitly protected: landing `set-c` literals (LingwaveWaves rule), `public/scripts` Script Lab audio stays bundled, Supabase-absolute guided video/TTS unaffected.
- **Test repair (premise corrected):** STATE said `test:i18n-display-labels` fails on `generate.words.typeOwn` — actually that script was already green; the failing one was `test:generate-responsive-layout`. Removed stale asserts for deleted UI (typeOwn choice buttons, WordsStep Back button) and updated drifted ones to current intended design (slider uses MIN/MAX_CATEGORY_WORD_COUNT 5–10 + i18n'd `generate.words.maxCount`; primary Generate button legitimately carries Sparkles, mode buttons stay label-only; quick/customize asserts moved with the controls out of CategoryPicker; `generate.quickGenerate` → `generate.primaryGenerate`). Script green.
- **Verified (combined tree, after all agents landed):** typecheck ✓, check:i18n 1468/1468 de+fr ✓, test:generate-responsive-layout ✓, test:i18n-display-labels ✓, eslint on changed files ✓ (0 problems), test:study-flashcard-pronunciation ✓.
- **Open:** ALL UNCOMMITTED (8 modified + 2 new components + script fix; owner commit call). Owner iPhone re-test should now include the bigger reticle. Slimming plan ready to implement on owner's word.

## 2026-07-08 (evening) — Speak pass committed; Capacitor shell verified nearly done + fixes; glassy default; orphaned dirty files all audited and committed (Fable orchestrating 2 Opus + 1 Sonnet agents; 5 commits pushed)
- **Committed on owner's call:** the morning's Speak feedback pass as `c4ede7a9` (7 files, scoped staging; translations.ts diff verified Speak-only first).
- **Capacitor readiness audit (2 Opus agents, repo inventory + /api\* deep-dive):** the shell is ~95% done and the old "#1 blocker" is solved — all 14 `/api/*` call sites already go through `publicApiUrl()` (zero bypass), CORS allow-list already includes `capacitor://localhost`, auth is Bearer-token via @capacitor/preferences (PKCE + `resonance://auth/callback` deep link, cold-start handled), Info.plist has camera+mic strings, PrivacyInfo.xcprivacy present, contract test `scripts/test-capacitor-ios-contract.ts` passes. **Fixed this session (`cf6081d8`):** `DEFAULT_PUBLIC_WEB_ORIGIN` resonanz.pro (dead) → lingwave.ai; new committed `frontend/.env.ios` (public origins only) + `build:ios` runs `vite build --mode ios`; `api/voice-sample.ts` wildcard CORS → shared `_shared/cors` helper. **Remaining, owner-side:** Xcode `DEVELOPMENT_TEAM` unset; Supabase dashboard must allow `resonance://auth/callback` redirect + Site URL lingwave.ai; **bundle-id decision** (`pro.resonanz.app` becomes permanent at first App Store Connect upload — rename to a Lingwave id now or never). **Found, deferred with a plan:** `webDir dist` bundles ALL of public/ → ~600 MB IPA (curriculum 460 MB tracked + guided 71 MB tracked + misc 65 MB). OK for private TestFlight (limit 4 GB, WiFi installs); before wider beta, add a native `publicAssetUrl()` + strip `dist/curriculum` — requires funneling DB-persisted relative `thumbnail_url` paths (35 render sites; curriculumDeckBridge writes them into card rows) through one resolver, so it was deliberately NOT half-wired tonight.
- **Skin decision (owner delegated):** keep BOTH skins selectable for beta; new users/devices now default to **glassy** (`a2ee4e16`, per the v1-done definition); PG/classic consolidation stays parked until beta feedback picks a winner. Same commit fixes a real bug: `profiles.skin` was written by ProfileModal but never read back — SkinProvider now restores it when localStorage is empty (mirrors the ThemeProvider restore from `c4ede7a9`).
- **Orphaned dirty files RESOLVED (Sonnet audit, all 8 verdict COMMIT):** they were one coherent finished initiative — usage/cost observability wiring for the five blind `/api/*` provider endpoints + `src/cost_logger.py` real rate table (Python twin) — plus 2 drive-bys (Onboarding rebrand header, lazyWithRetry lint comment). Committed as `02760527` (6 files + the previously-untracked additive migration `20260619120000_pipeline_events_usage_cost_columns.sql`) and `50288b8e`. voice-chat.ts hunk also fixed a stray misplaced `ru:` greeting line that `6f81f2a3` left inside the `buildVoxtralGreeting` call. **Migration NOT yet applied to Supabase** (owner right); code degrades gracefully until then. cost_logger rates partly marked UNVERIFIED — worth an owner skim before trusting cost dashboards.
- **Verified:** typecheck ✓, typecheck:api ✓, eslint changed files ✓, check:i18n 1468/1468 ✓, capacitor contract test ✓ (before and after edits).
- **Open:** owner deploy re-test (theme survive cookie-wipe, header hairline, Speak hero+accordion, mute); iPhone re-tests Lens 2G + 14 UX fixes; the 3 owner-side capacitor items above; curriculum-slimming fast-follow; guided/trophy-song untracked experiments (~97 MB local) still uncommitted clutter (L5).

## 2026-07-08 — Speak re-test feedback pass: casting hero + collapsible grid, tutor mute, theme restore, brand header hover (Fable orchestrating 2 Opus agents; UNCOMMITTED)
- **Owner's Speak re-test verdict (deploy was current):** language inheritance, ready-room lock-in, history, and settings sheet all work as designed; he liked it. The "header lost its purple, back to blue" scare was NOT a stale build — root cause found: `ThemeProvider` writes theme changes to Supabase `profiles.theme` but never read it back, so clearing site data reset localStorage (`resonance-theme`) to default `rainy-day`, whose accent is blue `#7faad4`. The remembered purple is the cosmos brand family the selectable themes never use.
- **Changed:** (1) `ThemeProvider.tsx` — new restore effect: when localStorage has no theme, fetch `profiles.theme` (on mount + SIGNED_IN), migrate/validate, adopt + persist. (2) `theme-contract.css` — `.app-topnav::after` brand hairline (gold→vermillion→magenta→plum→white, the pinned LingwaveWaves ramp, deliberately fixed colors) fading in on hover, both skins, hover-capable devices only, reduced-motion guarded. (3) `LiveDoorCard.tsx` rewritten as a centered max-w-xl vertical HERO (big gold Zap puck, Premium badge, CTA pill; no quota copy — test users unlimited on purpose). (4) `Speak.tsx` casting: CharacterGrid wrapped in a "Browse tutors & voices" accordion (`.speak-browse-panel` grid-rows 0fr↔1fr animation in index.css); defaults collapsed only when the Live door is showing and the user arrived organically, expanded for fil/voice-change/explicit "change tutor"; user toggle overrides stick. (5) Tutor mute: `useVoiceTutor` gained `muted`/`toggleMuted` (localStorage `speak-tutor-muted`, device preference, survives new chat/tutor change); the two autoplay sites skip playback when muted (no tap-to-hear fallback), explicit replay taps still play; toggling ON stops current audio via `stopAllAudio` and turns listenMode off (revealing hidden text); Grok live untouched. Header got an icon-only Volume2/VolumeX toggle next to Settings; settings sheet got a matching "Sound" row.
- **New i18n keys ×3 locales:** `speak.live.cta`, `speak.casting.browseTutors`, `speak.sound`, `speak.soundTooltip`, `speak.soundOffTooltip`, `speak.muteTutor`, `speak.unmuteTutor` (en 1468; de/fr 1468/1468).
- **Verified (final combined pass):** typecheck ✅, eslint on all 5 changed TS files ✅ (0 problems), check:i18n ✅.
- **Open:** UNCOMMITTED (owner commits on request; stage only this session's 7 files — the 8 orphaned dirty files still sit in the tree). Owner asks noted but not built: Stripe/Supabase purchase config + Capacitor shell next; quota visibility deliberately deferred until public use.

## 2026-07-07 — Brain audit (with Protocol OS onboarding); STATE was behind reality on three commits
- **Found:** Lens 2G hardening pass (`ae8ace4e`, 01:20), add-language UX fixes (`99dbb499`, 10:39), and the Speak redesign (`3950c97f`, 18:23) were all committed AND pushed to origin/main, plus the Russian langName repair (`b1737149`) — but STATE.md still described 2G and Speak as UNCOMMITTED and awaiting a commit call. Those committing sessions skipped brain-save. Corrected STATE (header, Lens, Speak, iOS-batch, Next actions). Unknown whether the owner's Speak visual pass happened before `3950c97f` — flagged ⚠️ in STATE for owner confirmation.
- **Also fixed:** ARCHITECTURE.md domains line (resonanz.pro "canonical" → lingwave.ai live, resonanz.pro dead); STATE's `[[project_script_lab]]` wikilink (a Claude-global memory slug, dead inside this brain) → pointer to the DECISIONS Script Lab entry.
- **Verified intact:** the 8 orphaned dirty files match memory's claim exactly (5× api/*.ts, Onboarding.tsx, lazyWithRetry.ts, ../src/cost_logger.py); raw/ capture is harvested; LOG (113 lines) and STATE within size budgets; FABLE_* plan docs are tracked in git (untracked docs/Product+scripts+tests files are old May-era clutter, part of the known L5 problem).
- **New:** Protocol OS onboarded at repo-parent root (`D:\CODING\ResonanceTEST\protocol\` — PROTOCOL/NEXT_STEP/BOARD/LOG, Mode 2 Milestone), registered in the root CLAUDE.md. Coordination now lives there; this brain stays the knowledge layer.

## 2026-07-07 — Lens trust/recognition hardening pass ("2G") + card_text display cleanup built and verified (Codex+Opus+Sonnet investigated, Fable implemented personally; UNCOMMITTED)
- **Changed (Lens):** (1) capture now crops the scan payload to the reticle circle + 35% context margin (`reticleRegion`/`drawVideoRegion` in `Lens.tsx`, cover-math mapping from CSS px to intrinsic video px); frozen preview stays full-frame so there's no zoom jump; falls back to full frame if the reticle rect is unavailable. (2) Reticle enlarged `min(58vw,18rem)` → `min(74vw,23rem)`, vignette hole widened to match. (3) Confidence is no longer shown as a positive signal — the always-on high/medium/low chip is gone (keys deleted from all 3 locales); only `low` surfaces, reworded as a caution pointing at the alternates; prompt gained explicit confidence-calibration rules ("never high when several objects could be the subject") and center-subject framing rules. (4) Server-side single silent retry in `createGeminiVisualScanProvider` for transient 502s (network blip / Gemini 5xx; not timeouts, not 4xx; ≤8s elapsed guard; quota is consumed once so no double charge); fetch injectable for tests. (5) In-Lens language switcher in the topbar (deck-derived + `lingwave_added_languages_<uid>` union, `setActiveLanguage` from context); recap items are language-tagged and bulk save groups per language so a mid-session switch can't save into the wrong deck; switching mid-result resets to live preview. (6) Recap rows gained per-item discard (X) for unsaved items. (7) Korean TTS: `usePronunciation` now normalizes lang names via `getLanguageCode` and explicitly picks a matching speech voice (exact → regional → base prefix); Lens warms the voice list on mount (Script Lab pattern).
- **Changed (card_text display):** `ImagelessCard`/`ImagelessCardThumbnail` now render the translation centered on the black card face (`revealed`-gated on the big card; StudyFlashcard front pinned to `revealed={false}` so study answers can't leak — canvas modes already pass false). The duplicate word render below the card is gone: viewer modal keeps only a speaker icon + IPA; DeckView/DeckViewPG complete-state info rows (word + "Generated" badge) are hidden for text decks; failed/pending rows keep the word but drop the badge; PG carousel shows a speak icon instead of the repeated word.
- **Verified:** typecheck ✓, `tsc -p tsconfig.api.json` ✓, eslint on all 12 changed files ✓ (0 problems), check:i18n ✓ (same 5 pre-existing fr speak.* warns), test-visual-scan 24/24 (6 new retry-contract cases), test-lens-save-mapping 17/17, test-lens-mock-provider 11/11, test-word-display-metadata 69/69.
- **Decided:** see [[DECISIONS]] 2026-07-07 (confidence surfacing policy + reticle crop; photo-on-card declined at MVP).
- **Open:** owner iPhone re-test of the pass (crop quality on menus/text especially — the crop is generous but a tall menu can exceed the reticle); commit call; 2E device matrix still untested.

## 2026-07-07 — Lens live iPhone Safari QA: works end to end, recognition-quality/trust bugs found (owner testing on lingwave.ai; no code changed this entry)
- **Changed:** Nothing in code. Owner tested the deployed Lens feature live on iPhone Safari at `https://lingwave.ai/lens` (domain fix from the prior session applied). Findings recorded verbatim into STATE.md's Lens section.
- **Works:** camera→scan→save→deck loop end to end; correct recognition for digital piano, lamp, headphones; German article chip + example sentence ("Ich höre Musik mit den Kopfhörern") correct; saved words land in a real "Lens — {Language}" deck as studyable cards.
- **Found, not fixed:** (1) one transient "Vision service unavailable" on a first scan that succeeded on immediate retry; (2) Korean tap-to-speak produced no audio (other languages worked) — suspected Web Speech API voice-availability, same class already solved for Script Lab via a voice-list warm-up; (3) no way to switch target language without leaving Lens; (4) **trust-critical:** the reticle circle is cosmetic only, `frameToCanvas()` captures the whole frame not a crop of it, and the model returned wrong answers ("nightstand" for a bedside scene, "suit" for another photo) while still self-reporting high confidence both times — confidence is an unverified model self-report, not calibrated, so it currently can't be used to distinguish good guesses from bad ones; (5) reticle felt too small (repeated back-away-and-retry); (6) every scan (including wrong ones) auto-joins the in-memory recap with no per-item discard, so a bad scan can only be avoided from bulk-save by remembering not to trust "Save unsaved," not by removing it from the list.
- **Decided:** Owner wants a dedicated hardening pass for this, run as a NEW chat with a specific division of labor: Codex investigates what the current code actually does (capture/crop, prompt, confidence handling, TTS path) and reports back — Codex does NOT write the fix; Fable reads that investigation and makes the high-level code changes personally, without delegating to further Opus/Sonnet implementer subagents. Continuation prompt handed to the owner to paste into the fresh chat.
- **Open:** the fix pass itself (tracked as the natural next Lens phase after 2A–2D, informally "2G" in conversation, not yet named in the master plan doc). Device-only Capacitor/WKWebView QA (torch, orientation, backgrounding, offline-mid-scan) is still untested — today's QA was Safari only.

## 2026-07-06 — iOS/mobile UX polish batch: 14 fixes across app + landing (Fable orchestrating Codex + 5 Opus/Sonnet agents; UNCOMMITTED)
- **Changed (all in `frontend/src`, from owner's iPhone test feedback):** (1) `LanguageCluster.tsx` rewritten: always openable, new `onAddLanguage` prop renders a "+ Add language" row that expands the not-yet-owned `WIZARD_LANGUAGES` in-panel; `Dashboard.tsx`/`DashboardPG.tsx` persist added languages per-user in localStorage (`lingwave_added_languages_<userId>`), union them into `availableLanguages`, and show the first-run empty state for a deckless language. (2) `Decks.tsx` swipe pill row replaced by the reused collapsed `LanguageCluster` (add-language there routes to `/generate` preselected); `.decks-language-cluster` local `--dashboard-glass-*` token fallback in `index.css`. (3) Decks Generate button restyled `.decks-generate-cta` (accent gradient per `.home-welcome-door--primary` recipe); glassy `.decks-glass-add` bumped in `glass-orb.css`. (4) Runner game fully removed: registry entry, `/games/runner` route + flag, `ComingSoonOverlay`/`ComingSoonPlaceholder` components, entire `games/runner/` dir (~110 files incl. assets), 16 `games.runner.*` keys ×3 locales; slicer untouched; `study_mode='runner'` data guards in `useStudyStreak.ts`/`dailyHabits.ts` deliberately kept. (5) `ProfileModal.tsx`: Delete account now hidden behind an "Advanced options" collapsible (resets closed on modal close). (6) `StudyModeSelector.tsx`: 2-col mobile grid, odd last tile centered at half-width, desktop flex unchanged. (7) `ToastProvider.tsx`: toasts now sit above the mobile bottom nav (`--mobile-bottom-nav-space`) and use `--surface-glass-strong` (old `--bg-card` var was undefined — toasts were literally transparent); `LevelDetailPage.tsx` gets durable `songRequested` button state + "Go to Music" link after level-song submit. (8) `AppLayout.tsx`/`PolishGlassLayout.tsx` Suspense fallbacks centered (`min-h-[calc(100dvh-8rem)]`). (9) `HomeAccountStrip.tsx`: 1fr/auto/1fr grid centers the brand; coin is icon-only (count in aria-label; RedeemCodeDialog already shows balance). (10) `index.css` mobile welcome spacing: padding-top 0.85→1.25rem, compact-height margins un-zeroed with clamp floors. Landing: (11) `memory-wave` scoped `background-position` shimmer on the Melody/Motion/Meaning gradient words (staggered 0/0.6/1.2s, reduced-motion guarded, hero untouched) in `cosmos.css`+`MemoryMechanicSection.tsx`; (12) `ModePortals.tsx` wall rows deduped vs CreatorRail (apple/hiking/bread/basketball → cat/lighthouse/cheese/waterfall, verified against set-c manifest); (13) CTA copy: `landing.ctaHeadline` → "Ready to learn in waves?", `landing.cta`+`landing.heroCta` → "Start here" (en/de/fr).
- **New i18n keys (en/de/fr):** `dashboard.addLanguage`, `profile.advancedOptions`, `categories.levelSongQueued`, `categories.goToMusic`. Removed: `games.runner.*` (16 keys ×3).
- **Verified (Fable, final combined pass):** typecheck ✅, check:i18n ✅ (en 1461; de 1461/1461; fr warn-only speak.* gaps), eslint on all 18 changed files ✅ (0 errors, 1 pre-existing warning in LevelDetailPage), `npm run build` ✅ 3.6s (runner chunks gone, SlicerGame chunk intact, main ~343 kB). Capacitor-safe: tap-first, safe-area vars, no new network surfaces.
- **Open:** UNCOMMITTED (owner commits on request). Owner should re-test on iPhone: add-language flow, decks selector, song-request confirmation, header centering, welcome spacing. Loading-perf ask ("home page sometimes slow") noted but NOT addressed this session — candidate next task.

## 2026-07-06 — Russian tiers 1+3+landing shipped (`6f81f2a3`); guided (Tier 5) scoped, not built (owner decision)
- **Changed (committed 6f81f2a3, pushed):** Promoted Russian from Tier 0 to browsable+speakable via the add-target-language skill. **Categories (Tier 1):** added `ru` to `StaticCategoryTargetLanguageCode` + experimental metadata entry (script: Cyrillic) in `data/categories.ts`; generated a Russian term for all **1,850** concepts in `staticCategoryTranslations.ts` (6,474 insertions, 0 deletions — verified byte-identical no-op regen before merge). Translations produced by 19 per-category subagents using en/de/fr/es cross-refs to disambiguate sense (caught many source homograph errors, e.g. honey→мёд not Schatz; conductor→кондуктор not дирижёр), citation form, Cyrillic with ё. **462/1850 carry needsReview** (mostly exotic long-tail fruits/nuts/spices) — status stays `experimental` until a native-review pass. **Speak (Tier 3a, Gemini path, code-only):** `LANGUAGE_CONFIG['ru']` in pedagogy.ts, ru retry phrase in voice-chat.ts (extracted as a 1-line hunk to avoid the concurrent session's ~114 uncommitted lines there), `isSpeak` + `SPEAK_ORDER` + Русский history label. Lights up both Grok "Live" (default) and Gemini "Voices"; no Voxtral change (rides Gemini like ko/ceb). **Landing:** isLanding + landingColor + LANDING_ORDER + greeting (Привет) + drift (помнить).
- **Also fixed:** the pre-existing `test:i18n-display-labels` failure — removed 2 assertions for `generate.words.typeOwn`/`pickCategory` (keys deleted in an earlier CategoryPicker refactor, unreferenced by product code; kept the suite's real language-label coverage). Left `test:generate-responsive-layout` alone — it fails on the same removed-CategoryPicker-UI root cause but reconciling it means rewriting several asserts against that unrelated refactor.
- **Verified:** typecheck (src+api) ✅, lint ✅ (0 errors, 1 pre-existing warning), check:i18n ✅ (no new gaps; fr speak.* still warn-only), test:i18n-display-labels ✅, build ✅ — Russian category data lands in its own lazy `categories-*.js` chunk (~637 kB); main `index` bundle unchanged (~344 kB); guided chunk untouched.
- **Guided (Tier 5) — deliberately NOT built, scoped for owner decision.** It's ~5,000+ lines of interlocking flagship content (10 lessons × up to 3 vibe variants; corePhrase↔build.chips↔speakTarget.requiredTokens↔typeRecall must stay internally consistent), needs extending `GuidedTargetLanguage`+`GuidedSpeakLocale`(+ru-RU)+`GUIDED_TARGET_LANGUAGE_SPEAK_LOCALES`+the 90-entry `getGuidedTodayPathOptions` list + `todayLanguage.ts` TARGET_LANGUAGES. The runtime vibe-fallback resolver wasn't located in src/ (grep only hit guidedLessons.ts itself); existing corpus is 514 draft / 1 final. Recommended: single-vibe (bright + fallbackVibeId) 10-lesson path as a tractable playable unit, authored via subagents against a real existing lesson template + an internal-consistency validator, with a native-review pass — but pending owner go-ahead + their categories test first (their stated sequence).
- **Open:** owner to test Russian categories/Speak/landing on the deploy; decide guided go-ahead + vibe count; Tier 2 static TTS for Russian categories (the "silent cards" gap) is a separate approved paid batch the owner runs.

## 2026-07-06 — Lens pushed to main (`0d42e989`), Vercel deploying; owner applied migrations + confirmed API key
- **Changed:** Owner confirmed both Lens migrations applied to Supabase and `GOOGLE_AI_API_KEY` already set in Vercel, then asked to push. Staged and committed only the Lens-scoped diff by hand (NOT `git add -A`) because the shared working tree had ~136 dirty entries, including unrelated pre-existing uncommitted work in `api/{extract-vocabulary,guided-transcribe,suggest-words,translate-and-ipa,voice-chat}.ts`, `src/pages/Onboarding.tsx`, `src/utils/lazyWithRetry.ts`, `../src/cost_logger.py`, and a same-file collision: the concurrent language-expansion session's uncommitted fix for misplaced `langName.Russian`/`langName.ru` keys sat inside the same `translations.ts` hunks as new `lens.*` keys. Extracted just the three `lens.*` locale hunks into a standalone patch and applied with `git apply --cached`, leaving the Russian langName fix unstaged for its own session. Also caught and excluded `ADVERSARIAL_REVIEW_CAMERA_VOCABULARY_FINDINGS.md` — despite the name, it's an unrelated pre-existing slicer-game doc, not Lens. Ran the previously-unexecuted `test-lens-mock-provider.ts` (11/11) as a final pre-commit check.
- **Verified:** commit `0d42e989` (27 files, 3527 insertions) pushed clean to `origin/main`; working tree afterward still shows exactly the pre-existing unrelated dirty files, untouched.
- **Open:** Vercel deploy in flight. Next is owner iPhone QA — the 2E device matrix (WKWebView camera permission/preview, orientation, torch, backgrounding, offline mid-scan, full scan→save→deck→study loop). This is the first real test of video `getUserMedia` in the Capacitor shell.

## 2026-07-06 — Language-expansion system built + Russian shipped (Fable orchestrating 4 agents + Codex + 3 proof/review agents)
- **Changed:** Executed `docs/Product/FABLE_LANGUAGE_EXPANSION_BRIEF.md`. (1) Five-way investigation (4 subagent reports + Codex full-repo sweep task-mr8volaj-potxgg) synthesized into `docs/Product/FABLE_LANGUAGE_ARCHITECTURE.md` — the 4-disjoint-registry map, launch tiers 0–5, Supabase name/code conventions, api/ allow-set contract, base-locale cascade, 10 landmines. (2) Three new skills: `.claude/skills/add-target-language` (tiered recipe), `add-base-locale` (Locale-union cascade, RTL-blocked), `review-language-addition` (second-agent QA recipe). (3) Proof runs by fresh agents using ONLY the skills: Russian added at Tier 0 (`languages.ts` entry `value:'Russian'/ru/#14b8a6/isWizard`, 6 langName keys en/de/fr, FlagIcon RU in all three maps) and Russian Cyrillic alphabet (`data/scripts/russianCyrillic.ts`, 33 letters, 4 sections, BGN/PCGN practical transcription, signs as advanced marks with homophone tag; registry entry; 83-line validator in `test-script-lab-data.ts`). (4) All 3 friction logs (7+10+8 items) folded back into the skills (langName ISO-alias twin block, COUNTRY_CODE_MAP, LATIN_FALLBACK_STACK mechanism, letter-name listen-quiz behavior, <4-symbol sections unquizzable, stressed-vowel example-word rule, dirty-tree diff scoping for reviews, chunk-size heuristics). (5) Review agent (via the new review skill) caught 2 real defects — unstressed vowels in А/И example words (автобус→август, игра→имя) — both fixed. Side fix: fr `langName.Portuguese/Arabic` casing normalized to capitalized (was inconsistent with 11 sibling entries).
- **Verified:** `npm run typecheck` ✅, `npm run lint` ✅ (0 errors, 1 pre-existing warning), `npm run check:i18n` ✅ (de 1473/1473, fr 1468/1473 = pre-existing speak.* warn-only), `npm run test:script-lab` ✅ 2540/2540, `npm run build` ✅ (russianCyrillic own 19.3 kB lazy chunk; guidedLessons unchanged 2.84 MB async chunk; main ~345 kB).
- **Found, not fixed (pre-existing on HEAD):** `npm run test:i18n-display-labels` fails — `generate.words.typeOwn` was removed from translations.ts but `test-i18n-display-labels.ts:18` and `test-generate-responsive-layout.ts:88` still assert it; no product code references it. Owner call: fix tests or restore key.
- **Open:** Russian is wizard-only (Tier 0) by design — Tier 1 categories (~1,850 terms), Tier 3a Speak (api/ allow-sets), landing showcase are separate approvable steps; Script Lab audio batch for Cyrillic likewise (browser speech works today). Base-locale skill unproven by a live run (no 4th locale requested yet).

## 2026-07-06 — Lens 2A–2D complete and Fable-verified: all checks green, ready for deploy + iPhone QA (Fable directing 4 Codex packets)
- **Changed:** Full Lens build finished in one session (Phase Zero plan → working feature, uncommitted). Fable's final pass on top of the four Codex packets: fixed `scripts/test-visual-scan.ts` runtime imports (named value imports from `api/` modules fail under tsx CJS interop — switched to the house default-import idiom from `test-stripe-billing-shell.ts`); aligned Lens gold accents to `var(--accent-warm, #f7c843)` in `index.css` (was off-brand `#f2d68a`); plan doc `FABLE_VISUAL_LENS_MASTER_PLAN.md` updated with owner answers (flag ON for testers; home-tile placement, no nav-tab fight; 30 scans/day approved) and status → built.
- **Verified by Fable (sandbox EPERM notes in the Codex entries below are superseded — all checks ran clean locally):** `npm run typecheck` ✅, `npx tsc -p tsconfig.api.json` ✅, `npm run lint` ✅ (0 errors, 1 pre-existing warning), `npm run check:i18n` ✅ (de 1471/1471; fr gaps pre-existing speak.*), `tsx scripts/test-visual-scan.ts` ✅ 18/18, `tsx scripts/test-lens-save-mapping.ts` ✅ 17/17. Migration SQL reviewed line-by-line against `submit_imageless_import`/`append_imageless_cards` (guard idiom, constraint lists, columns all match). Route placement (protected fullscreen, lazy), flag gating, `publicApiUrl` native-shell safety, and `NSCameraUsageDescription` all inspected directly.
- **Review findings fixed along the way:** 2A camera-stream leak on rescan, dead-end unsupported/empty result state, non-cancellable analyzing sheet (all fixed in 2C); recap falsely marking skipped items saved (fixed in 2D).
- **Open (deploy gate):** everything is UNCOMMITTED in `orchestrator/frontend`. To ship: commit + push (Vercel), apply migrations `20260706020000_visual_scan_quota_action.sql` + `20260706030000_submit_lens_save.sql` to Supabase, confirm `GOOGLE_AI_API_KEY` exists in Vercel env. Then owner iPhone QA (2E device matrix: WKWebView video getUserMedia, permission, orientation, torch, backgrounding). 2F decision gate after dogfood: tab promotion, name sign-off.

## 2026-07-06 — Lens Phase 2D text/menu refinement implemented (Codex; uncommitted)
- **Changed:** Implemented Phase 2D refinement packet on top of uncommitted 2A–2C. `frontend/src/pages/Lens.tsx` now gives each multi-item text/menu row its own save icon state, disables saved/already-known rows, adds a sheet-local "save all" RPC action for current scan rows, and stops falsely marking ambiguous mixed bulk results as saved when the RPC returns only counts. Mixed insert/skip results now show count-based copy instead of mutating unknown per-row state. `frontend/src/index.css` adds row-save layout styles using theme variables for the new surfaces.
- **API prompt:** `frontend/api/_shared/visualScanProvider.ts` now explicitly handles source-language edge cases: target-language text is reading/meaning passthrough, base-language text translates normally, third-language text translates the concept and may mention the source language in `base_text`.
- **Tests/i18n:** `frontend/scripts/test-visual-scan.ts` adds a text reading-mode passthrough normalization case. Added `lens.action.saveAll` and `lens.save.bulkMixed` to en/de/fr in `frontend/src/lib/translations.ts`.
- **Verified:** `npm run typecheck` exit 0 (with local `npm.ps1` access-denied warning); `npx tsc -p tsconfig.api.json` exit 0 (with `npx.ps1` access-denied warning); `npm run lint` exit 0 with one pre-existing warning in `src/pages/categories/LevelDetailPage.tsx`. `npm run check:i18n`, `npx tsx scripts/test-visual-scan.ts`, and `npx tsx scripts/test-lens-save-mapping.ts` all failed to execute because `tsx`/esbuild hits sandbox `spawn EPERM`.
- **Open:** No live DB/RPC calls were made. Full `tsx` script verification and real Supabase save behavior remain pending in an environment that can spawn esbuild.

## 2026-07-06 — Lens Phase 2C save/import path implemented (Codex; uncommitted)
- **Changed:** Added SQL-only migration `frontend/supabase/migrations/20260706030000_submit_lens_save.sql` defining authenticated `submit_lens_save(p_target_language,p_base_language,p_items)` as a `security definer` RPC. It locks per caller/language, finds or creates `card_text` deck `Lens — {Language}`, inserts complete `words` rows with real columns (`word`, `translation`, `ipa`, `pos`, `article`, `example`, `example_gloss`), stores `metadata.origin='lens'` plus transliteration, skips duplicates in the Lens deck by normalized `word_slug` with fallback to `lower(btrim(word))`, updates `word_count` by actual inserts, and returns `{deck_id, inserted, skipped}`. Migration was not applied.
- **Client:** Added `src/lib/lensSaveMapping.ts`, `src/hooks/useLensSave.ts`, and `scripts/test-lens-save-mapping.ts`. Wired `pages/Lens.tsx` real Save and recap bulk-save through the RPC, added deck links after save/duplicate results, added `get_user_words_for_language` 300-word duplicate hints, kept recap saved/already-present flags in sync, fixed rescan camera stream leakage by stopping old tracks before reacquiring, added cancel for frozen analyzing, and added a graceful empty/unsupported result sheet. Added required en/de/fr i18n keys and Lens save-state CSS.
- **Verified:** `npm run typecheck` exit 0 (with local `npm.ps1` access-denied warning); `npx tsc -p tsconfig.api.json` exit 0 (with `npx.ps1` access-denied warning); `npm run lint` exit 0 with one pre-existing warning in `src/pages/categories/LevelDetailPage.tsx`. `npm run check:i18n` and `npx tsx scripts/test-lens-save-mapping.ts` both failed to execute because `tsx`/esbuild hits sandbox `spawn EPERM`.
- **Open:** No local DB exists to execute the RPC; SQL was reviewed by eye against `submit_imageless_import` and `append_imageless_cards`. Full end-to-end save into Supabase and device WKWebView camera QA remain pending.

## 2026-07-06 — Lens Phase 2B real scan path implemented (Codex; uncommitted)
- **Changed:** Added `frontend/api/visual-scan.ts` with named `POST`, bearer auth, `consumeApiQuota(user,'visual_scan')`, `readJsonWithLimit` 2 MB cap, explicit base64/decoded-size guards, strict response normalization to the Phase 2A `lensTypes` contract, safety stripping, and usage-event writes (`feature='lens'`). Added `api/_shared/visualScanProvider.ts` with Gemini Flash-Lite primary provider (`GOOGLE_AI_API_KEY`, model in one constant), structured lexical prompt, Gemini JSON schema output, parse/validate helper, and an OpenRouter vision fallback stub behind the same interface. Added Gemini vision cost helper to `api/_shared/usageCost.ts`.
- **Quota/migration/admin:** Added `visual_scan` to `api/_shared/quota.ts`, admin Quotas action order/label, and wrote SQL-only migration `frontend/supabase/migrations/20260706020000_visual_scan_quota_action.sql` extending both quota CHECK constraints and seeding 30/day free-tier config. Migration was not applied.
- **Client/tests:** Added `src/lib/lensApiProvider.ts` and made `useLensScan` default to the real API provider while preserving the mock provider and dev 404 fallback. Lens error handling now shows localized quota-exceeded copy in en/de/fr. Added offline `scripts/test-visual-scan.ts` endpoint contract script with mocked provider cases: object success, menu clamp, safety, oversized body, quota exceeded, malformed model JSON.
- **Verified:** `npm run typecheck` exit 0; `npx tsc -p tsconfig.api.json` exit 0; `npm run lint` exit 0 with one pre-existing warning in `src/pages/categories/LevelDetailPage.tsx`; exact `tsx scripts/test-visual-scan.ts` unavailable on PATH, and local `node_modules/.bin/tsx.cmd scripts/test-visual-scan.ts` fails with sandbox `spawn EPERM`, so the script was typechecked cleanly via `tsc`.
- **Open:** Phase 2C save/import RPC and real deck persistence remain unbuilt; Phase 2D richer text/menu list UX is still later. The Gemini vision pricing constant is an in-repo estimate marked unverified.

## 2026-07-06 — Visual/Lens camera feature: Phase Zero discovery + master plan (Fable directing Codex; docs only, no code)
- **Changed:** Founder's GPT master prompt for a camera "point at the world, learn the word" tab was reviewed and re-architected from first principles. Codex (session 019f35b9-8d9d…) did the deep codebase investigation and then an adversarial review of the resulting docs; findings folded in. Deliverables: `D:\CODING\ResonanceTEST\investigations\VISUAL_LENS_CODEBASE_DISCOVERY_2026_07_06.md` + `docs/Product/FABLE_VISUAL_LENS_MASTER_PLAN.md` (working name **Lens**, flag `VISUAL_LENS_ENABLED`, route `/lens` from a dashboard tile — no nav-tab fight at MVP).
- **Architecture decided:** capture-first (shutter tap, no continuous scanning/auto-speak); one Gemini-Flash multimodal call returns the whole lexical card (object/OCR/menu classification, natural target word, translation, transliteration, article, example) — fal.ai rejected (not in stack); saves land in auto "Lens — {language}" `card_text` decks via ONE new RPC `submit_lens_save`; no new tables, one migration (RPC + `visual_scan` quota action on `api_quota_config`/`api_quota_usage` CHECKs + `quota.ts` union + admin Quotas UI); browser TTS tap-to-speak only; frames never stored. Cost ≈ 1–3 ¢/user/month at 30 scans/day quota.
- **Adversarial review caught (important for implementers):** imageless RPCs whitelist origins (`manual|tutor_extraction|category`) and accept only `{word,translation,ipa,is_phrase}` — NOT reusable for Lens as-is; `words` has no transliteration column (→ metadata); mic getUserMedia precedent doesn't prove *video* in WKWebView (device spike pulled into Phase 2A); `usageCost.ts` lacks a vision cost helper; endpoint needs explicit `readJsonWithLimit` body cap.
- **Open:** owner questions — beta exposure (recommend flag-off for TestFlight), eventual tab promotion (which of 6 yields), free quota level, name sign-off. Pre-existing gap rediscovered: `/api/generate-imageless-tts` referenced by hooks but doesn't exist (blocks Lens Phase C audio).

## 2026-07-06 — Script Lab hardened after adversarial review; Korean audio shipped; pushed to main (Fable, session continuation)
- **Changed:** Owner approved the audio batch: 141 ElevenLabs clips (Jini, one of the six QA'd ko voices; 198 chars total) generated offline via new `frontend/scripts/generate-script-lab-audio.ts` into `public/scripts/korean-hangul/` (~2.1 MB), manifest auto-written to `src/lib/scriptlab/audioManifest.generated.ts` and wired into `lib/scriptlab/audio.ts` (asset first, speech fallback — no SQL, no runtime provider). Adversarial review (Codex rescue + independent Fable-prompted agent, both report-only) found no criticals; fixed: quiz now samples ALL sections incl. advanced (homophone guard was dead code before), final-t example syllable 곧→앋 (consonant-initial syllables made listen questions ambiguous — ㄱ and ㄷ both audible), empty-quiz guard, quiz score persisted on last answer (was lost if user switched tabs within 900 ms), SymbolDetailPanel focus placement/trap/restore + body scroll lock, 44px touch targets, labeled nav buttons, script-switch state reset (stale detail/tab), chunk-load error state with reload, speechSynthesis cancel on page unmount + voice-list warm-up. Test suite now 1541 checks (added: bare-jamo speech ban, composed-romanization cross-check, ≥4-symbol quizability). `.gitignore`: `.claude/skills/` and `public/scripts/**/*.mp3` now tracked (were silently ignored — the mp3 ignore would have kept audio out of deploys). New `docs/Product/FABLE_LANGUAGE_EXPANSION_BRIEF.md` = hand-off brief for the add-a-language skills agenda (target languages vs base locales, deliverables, constraints).
- **Verified:** test:script-lab 1541/0 ✅, typecheck ✅, eslint scriptlab files ✅ (0), check:i18n ✅ (de full; fr gaps pre-existing speak.*), build ✅. Owner mobile QA still pending.
- **Open:** owner visual QA of /alphabet on the deployed site; Russian requires adding the language itself first (see expansion brief) before a Cyrillic pack is discoverable; possible later move of the alphabet tile from Study hub to Library (owner will judge placement).

## 2026-07-06 — Launch-readiness cleanup pass complete: video boundary drawn, ~2k dead lines removed (Fable, session A)
- **Changed:** Video lane tile hidden from new-deck wizard behind `VIDEO_LANE_ENABLED=false` (new `frontend/src/lib/productFlags.ts`; one shared `ProductLaneStep` covers both skins). Removed provably dead code: `components/stages/*` (8 DAW panels), `StageSettings.tsx`, `pages/Settings.tsx`, `GeminiAccentPicker.tsx`, `AppSidebar.tsx` (−~2,000 lines). Fixed video-era study empty-state copy (`study.addCardsHint`/`study.generateFirst`) to media-neutral wording in en/de/fr. Rewrote stale `README.md` (described the retired local DAW). Wrote `docs/Refactors/FABLE_{CODE_CLEANUP_AUDIT_REPORT,VIDEO_DEPRECATION_BOUNDARY,SAFE_CLEANUP_PATCH_PLAN}.md`.
- **Files:** see commits; docs in `docs/Refactors/`; brain updated.
- **Commits:** `6b5991ce` (hide lane), `ade651bc` (dead cluster), `baaeea7c` (AppSidebar), `b749a300` (i18n copy; staged surgically around session B's uncommitted scriptlab keys), plus docs commit.
- **Verified:** typecheck ✅, eslint changed files ✅ (0 new), full build ✅ after each deletion, check:i18n ✅ (only known warn-only fr `speak.*` gaps), `git diff --check` ✅.
- **Key findings:** Phase 1C real (all paid endpoints gate auth+quota RPC); direct Supabase writes contained (generation/credits/roles RPC-only); no `engines/` dup; local FastAPI can't reach production (image never ships it, `STORAGE_MODE=cloud` baked); `videos` bucket shared with card PNGs (`card_worker.py:710`) — DO NOT TOUCH; `start_cloud.py` boot-requires video-era `POD_URL`/`POD_AUTH_TOKEN` (footgun, L4). Codex deep-scan gathered evidence but wedged twice writing its packet — findings converged with Fable's direct checks and were folded into the audit report; `CODEX_REPO_INTEL_PACKET.md` never materialized.
- **Decided:** see [[DECISIONS]] 2026-07-06 (video deprecation boundary; evidence-first delegated cleanup).
- **Open:** later-pass ladder L1–L8 in the patch plan (skin-pair consolidation blocked on glassy-only decision; unreachable video wizard steps; POD_* decoupling; root clutter archive call).

## 2026-07-06 — Script Lab V1 shipped: Korean/Hangul alphabet module at /alphabet (Fable orchestrating Codex + Opus + Sonnet)
- **Changed:** Built the reusable writing-system module ("Script Lab", user-facing "Alphabet"). Generic layer: `frontend/src/lib/scriptlab/{types,registry,audio,progress,hangul}.ts`; Korean content: `src/data/scripts/koreanHangul.ts` (47 symbols — 14 consonants, 10 vowels, 7 batchim, 5 doubles, 11 compound vowels; Revised Romanization, IPA, en/de/fr notes/meanings, homophone tags, Unicode block composition). UI (built by Opus subagent to Fable's contract): `src/pages/ScriptLab.tsx` + `src/components/scriptlab/{ScriptAudioButton,SymbolGrid,SymbolDetailPanel,BuildMode,QuizMode}.tsx` — Learn/Build/Quiz tabs, bottom-sheet detail panel, theme-token-only styling (works in both skins). Wiring: routes `/alphabet` + `/alphabet/:scriptId` in both skin groups of `App.tsx`, lazy chunk via `routeImports.ts`, registry-gated "Alphabet" tile in `StudyModeSelector.tsx`, `scriptlab.*`/`study.mode.script` keys in en/de/fr. Tests (Sonnet subagent): `scripts/test-script-lab-data.ts` (`npm run test:script-lab`) — 1392 checks incl. composition cross-checks of every example syllable. Docs: `docs/Product/FABLE_SCRIPT_LAB_ARCHITECTURE.md`, `FABLE_HANGUL_V1_PRODUCT_SPEC.md`, `FABLE_SCRIPT_AUDIO_PROVIDER_PLAN.md`. Skill for future scripts: `.claude/skills/add-script-lab-language/SKILL.md`.
- **Verified:** `npm run typecheck` ✅, eslint on all new/changed files ✅ (0 problems), `npm run check:i18n` ✅ (de 1411/1411; fr gaps are pre-existing speak.* warn-only), `npm run test:script-lab` ✅ (1392 passed), `npm run build` ✅ (ScriptLab is its own lazy chunk), `git diff --check` ✅. NOT verified: manual mobile smoke in an authed browser (no credentials in this session) — checklist is in the Hangul spec doc.
- **Audio:** V1 = declared spoken-text specs ({itemId, text}: letter names 기역, never bare jamo) → static asset manifest (empty) → browser speechSynthesis ko-KR. No paid provider calls anywhere. Upgrade path (recommended: reuse static-TTS pipeline's six QA'd ko voice profiles) in the audio plan doc.
- **Decided:** See [[DECISIONS]] 2026-07-06 Script Lab entry (route choice, rejected alternatives, abjad blockers).
- **Open:** owner mobile/visual QA; audio asset batch (needs approval); dashboard `dashboard-library-tile` entry point; Cyrillic/Kana data packs as first reuse proof; Arabic blocked on RTL + letterform type extensions.
## 2026-07-06 — Speak Phase 1 palette re-skin landed (Fable, same session as UX audit)
- **Changed:** Re-skinned the entire Speak selection flow from hardcoded indigo/slate/gray onto cosmos tokens — zero behavior change. `index.css` speak-* primitives (page shell, aura, ripple, glass card, start button, mode emblems) now use `--accent`/`--accent-2`/`--surface-*`/`--border-*` via color-mix; `ProviderToggle` now uses the `theme-panel`/`theme-chip-active` pill pattern; `GrokPicker`, `GeminiModeVoicePicker`, `CharacterGrid`, `SpeakHistoryPanel`, `ExtractWordsModal`, `VoiceSampleButton` fully tokenized (history transcript bubbles now reuse `speak-message-user/assistant`; provider badges use accent/accent-2 instead of violet/cyan).
- **Owner decision captured:** provider consolidation (tutor catalog, hiding the Live/Characters/Voices toggle) is ON HOLD — Grok realtime is materially more expensive than Voxtral/Gemini, so the provider distinction is cost-relevant and the owner will decide the consolidation shape himself. Phase 2+ of the Speak plan needs his sign-off; Phase 1 (visual only) was approved and is done.
- **Verified:** typecheck ✅, eslint on all 7 changed speak files ✅, `git diff --check` ✅. i18n untouched (class-only edits). Visual QA in a browser still pending — recommend a quick pass over the 3 Grok steps, Gemini 3 stages, character grid, history panel, extract modal in both skins.
- **Files:** `src/index.css`, `src/components/speak/{ProviderToggle,GrokPicker,GeminiModeVoicePicker,CharacterGrid,SpeakHistoryPanel,ExtractWordsModal,VoiceSampleButton}.tsx`.

## 2026-07-06 — UX coherence audit for TestFlight (Fable pass, parallel session)
- **Changed:** Full UX-coherence audit of the learner frontend (Codex inventory + Sonnet study audit + direct Speak/prompt review). Wrote 4 product docs: `docs/Product/FABLE_UX_COHERENCE_AUDIT.md`, `FABLE_SPEAK_PRODUCTIONIZATION_PLAN.md`, `FABLE_STUDY_AND_CARD_RENDERING_COHERENCE_PLAN.md`, `FABLE_TESTFLIGHT_FEATURE_STRATEGY.md`. Landed 7 small verified UI fixes: StudyCanvas raw "Loading…"→i18n; StudyAudio cyan `--accent` fallback removed; OrbDock rainbow placeholders→glass tokens; `#fb923c`→`var(--pg-accent-gold)` in StudyPG/StudyImagePG; alt-reveal gating + `object-contain` parity in Study/StudyPG; new `src/lib/speakCuration.ts` role flag; theatrical Gemini accents now admin-only (GeminiModeVoicePicker).
- **Files:** `frontend/src/pages/Study{Canvas,Audio,,PG,ImagePG}.tsx`, `frontend/src/components/OrbDock.tsx`, `frontend/src/components/speak/GeminiModeVoicePicker.tsx`, `frontend/src/lib/speakCuration.ts` (new), `docs/Product/FABLE_*.md` (4 new).
- **Verified:** `npm run typecheck` ✅, eslint on changed files ✅ (0 new), `npm run check:i18n` ✅ (only pre-existing warn-only fr gaps).
- **Key findings:** Speak selection flow is the biggest brand seam (hardcoded indigo/slate palette + 3-provider wizard exposing 5+28+26 voices/10 modes/27 accents); Study is one component written as four drifting files; `GeminiAccentPicker.tsx` and `pages/Settings.tsx` are dead code; landing experiment routes `/a` `/b` `/landing*` are publicly routable; no learner feature-flag registry existed before `speakCuration.ts`.
- **Open:** Speak Phase 1 re-skin + Phase 2 curation (see plan doc phasing); VideoPlayer.tsx i18n left to the video-deprecation pass; glassy-only-for-beta skin decision needs owner sign-off.

## 2026-07-06 — Second Brain installed; launch-readiness cleanup pass begun
- **Changed:** Installed `memory/` (INDEX/STATE/DECISIONS/ARCHITECTURE/LOG + raw/notes/archive) from a fresh evidence survey; wired the protocol block into `AGENTS.md`/`CLAUDE.md` at repo root.
- **Files:** `memory/*`, `AGENTS.md`, `CLAUDE.md`.
- **Context:** Session mission is a token-efficient cleanup + launch-readiness audit: Codex generates a repository intelligence packet (`docs/Refactors/CODEX_REPO_INTEL_PACKET.md`); Fable reviews, writes `FABLE_CODE_CLEANUP_AUDIT_REPORT.md`, `FABLE_VIDEO_DEPRECATION_BOUNDARY.md`, `FABLE_SAFE_CLEANUP_PATCH_PLAN.md`, then lands small evidence-backed patches.
- **Decided:** See [[DECISIONS]] 2026-07-06 entries (video deprecation boundary; delegated evidence-first cleanup).
- **Open:** Phase 1C–1H runtime verification; exact list of video UI surfaces safe to hide (pending packet).
