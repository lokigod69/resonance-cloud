# Session Log
Newest first. Append-only — entries are never rewritten.
When this file exceeds ~300 lines, move the oldest half to `archive/log-2026.md`.

## 2026-07-06 — Launch-readiness cleanup pass complete: video boundary drawn, ~2k dead lines removed (Fable, session A)
- **Changed:** Video lane tile hidden from new-deck wizard behind `VIDEO_LANE_ENABLED=false` (new `frontend/src/lib/productFlags.ts`; one shared `ProductLaneStep` covers both skins). Removed provably dead code: `components/stages/*` (8 DAW panels), `StageSettings.tsx`, `pages/Settings.tsx`, `GeminiAccentPicker.tsx`, `AppSidebar.tsx` (−~2,000 lines). Fixed video-era study empty-state copy (`study.addCardsHint`/`study.generateFirst`) to media-neutral wording in en/de/fr. Rewrote stale `README.md` (described the retired local DAW). Wrote `docs/Refactors/FABLE_{CODE_CLEANUP_AUDIT_REPORT,VIDEO_DEPRECATION_BOUNDARY,SAFE_CLEANUP_PATCH_PLAN}.md`.
- **Files:** see commits; docs in `docs/Refactors/`; brain updated.
- **Commits:** `6b5991ce` (hide lane), `ade651bc` (dead cluster), `baaeea7c` (AppSidebar), `b749a300` (i18n copy; staged surgically around session B's uncommitted scriptlab keys), plus docs commit.
- **Verified:** typecheck ✅, eslint changed files ✅ (0 new), full build ✅ after each deletion, check:i18n ✅ (only known warn-only fr `speak.*` gaps), `git diff --check` ✅.
- **Key findings:** Phase 1C real (all paid endpoints gate auth+quota RPC); direct Supabase writes contained (generation/credits/roles RPC-only); no `engines/` dup; local FastAPI can't reach production (image never ships it, `STORAGE_MODE=cloud` baked); `videos` bucket shared with card PNGs (`card_worker.py:710`) — DO NOT TOUCH; `start_cloud.py` boot-requires video-era `POD_URL`/`POD_AUTH_TOKEN` (footgun, L4). Codex deep-scan gathered evidence but wedged twice writing its packet — findings converged with Fable's direct checks and were folded into the audit report; `CODEX_REPO_INTEL_PACKET.md` never materialized.
- **Decided:** see [[DECISIONS]] 2026-07-06 (video deprecation boundary; evidence-first delegated cleanup).
- **Open:** later-pass ladder L1–L8 in the patch plan (skin-pair consolidation blocked on glassy-only decision; unreachable video wizard steps; POD_* decoupling; root clutter archive call).

## 2026-07-06 — Script Lab V1 shipped: Korean/Hangul alphabet module at /alphabet (Fable orchestrating Codex + Opus + Sonnet)
- **Changed:** Built the reusable writing-system module ("Script Lab", user-facing "Alphabet"). Generic layer: `frontend/src/lib/scriptlab/{types,registry,audio,progress,hangul}.ts`; Korean content: `src/data/scripts/koreanHangul.ts` (47 symbols — 14 consonants, 10 vowels, 7 batchim, 5 doubles, 11 compound vowels; Revised Romanization, IPA, en/de/fr notes/meanings, homophone tags, Unicode block composition). UI (built by Opus subagent to Fable's contract): `src/pages/ScriptLab.tsx` + `src/components/scriptlab/{ScriptAudioButton,SymbolGrid,SymbolDetailPanel,BuildMode,QuizMode}.tsx` — Learn/Build/Quiz tabs, bottom-sheet detail panel, theme-token-only styling (works in both skins). Wiring: routes `/alphabet` + `/alphabet/:scriptId` in both skin groups of `App.tsx`, lazy chunk via `routeImports.ts`, registry-gated "Alphabet" tile in `StudyModeSelector.tsx`, `scriptlab.*`/`study.mode.script` keys in en/de/fr. Tests (Sonnet subagent): `scripts/test-script-lab-data.ts` (`npm run test:script-lab`) — 1392 checks incl. composition cross-checks of every example syllable. Docs: `docs/Product/FABLE_SCRIPT_LAB_ARCHITECTURE.md`, `FABLE_HANGUL_V1_PRODUCT_SPEC.md`, `FABLE_SCRIPT_AUDIO_PROVIDER_PLAN.md`. Skill for future scripts: `.claude/skills/add-script-lab-language/SKILL.md`.
- **Verified:** `npm run typecheck` ✅, eslint on all new/changed files ✅ (0 problems), `npm run check:i18n` ✅ (de 1411/1411; fr gaps are pre-existing speak.* warn-only), `npm run test:script-lab` ✅ (1392 passed), `npm run build` ✅ (ScriptLab is its own lazy chunk), `git diff --check` ✅. NOT verified: manual mobile smoke in an authed browser (no credentials in this session) — checklist is in the Hangul spec doc.
- **Audio:** V1 = declared spoken-text specs ({itemId, text}: letter names 기역, never bare jamo) → static asset manifest (empty) → browser speechSynthesis ko-KR. No paid provider calls anywhere. Upgrade path (recommended: reuse static-TTS pipeline's six QA'd ko voice profiles) in the audio plan doc.
- **Decided:** See [[DECISIONS]] 2026-07-06 Script Lab entry (route choice, rejected alternatives, abjad blockers).
- **Open:** owner mobile/visual QA; audio asset batch (needs approval); dashboard `dashboard-library-tile` entry point; Cyrillic/Kana data packs as first reuse proof; Arabic blocked on RTL + letterform type extensions.
## 2026-07-06 — Speak Phase 1 palette re-skin landed (Fable, same session as UX audit)
- **Changed:** Re-skinned the entire Speak selection flow from hardcoded indigo/slate/gray onto cosmos tokens — zero behavior change. `index.css` speak-* primitives (page shell, aura, ripple, glass card, start button, mode emblems) now use `--accent`/`--accent-2`/`--surface-*`/`--border-*` via color-mix; `ProviderToggle` now uses the `theme-panel`/`theme-chip-active` pill pattern; `GrokPicker`, `GeminiModeVoicePicker`, `CharacterGrid`, `SpeakHistoryPanel`, `ExtractWordsModal`, `VoiceSampleButton` fully tokenized (history transcript bubbles now reuse `speak-message-user/assistant`; provider badges use accent/accent-2 instead of violet/cyan).
- **Owner decision captured:** provider consolidation (tutor catalog, hiding the Live/Characters/Voices toggle) is ON HOLD — Grok realtime is materially more expensive than Voxtral/Gemini, so the provider distinction is cost-relevant and the owner will decide the consolidation shape himself. Phase 2+ of the Speak plan needs his sign-off; Phase 1 (visual only) was approved and is done.
- **Verified:** typecheck ✅, eslint on all 7 changed speak files ✅, `git diff --check` ✅. i18n untouched (class-only edits). Visual QA in a browser still pending — recommend a quick pass over the 3 Grok steps, Gemini 3 stages, character grid, history panel, extract modal in both skins.
- **Files:** `src/index.css`, `src/components/speak/{ProviderToggle,GrokPicker,GeminiModeVoicePicker,CharacterGrid,SpeakHistoryPanel,ExtractWordsModal,VoiceSampleButton}.tsx`.

## 2026-07-06 — UX coherence audit for TestFlight (Fable pass, parallel session)
- **Changed:** Full UX-coherence audit of the learner frontend (Codex inventory + Sonnet study audit + direct Speak/prompt review). Wrote 4 product docs: `docs/Product/FABLE_UX_COHERENCE_AUDIT.md`, `FABLE_SPEAK_PRODUCTIONIZATION_PLAN.md`, `FABLE_STUDY_AND_CARD_RENDERING_COHERENCE_PLAN.md`, `FABLE_TESTFLIGHT_FEATURE_STRATEGY.md`. Landed 7 small verified UI fixes: StudyCanvas raw "Loading…"→i18n; StudyAudio cyan `--accent` fallback removed; OrbDock rainbow placeholders→glass tokens; `#fb923c`→`var(--pg-accent-gold)` in StudyPG/StudyImagePG; alt-reveal gating + `object-contain` parity in Study/StudyPG; new `src/lib/speakCuration.ts` role flag; theatrical Gemini accents now admin-only (GeminiModeVoicePicker).
- **Files:** `frontend/src/pages/Study{Canvas,Audio,,PG,ImagePG}.tsx`, `frontend/src/components/OrbDock.tsx`, `frontend/src/components/speak/GeminiModeVoicePicker.tsx`, `frontend/src/lib/speakCuration.ts` (new), `docs/Product/FABLE_*.md` (4 new).
- **Verified:** `npm run typecheck` ✅, eslint on changed files ✅ (0 new), `npm run check:i18n` ✅ (only pre-existing warn-only fr gaps).
- **Key findings:** Speak selection flow is the biggest brand seam (hardcoded indigo/slate palette + 3-provider wizard exposing 5+28+26 voices/10 modes/27 accents); Study is one component written as four drifting files; `GeminiAccentPicker.tsx` and `pages/Settings.tsx` are dead code; landing experiment routes `/a` `/b` `/landing*` are publicly routable; no learner feature-flag registry existed before `speakCuration.ts`.
- **Open:** Speak Phase 1 re-skin + Phase 2 curation (see plan doc phasing); VideoPlayer.tsx i18n left to the video-deprecation pass; glassy-only-for-beta skin decision needs owner sign-off.

## 2026-07-06 — Second Brain installed; launch-readiness cleanup pass begun
- **Changed:** Installed `memory/` (INDEX/STATE/DECISIONS/ARCHITECTURE/LOG + raw/notes/archive) from a fresh evidence survey; wired the protocol block into `AGENTS.md`/`CLAUDE.md` at repo root.
- **Files:** `memory/*`, `AGENTS.md`, `CLAUDE.md`.
- **Context:** Session mission is a token-efficient cleanup + launch-readiness audit: Codex generates a repository intelligence packet (`docs/Refactors/CODEX_REPO_INTEL_PACKET.md`); Fable reviews, writes `FABLE_CODE_CLEANUP_AUDIT_REPORT.md`, `FABLE_VIDEO_DEPRECATION_BOUNDARY.md`, `FABLE_SAFE_CLEANUP_PATCH_PLAN.md`, then lands small evidence-backed patches.
- **Decided:** See [[DECISIONS]] 2026-07-06 entries (video deprecation boundary; delegated evidence-first cleanup).
- **Open:** Phase 1C–1H runtime verification; exact list of video UI surfaces safe to hide (pending packet).
