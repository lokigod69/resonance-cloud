# Session Log
Newest first. Append-only — entries are never rewritten.
When this file exceeds ~300 lines, move the oldest half to `archive/log-2026.md`.

## 2026-07-31 — FIRST SANDBOX PURCHASE COMPLETED; billing=success toast built (uncommitted); Codex's untracked src/vendor/ breaks local `tsc -b`
- **Robert ran the 4242 purchase as laqy69** (after the wrong-account fix): Stripe checkout rendered Lingwave Standard at ₱508.75/mo (PHP auto-localized, USD toggle offered), subscribe succeeded, and the app header showed **298 credits** afterwards — consistent with the +100 grant landing (was ~198). Server-side verification query (subscription row / balances / credit_ledger) handed to Robert, result pending.
- **His UX feedback from the purchase, triaged:** (1) *no success confirmation* — CONFIRMED in code: Stripe returns to `/dashboard?billing=success` and NOTHING reads the param → built `BillingReturnNotice` in App.tsx (global watcher, toasts success/cancelled once, strips the param; 2 new keys ×3 locales → 1636/1636). UNCOMMITTED, on his call. (2) Stripe line shows the full grant description ("100 creation credits, 60 Speak minutes…") — that's the Stripe **price description** we set server-side; cosmetic, optional trim later. (3) Onboarding should ask level (A1 vs B1 start) — product backlog. (4) His old laqy69 account: base=German saved but target stuck English with junk legacy words + 22 decks — he said leave as is for now; possible later: wipe/transfer admin-account content.
- **LANDMINE (concurrent Codex agent):** untracked `frontend/src/vendor/analyticsos/posthog-sink.ts` (+ `orchestrator/analyticsos/`, written 11:30) fails `erasableSyntaxOnly` → local `npm run typecheck`/`build` now FAIL on a file that is NOT ours and NOT committed. Verified my diff clean via temp tsconfig extending tsconfig.app.json with `exclude: ["src/vendor"]` (created + deleted same command). Vercel builds are unaffected (file is untracked). Do not delete/edit their tree.
- Gates on the toast change: typecheck-minus-vendor ✓ · i18n 1636/1636 ✓ · eslint clean ✓; full build gate deferred to Vercel (blocked locally by the vendor file only).
- **BILLING PROVEN END-TO-END (Robert's SQL result, 2026-07-31):** `user_subscriptions` = standard / month / **active**, period end 2026-08-31 09:14:47Z; `credit_ledger` = `subscription_grant` **amount=100** at 09:14:52Z (note: event_type reads `subscription_grant`, not the older shell's `stripe_subscription`); `profiles` = credits=198 + plan_credits=100 → exactly the 298 the header shows. Webhook delivered, signature-verified, granted once. The Stripe thread's only remaining items are cosmetic/decision-level (trim price description; flip `STRIPE_BILLING_ENABLED` when going live).

## 2026-07-29 (close) — BILLING GATE UNIFIED, COMMITTED + PUSHED `e7f606e3`; the /plans "no buttons" scare was a wrong account; query 3 answered
- **Webhook secret is in Vercel and live:** Robert replaced the old `STRIPE_WEBHOOK_SECRET` value and manually redeployed (Ready · Production · Current, source main @ `e722935`, "triggered without cache"). Probe confirmed: unsigned/garbage-signature POST to `https://lingwave.ai/api/webhooks` → 400 "Invalid Stripe signature" (secret loaded; a missing secret would 500).
- **Query 3 ANSWERED (SQL editor):** `static_tts_playback` has **1,850 rows each for de/pl/ko/fr/id/pt/it/es/ceb** — full Tier-2 coverage for all nine — and en = 30 (the old scoped animals test). Paid Library audio can be switched on for those nine; en needs its own batch first.
- **OPEN MYSTERY — /plans shows no Subscribe buttons for Robert:** page renders (949 credits, Free badge) but with the `plans.comingSoon` line, meaning the API returned `billing_available !== true`. Yet: deployed commit == local HEAD `e7229356` (contains the gating), env flags deliberately unset, and Robert's laqy69@gmail.com row is **admin in BOTH `profiles.role` and `admin_roles`** (verified by SQL). Only consistent explanation found: the browser session belongs to a DIFFERENT auth user. Fingerprint query handed to Robert (find the row with credits 900–1000 alongside all admin rows) — awaiting result. Also learned: `phase1f_protect_admin_roles` blocks manual admin-role SQL writes ("must use audited admin RPCs") — irrelevant here since the role already exists, but relevant to any future admin grant.
- **MYSTERY SOLVED (profile screenshot):** the browser was signed in as **cryptobonobo@proton.me**, not laqy69@gmail.com. cryptobonobo is a normal learner, so `billing_available:false` and the hidden buttons were CORRECT behaviour, not a bug. Unblock = log in as laqy69 (admin in both tables).
- **Real defect found and FIXED while investigating (COMMITTED + PUSHED `e7f606e3` on Robert's call — 6 files, +118/−92):** three different answers existed to "may this user reach Stripe" — entitlements gated on `profiles.role`, create-checkout-session on `admin_roles` + `app_metadata.{is_test_user,stripe_tester}`, frontend `billingFlags` on a third mix. A user flagged as a stripe tester via app_metadata could therefore NEVER see the button the checkout endpoint would have honoured. Extracted the gate to **`api/_shared/billingAccess.ts`** (env switches + app_metadata + admin_roles probe + the admin client factory); create-checkout-session imports and re-exports it (test import path preserved); `/api/entitlements` now sets `billing_available` from that same predicate instead of its own `profiles.role` check — the button can no longer promise what checkout refuses. Note the deliberate semantic change: a `profiles.role='admin'` user with no `admin_roles` row loses the button, but their checkout would have 403'd anyway, so the dead button is gone rather than the access.
- **Gates green on the fix:** `npx tsc -p tsconfig.api.json` ✓ · typecheck ✓ · stripe-billing-shell test PASS ✓ · eslint on all 4 changed files ✓ · i18n 1634/1634 ✓ · prod build ✓ · WordTide.tsx hash-verified `d4bcb0929af228c78d7c4f3f56aec8557cbbf095` and left unstaged throughout.
- **i18n tweak rode along in the same commit:** `plans.feature.songs` "AI song generation"/"KI-Song-Generierung"/"Génération de chansons IA" → "Song generation"/"Song-Generierung"/"Génération de chansons" (Robert: the AI prefix states the obvious).
- **Decision recorded** (DECISIONS 2026-07-29): one predicate for Stripe access; admin stays on laqy69; admin grants are audited-RPC-only and the SQL editor cannot mint them — do not work around the trigger.
- **Left for Robert:** the 4242 sandbox purchase signed in as laqy69, then Fable verifies webhook delivery + the credit grant server-side; and a product call on whether to switch on paid Library audio for the nine covered languages.

## 2026-07-28 (session close) — GENERATE SIMPLIFICATION BATCH COMPLETE: premium wizard gated, /plans deep-links, category picker redesigned, 9 brand bubble images generated
- **Delegated per protocol (Fable architect/reviewer):** entitlement gating + deep-links via Codex (job task-ms59kjm8-ynvcnh, entry below, diff fully reviewed by Fable — one accepted tightening: the words-step Customize secondary is now `card_standard`-only, safe because `VIDEO_LANE_ENABLED=false` makes the video lane unreachable); CategoryPicker redesign via Opus 5 (diff reviewed): each category group is now its own compact `rounded-2xl border-border/60 bg-card/50` box with a small left-aligned non-h3 header (`<p role="heading">` deliberately dodges the glassy `.gen-section h3 { 1.8rem }` override that made GRUNDLAGEN etc. huge), tightened 40px chips with subtle hover lift.
- **Premium option bubble art, Fable's hand:** 9 gpt-image-2 images via kie.ai (script in session scratchpad; taskIds logged), cosmos palette (near-black plum, vermillion/gold glow) — meaning: realistic bulb / exaggerated lightning / anchor-on-balloons / donkey-on-bridge (literal Eselsbrücke); presentation: framed lighthouse / 3-panel seed→flower / night-day split tree / letter-of-branches / brain infographic poster. 512px webp (19–48 KB) in `public/premium-option-samples/{enum}.webp`; wired via `MEANING_STRATEGY_SAMPLE_PATHS`/`PRESENTATION_FORM_SAMPLE_PATHS` in premiumVisualAssets.ts → PremiumVisualSelector imageSrc (both skins share the orb CSS); contract test extended (64/64). Gotcha for reruns: kie.ai result CDN 403s Python-urllib's default User-Agent — send a browser UA on download (first 9-image run was paid but lost; ~$0.3 sunk, taskIds now printed for redownload).
- **Fable fixes on review:** 3 `speak.liveAllowanceExhausted` locales comma-splice → em-dash (matches sibling keys).
- **Gates ALL GREEN (run by Fable):** typecheck · api tsc · i18n 1634/1634 · eslint on every changed file · premium-asset test 64/64 · prod build · WordTide.tsx hash `d4bcb092` untouched throughout.
- Codex's same-session sub-entry follows (its self-report; superseded by this close for verification claims).

## 2026-07-28 (frontend entitlement UX follow-through) — Generate premium UI gated; plan-limit errors deep-link to Plans
- **Generate:** added a page-load-sticky `usePlan` cache around authenticated `GET /api/entitlements`, warmed from both Generate skins. Premium-card quick/custom controls now render only for Premium/admin UI; other users get the single primary Generate action, while standard/text behavior stays unchanged.
- **Plan-limit recovery:** toasts now support optional dismissing actions; both song-generation premium errors offer a translated `/plans` action and close their modal before navigation. Speak and Grok Live classify the four entitlement-limit codes with `SpeakPlanLimitError`, expose a non-stale `planLimited` flag, translate the two Live-specific codes, and show Plans links at all four requested error surfaces.
- **Verification:** `npm run typecheck` PASS; `npm run check:i18n` PASS (en/de/fr 1634/1634); exact targeted ESLint PASS; `git diff --check` PASS. The npm/npx PowerShell wrappers emitted a sandbox-only denied `Test-Path` diagnostic after each successful command, but every command exited 0. No commit. Pre-existing dirty `WordTide.tsx`, CategoryPicker/premium visual assets, and their asset contract script were not edited.

## 2026-07-28 (close) — Tier batch COMMITTED `7fbe30e8` + Speak chip `aa28945f`; STRIPE SANDBOX FULLY CONFIGURED by Fable's hand
- **Robert applied migration 20260728090000, gave the commit call, and handed over Stripe.** Tier batch pushed as `7fbe30e8` (35 files, +3059/−174, WordTide excluded); Speak polish as `aa28945f` (remaining-minutes chip in the async tutor header fed by the per-turn `speak_allowance`, plus translated 403 messages for `speak_trial_exhausted`/`speak_allowance_exhausted`; i18n 1631/1631).
- **Stripe (Deep Blue Dodo LLC sandbox) set up via API using the key in `frontend/.env.local`:** products `prod_Uy50MebCGggDx7` (Standard) + `prod_Uy50XFU8OLdLBp` (Premium); prices `price_1Ty8qdBBdwTpOD9gMGcv2OKS` (std monthly $7.99), `price_1Ty8qeBBdwTpOD9gKRiW10yv` (std weekly $2.99), `price_1Ty8qeBBdwTpOD9ggraw2G1U` (prem monthly $14.99), `price_1Ty8qfBBdwTpOD9gQXuUjea3` (prem weekly $4.99), lookup_keys standard_monthly/…. **Found the Lingwave webhook pointing at the DEAD resonanz.pro domain with no path** — created `we_1Ty8rIBBdwTpOD9gzwmvIGrN` → `https://lingwave.ai/api/webhooks` with the 5 handled events, disabled the dead `we_1TaMrk…` (the canta.lol endpoint is Robert's other app — untouched). New webhook secret + 4 price ids written to `frontend/.env.local` (gitignored). **Robert still must copy 5 vars to Vercel: the 4 STRIPE_PRICE_* ids and the NEW STRIPE_WEBHOOK_SECRET** (the old secret verified the dead endpoint) — until then webhooks 400 and checkout 500s on missing price env.
- **Generate simplification DEFERRED deliberately** (owner asked for it "if we wanted"): gating PremiumCardCustomizationStep behind plan requires wizard-flow surgery in both skins + a usePlan hook — next session's focused batch, spec: non-premium skips the layer2/mnemonic/infographic customization (defaults), premium/admin keep the full flow; both skins; a plans deep-link from the premium toasts belongs in the same batch.
- Query 3 re-explained to Robert (static_tts_playback count by language — unlocks paid Library audio); still open.

## 2026-07-28 (later) — Codex xhigh review of the tier batch ARBITRATED: 2 blockers + 9 highs → 13 accepted/adapted, 4 rejected; all fixes applied, gates re-green
- **Verdict was "not safe to commit" (2 BLOCKER / 9 HIGH / 6 MED / 3 LOW) and the blockers were real.** Fixed: (B1) tier refunds now claw back the plan grant first via new `record_stripe_plan_refund` RPC (the legacy RPC was subtracting permanent credits while refunded plan_credits stayed spendable); (B2) checkout returns **409 already_subscribed** for any active/trialing subscriber — a second Checkout would double-bill; the probe fails CLOSED; plan changes are a future portal flow.
- **Highs fixed:** stale-period guard in the plan grant (a delayed/prorated invoice for an older period can no longer reset a newer balance — grant RPC now also locks profile→subscription in that order); `record_stripe_subscription_checkout` made monotonic (a late checkout_completed can't overwrite active); music-scope `request_word_retry` premium-gated (it requeued paid Suno past the songs gate); **Speak now debits atomically post-STT before any LLM/TTS spend** (ceiling = allowance + current turn, so a turn may cross the boundary once but concurrent/oversized turns can't run away) and **audio-less turns (greeting/roleplay text) debit a flat 15s** instead of bypassing metering; Grok config is checked before the Live debit and **every failed mint refunds its 10 minutes** (consume_feature_usage now accepts negative compensation, floored at 0); **useGrokRealtime auto-ends the session at 10 minutes** so the experience matches the debit (Codex was right that the 600s TTL expires the secret, not an established socket — residual risk: a modified client holding the socket; durations remain auditable via speak_conversations).
- **Mediums fixed:** webhook scans ALL invoice lines for a tier price (line[0] can be an old-price proration line); status RPC locks profile-first (deadlock order now uniform); premium weekly Live 15→**20** (10-min blocks stranded 5 min); `/api/entitlements` returns `billing_available` + free-trial constants and PlansPage renders CTAs only when checkout would succeed (no dead 403 button during staged rollout; +2 i18n keys ×3 → 1627/1627). **Lows fixed:** `plan_credits >= 0` CHECK, free grants no longer duplicated client-side, the trailing-whitespace line.
- **Rejected, with rationale:** (H3 legacy grandfathering) there are no production legacy subscribers — billing has been sandbox/tester-only since inception; the legacy path exists solely so old TEST subscriptions don't crash the webhook, delete it post-beta. (H9 expiry check inside the debit transaction) bounded exposure of at most one grant during failed-payment windows, terminal webhooks already zero it — revisit at scale. (M1 video lane premium gate) pre-existing, credit-priced, UI-hidden surface — out of batch scope. (M2 fail-open usage reads) deliberate deploy-before-migration resilience; unmetered equals pre-tier behavior and the daily quota rails bound it — revisit once the migration is confirmed applied.
- **Gates re-run ALL GREEN:** typecheck · api tsc · i18n 1627/1627 · eslint 0 · stripe-shell · visual-scan 24/24 · capacitor contract · oauth 29/29 · first-light 27/27 · prod build · `git diff --check` clean. Review job task-ms4hw1dg-gdqmpp (Codex session 019fa834-5ce8-7491-8cc7-1b386a0ec6e5). **Awaiting Robert's commit call.**

## 2026-07-28 — TIER SYSTEM BUILT: plans, expiring plan credits, metering, Stripe 4-price checkout (uncommitted; Codex xhigh review in flight)
- **Prices locked by Robert** (DECISIONS 2026-07-28): Standard **$7.99/mo · $2.99/wk** (raised from the proposal's $5.99/$1.99 — Stripe's fixed fee guts small weekly charges), Premium **$14.99/mo · $4.99/wk**; implementation GO given in the same message. Music cost anchor corrected to $0.054 (he always buys with top-up bonus).
- **Migration `20260728090000_subscription_tiers_and_metering.sql`** (owner applies BEFORE the next deploy, though the API fails open on reads if late): `profiles.plan_credits` — the expiring subscription grant, RESET (never added) each renewal per the no-rollover decision, spent before `credits` via `debit_user_credits` (single UPDATE; right-hand side reads OLD plan_credits, so the spend-order math is atomic); `user_subscriptions` gains plan/plan_interval/current_period_start; `usage_counters` + `consume_feature_usage` RPC (atomic increment with optional ceiling — ceiling = authorization for Lens/Live, unconditional = post-hoc Speak recording); `live_sessions` log; `record_stripe_plan_grant` (idempotent by invoice, reset semantics, upserts the plan onto the subscription row); `record_stripe_subscription_status` now zeroes plan_credits on canceled/unpaid/incomplete_expired; all four debit RPCs rewritten from their canonical bodies with surgical patches (total-balance checks, plan-first debit); **both music RPCs premium-gated** (`has_active_plan`, admins exempt); protect-trigger recreated with a plan_credits INSERT guard.
- **API layer:** `_shared/planCatalog.ts` is the single source of truth (grants, prices, env mapping `STRIPE_PRICE_{STANDARD,PREMIUM}_{MONTHLY,WEEKLY}`, prod switch `STRIPE_BILLING_ENABLED`); `_shared/entitlements.ts` (resolve plan from user_subscriptions + admin bypass; periodKey = `sub:<period_start>`, free trials = `lifetime`; usage reads FAIL OPEN, conditional debits fail closed); checkout takes `{plan, interval}` and stamps plan metadata; webhook resolves the plan from the price id (metadata fallback, legacy single-price path preserved for in-flight test subs); `voice-chat.ts` meters STT seconds (free = 60s lifetime trial; corrections mode never gated; response carries `speak_allowance`); `grok-token.ts` = premium gate + atomic 10-min debit per token mint (the 600s TTL is the ceiling — unspoofable) + `live_sessions` row + cost event at the VERIFIED $0.05/min; `visual-scan.ts` debits scans via injectable dep (contract tests unaffected); new `GET /api/entitlements` for the UI.
- **Frontend:** `/plans` page in both skin route blocks (server-driven prices, weekly/monthly toggle, current-plan badge; native = informational only, no checkout — App Review 3.1.1, asserted in the capacitor contract test); RedeemCodeDialog's sandbox-subscribe button REPLACED by a plans link (it posted `{}`, which the new API rejects); `totalCredits()` sweep across all 9 balance displays/prechecks (sonnet executor, verified by typecheck + my read of its diff summary); `premium_required`/`insufficient_credits` mapped to i18n toasts in both song modals; 31 new keys ×3 locales.
- **Gates ALL GREEN:** typecheck · api tsc · i18n 1625/1625 · eslint 0 errors (1 stale disable --fixed in LevelDetailPage) · stripe-shell test rewritten for the new surface + passes · visual-scan 24/24 · capacitor contract (checkout assertions repointed) · oauth-onboarding 29/29 · first-light 27/27 · prod build · WordTide `d4bcb092` untouched.
- **Owner setup before billing can go live:** create the 4 recurring prices in Stripe, set the 4 price env vars in Vercel, flip `STRIPE_BILLING_ENABLED=true` when ready (until then checkout stays tester/admin-only), apply the migration. **Accepted simplifications (disclosed):** music-failure refunds land on the non-expiring balance; Lens debits at request time; Live bills a full 10-min block per mint regardless of real duration; existing `speak_conversations.ended_at` remains the only real-duration record.
- **Codex xhigh adversarial review launched** on the full diff (money integrity, RPC-copy drift, bypass paths, App Review) — arbitrate its findings before the commit call.

## 2026-07-27 (later) — Beta Coherence Batch COMMITTED `eedddf8f`; pricing research verified the cost model; concrete tiers proposed
- **Committed + pushed on Robert's call** (he applied both migrations first — order respected): one scoped commit `eedddf8f` (49 files, +942/−260) carrying the Beta Coherence Batch AND the settled 2026-07-25/26 iOS identity batch — they share package.json and the capacitor contract test, and all gates ran green on the combined tree, so splitting would have produced never-verified intermediate states. WordTide.tsx hash-verified `d4bcb092` pre-stage and left unstaged. Pre-push typecheck clean. Standard push, Vercel deploying.
- **Robert's pricing directives recorded** (DECISIONS 2026-07-27 pricing entry): ~3x measured cost rounded friendly; two paid tiers + a weekly option; subscription grants don't roll over; simplify generation UX for standard users; Lens generous; growth over margin.
- **Cost research (2 agents: code inventory via sonnet/Explore, web pricing via general-purpose; owner beliefs reconciled):** async Speak is Groq Whisper ($0.111/hr) + Groq Llama-3.3-70B + Voxtral TTS ($16/1M chars) or Gemini TTS — **no xAI, no ElevenLabs in the live loop → ≈$0.01–0.02/min all-in**. Grok = Live only: `grok-voice-think-fast-1.0`, **$0.05/min flat VERIFIED** (x.ai announcement — the code's UNVERIFIED constant was exactly right) but **sessions still write no cost row**. Standard card = z-image-turbo $0.004 (+~$0.0005 DeepSeek prompt call); premium card = gpt-image-2 **1K = 6 kie credits = $0.03 verified** (cost_logger's $0.05 is the 2K rate while card_engine passes 1K — overstatement, worth a one-line fix); music = 12 kie credits = $0.06/submit for 2 tracks; Lens = Gemini 2.5 Flash-Lite ≈ $0.0003/scan. **The product coin is already cost-true: 1 credit ≈ $0.005–0.006 provider spend** (standard 1cr/$0.0045 · premium 5cr/$0.03 · music 10cr/$0.06).
- **Proposal put to Robert (his call pending):** Free = 10 signup credits + 1 trial Speak min + 3 Lens scans; Standard ≈ **$5.99/mo or $1.99/wk** = 100 credits/mo + 60 async min + 100 Lens (worst-case COGS ≈ $1.85); Premium ≈ **$14.99/mo or $4.99/wk** = 300 credits (songs enabled, max 30) + 240 async min + 60 Live min (only after logging ships) + 300 Lens (worst-case COGS ≈ $9.7, typical $3–4). Weekly grants = ¼ monthly, no rollover. Engineering prerequisites regardless of numbers: Speak minute metering (today it counts requests), Live session/duration/cost logging, credit ledger + single entitlement policy per the Codex migration plan, Stripe products (web-only).

## 2026-07-27 — Beta Coherence Batch: onboarding resurrected, one canonical language, 8-language trim, classic retired (uncommitted; Codex review in flight)
- **Robert answered everything in one voice message + ran the prod queries.** Query 1: newest profile has `base_language='English'`, `credits=0` — the DB default is live, the onboarding gate never fires, **the audit's headline blocker is confirmed**. Query 2: `enforcement_enabled = TRUE` — rate limits are ON in prod; the audit's "every limit is a no-op" is corrected to "the default is dangerous, but Robert had flipped it". Query 3 failed — my report guessed the column name; it is `target_language_code` (corrected SQL now in NEXT_STEP; still unanswered, gates the one-line Library-audio unlock).
- **Decisions locked** (full record in DECISIONS 2026-07-27): 8 beta languages, one canonical target language, classic skin = debt → retire, Library ships, SRS is the loop, signup credits (10), typed recall gated off non-Latin, subscriptions = next session. Robert delegated implementation judgment ("I trust you fully").
- **Implemented by Fable's hand** (Opus 5 subagents mapped read-only, per the Codex-investigates/Fable-fixes rule): (1) **lib/targetLanguage.ts** — the canonical bridge: `syncTargetLanguageStores` fans every `setActiveLanguage` out to the guided key, the Library key (dynamic import so data/categories stays out of the root chunk), and `lingwave_added_languages_<uid>` (so dashboard pin effects can't clobber a deckless seed); owns the only Bisaya↔Cebuano map (useTodayMission's local copy deleted). (2) **Onboarding.tsx rebuilt** — step 1 native (BASE_LANGUAGES trimmed to en/de/fr — the other five silently rendered an English UI), step 2 target (8 tiles); writes `profiles.target_language` best-effort + sticky local flag + `setActiveLanguage`; invite step removed (redemption lives in the global dialog, 4 entry points verified); deep-link `state.from` honored. (3) **App.tsx gate** now keys on target_language + local flag, never base_language; `/a` `/b` `/landing/*` unshipped. (4) **Beta allow-list** — `BETA_TARGET_LANGUAGES` (en/de/es/fr/it/pt/Bisaya/id) filters wizard tiles, +Add-language, Library chooser+select, CategoryPicker ×2, SPEAK_ORDER, Today's language options; **resolvers deliberately untrimmed** (legacy ko/ru/ja/pl decks keep working). (5) **Classic skin retired** via `CLASSIC_SKIN_RETIRED` (SkinProvider pins glassy, never overwrites stored choices; ProfileModal selector hidden; deletion = later batch). (6) **Typed recall hidden for non-Latin** (`isLatinScriptLanguage`; ko/ru/ja/ar/hi). (7) ko/ru/ja `today.language.*` raw-key defect fixed ×3 locales. (8) **Decks.tsx no longer nulls the active language at zero decks** (a wipe is not a choice). (9) ProfileModal keeps a legacy base_language visible in the select. (10) experiencePreset.ts deleted (zero importers — one Opus agent claimed FirstLightHome used it, disproven by grep; the audit's "dead" verdict stood).
- **Migrations WRITTEN, owner applies:** `20260727090000` — `profiles.target_language` (nullable, NO default — a default would recreate the dead-gate bug) + **`protect_profile_privileged_fields` recreated with target_language allowlisted** (the Opus map caught that without this the column is silently un-writable) + `handle_new_user` credits 0→10. `20260727091000` — drops the four ownerless `Public read` storage.objects policies (videos/audio/tts-pronunciations/voice-samples): kills anon enumeration, playback unaffected (public-URL downloads don't consult RLS; verified no client `storage.from()` on those buckets). **Frontend tolerates unapplied migrations**: profile fetch switched to `select('*')` (explicit column list would 400 every login against the old schema), onboarding write fails soft to the local flag.
- **Gates all green:** typecheck · eslint 0 (the 4 unused-disable warnings my Today.tsx edit exposed were --fix'd) · i18n **1597/1597** (+3 langName, +3 onboarding.target*, −5 dead invite keys) · capacitor contract PASS · oauth-onboarding 29/29 (asserts rewritten for the new gate incl. a "must NOT key on base_language" tripwire) · vocabulary-library OK · static-category-translations OK · category-picker asserts updated+pass · first-light 27/27 · fixtures 17/17 runnable (stub gained STATIC_CATEGORY_TARGET_LANGUAGES — the fan-out pulled staticLibraryLanguage into the Home graph) · wave pixel-identity PASS · prod build ✓ · WordTide `d4bcb092` untouched.
- **Known-stale contract tests, all verified failing identically on HEAD (not this batch):** test-dashboard-home-glass (`dashboardLibraryHref` gone from both dashboards), test-speak-polish-contract (reads deleted ProviderToggle.tsx), test-generate-category-picker-flow's LAST assert (`categories.generateFromCategories` left CategoryListPage in the nav restructure; my earlier asserts pass), test-lingwave-rebrand-sweep (deleted CtaFooterSection), test-hybrid-a/b (not in package.json). Same repoint-at-live-surfaces family as STATE Known problems.
- **Codex (gpt-5.6-sol): tier-design consultation DONE but its sandbox is read-only** — headline: Free = 10 credits/unlimited SRS+Today/1 trial Speak minute/3 Lens scans; Standard = 60 Speak min/mo, 30 Lens, 50 credits; Premium = 240/150/150 + songs + Live capped 10min×1/day; tiers become the sole policy, credits stay the creation wallet, quotas demote to abuse rails; Live stays OFF until server-side duration+cost logging exists. Full draft dumped from its session → `investigations/CODEX_TIER_DESIGN_2026_07_27.md`. Its pushback independently converged on the batch's own choices (server-persisted canonical language with multi-language kept; classic hidden-not-deleted; typed gated-not-replaced); its strongest NEW flags for the pre-invite checklist: **wrong-sense Library senses amplified by the SRS loop (7 of 8 beta languages)** and **no per-language audio-parity matrix**.
- **CODEX xHIGH ADVERSARIAL REVIEW COLLECTED + FULLY ARBITRATED (job task-ms40mgss-1vqt9g, 18m41s): 0 blockers, 3 high, 9 medium, 4 low → "not safe to commit" → all three highs FIXED, gates re-run green.** Gotcha en route: the companion job registry is cwd-scoped — `status` from the repo root says "No jobs recorded yet"; run it from `orchestrator/`. Accepted+fixed: **H1** Today could freeze a stale language on cold load and the beta filter bounced legacy ko/ru/ja/pl learners into English → resolution now checks ALL guided languages (trim applies to the switcher only) + a one-shot adopt-canonical effect when `languageReady` arrives late (skipped for `?path=` links and running sessions); **H2** `profiles.target_language` was only a gate bit → LanguageProvider now hydrates from it on a fresh device (stored key → profile → newest deck) and all storage writes are try/caught (also M6); **H3** Generate auto-seed skipped the picker for ANY active language → gated on `isBetaTargetLanguage`, so a Korean-active user can't mint a new Korean deck (existing-deck append untouched); **M8** `canonicalizeLanguageValue` learns the `cebuano→Bisaya` alias (GameShell `?lang=Cebuano` no longer stores a phantom language); **M9** legacy values stay visible in the Library/CategoryPicker selects (ProfileModal pattern); **M11 — genuine miss of mine: Spanish still had `isBase: true`** after the trim, fixed; **M12** `cap:sync:ios` now runs the Windows guard (the raw script bypassed it); **L15** deep-link restore keeps the URL hash; **L16** `git diff --check` clean. Accepted-as-intended, no change: **M5** every existing account (incl. admin Robert) gets the two-step onboarding exactly once — deliberate, it seeds the canonical store; **M10** Speak dead-ends legacy ko/ru active languages — deliberate trim; **L13** admin classic chrome; **L14** typed-tile async flash (deep links deliberately keep working — UI gating, not route enforcement). Rejected with rationale: **M4** Library in-page selector stays a session-only override per standing DECISIONS 2026-07-16 (Codex lacked that context); Generate's wizard pick converges via the deck-creation pin; **M7** logged-out deep-link restore is pre-existing Login behavior (always → /dashboard), backlog polish, not this batch's regression. Codex also confirmed: migrations faithful (trigger recreation matches 20260622090000 + only intended changes; policy drops exact; only affected `.list()` caller is service-role delete-account, RLS-exempt), bundle layering safe at source level, i18n complete, no orphaned keys. **Post-fix gates ALL GREEN:** typecheck · eslint 0 · i18n 1597/1597 · capacitor contract · oauth-onboarding 29/29 · library contract · static translations · first-light 27/27 · fixtures 17/17 runnable · prod build · WordTide `d4bcb092` · diff --check clean. Batch is commit-ready on Robert's call.


## 2026-07-26 — Bundle id + signing team settled; then a 12-agent product audit found the beta's real blockers
- **Robert answered the Mac question: he owns a Mac mini (48 GB) and an active membership on `ZL69STWQHV`.** He also asked why a Mac is needed at all, having shipped Expo projects before — the answer is that Expo Go is a pre-built signed app that downloads JS at runtime, while Capacitor produces his own native binary, so Xcode/macOS must compile and sign it. No cloud runner needed. He supplied an App Store Connect API key (`AuthKey_X57YYWBFFF.p8`, key `X57YYWBFFF`, issuer `e7dc0119-…`), which sits at the non-git repo root and is only needed for automated CI uploads, not a manual archive.
- **The decisive new fact: App Store Connect holds only Canta.** No `pro.resonanz.app` record exists. That disproves INV-IOS-001's "app no longer available" → expired-TestFlight inference and the 2026-07-25 C1 recommendation built on it. Renaming was free today and impossible after the first upload.
- **BUNDLE ID → `ai.lingwave.app`, TEAM → `ZL69STWQHV`, both applied.** Reverse-DNS from lingwave.ai, chosen via AskUserQuestion. Touched `capacitor.config.ts`, `ios/App/App.xcodeproj/project.pbxproj` (Debug + Release, both `PRODUCT_BUNDLE_IDENTIFIER` and a new `DEVELOPMENT_TEAM`), `ios/App/App/capacitor.config.json`, the `Info.plist` CFBundleURLName, and both AASA copies → `ZL69STWQHV.ai.lingwave.app`. **This clears B3**, the last code-side blocker on producing an archive. `resonance://` deliberately left alone: load-bearing for OAuth, not permanent, invisible to users. `scripts/test-capacitor-ios-contract.ts` extended — bundle id in all four places, absence of the old id in the pbxproj, exactly two `DEVELOPMENT_TEAM` entries, and AASA/bundle agreement. Gates: contract PASS · typecheck clean · eslint 0 · i18n 1596/1596.
- **Found while running gates:** `scripts/test-lingwave-rebrand-sweep.ts` throws ENOENT on `src/components/landing/CtaFooterSection.tsx`, deleted by the 2026-07-04 landing rebuild. Confirmed absent in `HEAD` — pre-existing, unrelated, same class as the other stale contract tests. Not fixed (out of scope).
- **12-agent adversarial product audit** — 6 read-only domain surveys (onboarding, feature surface, language coverage, text input, paid providers, security), each followed by a verifier instructed to refute rather than agree. 65 findings: 44 CONFIRMED, 19 OVERSTATED (severity corrected down), 1 upgraded, 0 WRONG. Seven highest-consequence claims then re-verified by hand against the migrations and `App.tsx`; all seven held. Report: `investigations/BETA_READINESS_PRODUCT_SCOPE_2026_07_26.md`. **The verifiers' "what did the surveyor miss" lists were the most valuable output — three of the seven blockers came from there, not from the surveys.**
- **Headline finding: the app very likely never asks a new user anything.** `handle_new_user` inserts a profile without `base_language` (`20260428120000_…:158-167`); the column carries `default 'English'` (`20260322210000_…:8`); the sole route to the wizard is `!profile.base_language?.trim()` (`App.tsx:109`). If that default is live in prod, `Onboarding.tsx` is dead code. Four surfaces then guess the target language independently from four different storage keys, and Today's guess resolves to `english-a1-practical-1` — an English course whose base strings exist only in German. This is the mechanical root of Robert's "very complicated to understand how you would even use this".
- **Six more blockers.** Guided progress is localStorage-only (`lib/todayProgress.ts:290,312`, no table in 89 migrations) so Home never leaves first-run and the streak is permanently 0 · every rate limit is a no-op (`api_quota_settings.enforcement_enabled` default false; `consume_api_quota` allows everything in that mode) · new accounts start at **0 credits** (the "10" is `invite_codes.credits`, a different column) with a 403 purchase path · the Classic skin is one untranslated tap away in the profile modal · **four storage buckets — `videos`, `audio`, `tts-pronunciations`, `voice-samples` — are `public = true` with read policies carrying no owner predicate**, so anon can enumerate and download every user's media (the correct pattern already exists on `profile-avatars`) · ko/ru/ja lessons print raw `today.language.*` keys, which `check:i18n` structurally cannot catch because they are missing from all three locales.
- **Cost surface, for the pricing conversation:** the one unbounded spend path is Speak "Live" (browser talks straight to `wss://api.x.ai`, no session cap, zero cost rows, and it is the *default* provider). The app has two unrelated limiting mechanisms — credits and per-feature daily quotas — that don't talk to each other, and every quota is per-user so there is no global ceiling. Supabase storage/egress and Vercel GB-seconds produce no `pipeline_events` row at all, so the cost dashboard structurally cannot see the hosting bill.
- **Security posture verified genuinely good** and worth recording so it stops being re-litigated: no secrets in the client bundle, RLS on all 33 tables, one shared `requireSupabaseUser` on all seven paid endpoints, server-side `is_admin()`, a trigger blocking self-writes to `role`/`credits`, signature-verified Stripe webhooks. An agent went looking for an invite-code RLS hole and confirmed it was correctly hardened.
- **NOTHING IN THE AUDIT IS FIXED.** The report is a scope. Part 3 holds three production SQL queries that gate the next build decisions; Part 9 holds 16 owner decisions for the strategy conversation Robert wants to have with another AI.

## 2026-07-25 — TestFlight scoping: the "relative /api/*" blocker is retired; the real ones are an icon, a signing team, and a Windows/Mac split
- **The standing blocker is dead and should stop being carried forward.** All 14 `/api/*` fetch sites still go through `publicApiUrl()` (a repo-wide grep for `/api/` finds nothing else — the rest are comments); `.env.ios` supplies the absolute origin; `api/_shared/cors.ts` still allows `capacitor://localhost`; auth is PKCE + `@capacitor/preferences` + `appUrlOpen`; the contract test passes. Seven weeks of Lens/Speak/B1/Wave Rider/First Light did not regress it. Two bonus clears: `canUseBrowserSpeechRecognition()` has ZERO call sites (the WKWebView Web-Speech trap does not apply — guided speak is MediaRecorder → Whisper), and `src/api.ts` (Railway) is imported by one admin file only.
- **`npm run build:ios` still works and dist is 82.6 MiB** (stripped 526.3 MiB; was 77.7 MiB on 2026-07-08 — growth is three.js + First Light, not a regression). Full scope: `investigations/TESTFLIGHT_READINESS_SCOPE_2026_07_25.md`.
- **NEW LANDMINE — `cap sync ios` on Windows corrupts a tracked file.** It rewrites `ios/App/CapApp-SPM/Package.swift` with backslash paths (`"..\..\..\node_modules\@capacitor\app"`); `\.` and `\n` are Swift escape sequences, so the manifest will not compile on the Mac. Found by running it, reverted immediately. The contract test already *detects* it (`assertNotIncludes '..\\..\\..\\node_modules'`) — what was missing was prevention, now `scripts/guard-cap-sync-platform.ts` in the `build:ios` chain. **`build:ios` is a Mac-only command from here on**; the vite+strip half still runs on Windows for measurement.
- **NEW — the iOS app icon and splash are the stock Capacitor placeholder** (blue chevron on white). INV-IOS-001 inferred from file size that it was real branding; that inference was wrong — I looked at the pixels. Every tester would install a Capacitor-logo icon and get a white splash before a near-black app. Largest brand raster in the repo is `android-chrome-512x512.png`; Apple needs 1024 and no `sharp`/`@capacitor/assets` is installed.
- **`ZL69STWQHV` is already live in production** — `public/.well-known/apple-app-site-association` serves `ZL69STWQHV.pro.resonanz.app` (HTTP 200). Combined with INV-IOS-001's *"app no longer available"* symptom (the wording of an expired TestFlight build), this is probably not a first-ever upload. Recommendation on the standing bundle-id question: **keep `pro.resonanz.app`** — it is baked into the live AASA, the entitlements, the `resonance://` scheme name and probably an existing App Store Connect record; testers only ever see `CFBundleDisplayName`, which already says Lingwave.
- **Applied (uncommitted, decision-free):** the platform guard above; pruned the dead `applinks:resonanz.pro` pair from `App.entitlements` (that domain is DEPLOYMENT_NOT_FOUND and can never serve an AASA) and flipped the contract test's two assertions to `assertNotIncludes`; added `ITSAppUsesNonExemptEncryption=false` to `Info.plist` (HTTPS-only ⇒ standard exemption; kills the per-upload export-compliance prompt) + a contract assertion. Green: contract test, typecheck, eslint, plist XML integrity.
- **Left for Robert (in the scope doc):** internal vs external testers — external means Beta App Review, which makes the Stripe surface (`RedeemCodeDialog.tsx:82-101` opens Stripe Checkout in an external browser on native — guideline 3.1.1) and the empty `NSPrivacyCollectedDataTypes` matter; background audio (`UIBackgroundModes` absent, so music dies on lock); bundle id.
- **Not done on purpose:** the AASA `Content-Type` fix (served as `application/octet-stream`). The obvious `headers` block would break the deploy — `vercel.json` uses legacy `routes`, which Vercel forbids combining with `headers`. Needs a route-level `headers` + `continue: true`, verified on a preview. Universal links are not required for TestFlight.
- **Top device-test risk when a build exists:** `AppDelegate.swift:18` sets `AVAudioSession` `.playback` only; WebKit switches to `playAndRecord` when the mic opens, and the classic symptom is Speak output dropping to the earpiece. Cannot be settled off-device.
- **Robert chose EXTERNAL testers (invite), same session** — so Beta App Review applies and the two conditional items became real work, all done and green:
  - **The Stripe button would have shipped to EVERY tester, not just admins.** `isBillingTester()` reads `VITE_STRIPE_BILLING_SANDBOX_ENABLED`, which is `true` in this machine's `.env` — and `.env` loads in `ios` mode too, so the iOS bundle compiled the gate to `function x(){return "true"}` (verified in the built chunk). "Subscribe with Stripe Sandbox" → external Stripe Checkout, visible to the reviewer: a clean guideline-3.1.1 rejection. **Generalise this: the iOS bundle inherits the `.env` of whatever machine builds it, so a review-relevant gate must never live in configuration.** `isBillingTester()` now returns false on native before reading anything; contract test asserts the line. Invite-code redemption untouched (not a purchase).
  - **Icon + splash replaced** with the real brand mark, generated from `public/android-chrome-512x512.png` via local ffmpeg (no repo dependency): 1024×1024 RGB **no alpha** (Xcode rejects alpha), splash 2732×2732 on black so the mark's own black field blends. Contract test now asserts dimensions + no-alpha, so the Capacitor placeholder cannot return.
  - **`PrivacyInfo.xcprivacy` filled in** — email, name, user ID, other user content, audio, photos/videos, product interaction; all linked, none tracking. Grounded in the code: `speak_conversations`/`speak_messages` persist transcripts and `pipeline_events` logs usage per user id, while guided-transcribe/voice-chat/visual-scan hold audio and images in memory and never write them. Audio/photos declared anyway because they reach third-party providers. **Robert still owns the matching App Store Connect privacy answers + privacy policy.**
  - **`capacitor.config.ts` gained `ios.backgroundColor: '#0e0810'`** — kills the white webview flash between launch screen and first frame.
- **Gates after all of it:** contract test PASS, typecheck, eslint (changed files, 0), check:i18n 1596/1596, home-first-light 27/27, prod build. WordTide still `d4bcb092`.

## 2026-07-24 (late) — TTS back-fill applied to production (30 rows); the forward import path turned out to be already correct
- **Robert authorised the production write.** `scripts/backfill-curriculum-tts-audio.ts` (committed `c6cecfb2`) resolves audio-less curriculum word rows exactly the way the runtime does — `metadata.curriculum` concept id → category/level/target language → `static_tts_playback`, voices in `getStaticThematicVoiceProfileKeys` order — so a back-filled row plays the same file the app would have picked. Dry run by default; `--report` dumps the touched ids BEFORE any update (revert = `set tts_audio_url = null` over that list); the UPDATE carries its own `tts_audio_url is null` predicate so a row that gained audio between read and write can never be overwritten.
- **Applied: 30 rows** — fr animals L1, ceb animals L1 (Robert's Home buoys), id animals L4. Verified after: curriculum rows with audio 30 → 60; `iro/kabayo/baboy/manok/karnero` all carry urls; one file per language HEAD-checked 200 audio/mpeg before writing.
- **Deliberately left alone:** 80 en rows in categories the en static library never covered (it is animals-scoped, and only some levels), and 86 rows whose `metadata.curriculum` carries no concept id at all — those are the guided/enrichment content system (`topic`, `noun`, `per_source`), not the static thematic library.
- **Correction to the earlier claim that "the import RPC has never written it".** True but not the whole story, and the conclusion was wrong: `submit_curriculum_import` still does not write the column, but `importStaticCategoryLevel` resolves the audio BEFORE the RPC (`fetchStaticCategoryAudioForImport` → payload `tts_audio_url` + `metadata.static_tts_public_url`) and `attachStaticThematicAudioToImportedDeck` patches it after. Proof in the data: every audio-less row with a concept id is a legacy import (ceb 2026-06-29, id 2026-06-26, fr 2026-06-25, all with the pre-`source_*` metadata shape), while de (2026-06-30) and ko (2026-07-06/16) — the first imports after the wiring — landed WITH audio. **No forward fix was needed, so none was invented.** Re-run the script after any new static-TTS language batch.

## 2026-07-24 (late) — Robert's first real session on First Light: the practice card now HOLDS; real TTS instead of the robot voice (`5eeb1aed`, pushed)
- **Robert's findings, all addressed:** (1) the sheet spoke the word and immediately advanced — he wants to sit with a card, hear it several times, then press on; (2) a synthetic voice played where recordings exist; (3) tapping the picture / the word should replay; (4) the mobile language pill floated above the header row; (5) desktop buoy labels too small; (6) the hero CTA was "aggressive orange", off the wave palette.
- **The card holds.** Any resolution (correct / almost / revealed) settles in place: picture stays and becomes a replay control, the word sits there with a speaker on it, translation under it. The queue moves ONLY on Next (or Done when the set is finished — `hasNext` comes from the parent's live `nextInQueue` memo). Reveal now writes its attempt immediately, so the old Continue-then-grade step is gone; `onAdvance()` takes no args and the auto-advance effect + celebration timer are deleted.
- **Two real defects behind "it says the next word before I've seen it":** the late-TTS effect fired on the commit where `lemma` swaps while `result` still held the PREVIOUS card's value — it read the incoming word's answer aloud AND burned the once-per-card play, so the correct moment stayed silent. Fixed by making the resolution carry its card (`SheetAnswer = {key, value}`; `settled = answered.key === lemma.lemmaKey`), which also stops the next word flashing its own answer for a frame. Replaces the ref-based guard — `react-hooks/refs` forbids reading refs during render.
- **Root cause of the robot voice (verified against prod, service + anon key):** these ceb rows carry **NO `tts_audio_url`** (`iro`/`baboy`/`karnero`/`manok`/`kabayo` all NULL, no `metadata.curriculum.static_tts_public_url`) — the real files exist only in `static_tts_playback` (ceb/animals/level-1, `static_thematic_ceb_yumi_raw_v1`, `qa_status=ready`, anon-readable, HEAD 200 audio/mpeg). So EVERY Home word resolves through the async static lookup, and the first card of a visit routinely answers before its url lands → plays from an effect, outside the user gesture → `audio.play()` rejects → old code fell through to `speechSynthesis`. Fixes: `prefetchPronunciationAudio()` warms every visible buoy's file as soon as its url resolves (so the play happens inside the click), and `playPronunciation` gained `allowSpeechFallback` — a word we KNOW has a recording stays **silent** when the file will not start rather than teaching a robot pronunciation. The replay control is the honest recovery.
- **Open data debt (needs Robert's call — prod write):** back-filling `words.tts_audio_url` from `static_tts_playback` for curriculum-imported rows would remove the extra round-trip for every surface, not just Home. The import RPC still does not write it (see the 2026-07-13 playback gotcha).
- **Polish:** `.language-picker` carries the classic dashboard's dvh-scaled `margin-bottom`, which a centered flex row turns into a ~21px lift — reset locally in `FirstLightTopRow`, not in the shared component. Desktop buoy labels step up one band (`lg:` variants on WORD_CLASS/GLOSS_CLASS — the measuring twin shares the classes, so collision widths stay honest). New `.lw-swell-cta`: the sea's own ramp (plum → magenta → vermillion → held-back gold, matching LingwaveWaves' SWELL_LOW→CREST), 210% background drifting on a 14s cycle, `--m-cool`/`--m-mid` with accent-derived fallbacks so non-cosmos themes stay on-theme; animation off under reduced motion.
- **Fixture harness updated** for the new contract: `advanceButton()`/`advanceCard()` helpers, `missCurrentCard` = reveal → insert lands → press on; fixture 12 no longer clicks Continue (reveal writes) and asserts "no way to advance after failure"; fixture 16 advances explicitly. Stub `usePronunciation` gained `prefetchPronunciationAudio` + records `allowSpeechFallback`.
- **Gates:** typecheck · eslint 0 new · check:i18n **1596/1596** (+2 ×3: `home.fl.sheet.next`/`.done`) · unit 27/27 · fixtures **17/17 runnable PASS** (2 honest not-run) · build · wave pixel-identity **PASS**. `WordTide.tsx` = `d4bcb0929af228c78d7c4f3f56aec8557cbbf095`, never staged.
- **Deferred:** compact-desktop (<735px tall) card/waterline overlap; profile toggle for buoy pictures (Robert: word + translation is fine for now); sheet focus-trap; Codex xhigh verdict (job `bvm693p21`) still uncollected.
- **Next:** Robert's phone verdict, then TestFlight preparation.

## 2026-07-24 (night) — FIRST LIGHT BUILT: all six §14 steps implemented + verified; Opus 5 implementation review fully arbitrated; Codex + fixture-harness verdicts pending
- **Robert gave the implementation go; Fable built the whole spec by hand** (delegation used only for verification harnesses/reviews, per the trust rules). All §14 steps: (1) `waveField.ts` projection exports + additive `LingwaveWaves` dawn-ease/`clockRef`/stars-ref — **pixel-identity gate PASS** via a deterministic headless-Chrome harness (`scripts/wave-pixel-gate/run.mjs`, built by an Opus subagent: seeded LCG, virtual clock, manual rAF ladder; 60 frames × dawn 0/0.35/1 old-vs-new byte-identical, control + sensitivity self-checks, reduced-motion still seeds clockRef=30); (2) `lib/homeWordDetails.ts` (WordTide :150-242 copy + hook) + `BuoyPracticeSheet` (chained queue, insert-before-count, inline retry, cooldown card); (3) `useTodayMission` strict-default + `lessonId` (fallback now a named opt-in nobody passes) + `languages.speakConversationLanguageCode` (9th edited file, §4-mandated, disclosed) + `useHomeVisit` (visit record, monotonic dawn, single duePool, lapse, rollover-on-wake, speak probe) + `useHomeRecommendation` (total resolver, commit-once, 800ms recall-first) — **unit tests 27/27** (`node scripts/run-home-first-light-test.mjs`, esbuild runner because tsx lacks import.meta.env); (4) six `components/home/` files — buoys ride the real projection via one rAF + shared clock, collision count-reduction on measured labels, per-script gloss budgets, priority-drop strip, fixed-height hero card; (5) PolishGlassLayout right-slot portal + DashboardPG populated branch → `<FirstLightHome/>` (WordTide/presets/probes/dive-in/mastered pill removed; wordStates moved inside) + classic always-a-CTA fallback; (6) i18n **38 keys ×3 exactly** (1556→1594, natural de/fr).
- **§11 budgets measured** (perf `baseline-20260724T222906Z`): entry JS delta **−119 B** (gzip 106.6 KiB unchanged); home chunk 38.1 KiB/12.5 gz, `containsGuidedLessons=false`; guided (6.96 MB) off the critical path — /dashboard cold chunk wait 187.6 ms vs /today 1382.5 ms; time-to-hero cold 1876 ms auth, warm 89.6 ms.
- **Opus 5 adversarial implementation review: 4 HIGH / 10 MED / 15 LOW, all arbitrated, all blocking fixed by Fable's hand:** H1 never-lock-last made REACTIVE (a correct grade could strand the last buoy locked — release earliest cooldown); H2 committed recall hero's CTA got a fallback (dead button on the ordinary success path — §8.1's own bug reborn); H3 label measurement now keyed to slot font-band (collision reduction silently under-fired); H4 test-file lint; M3 pool-empty arm now completes the dawn (`visitForDawn`, +1 test — strip/dawn/state line tell one story); M1 desktop line dedupe (nav portal only); M2 SE fold carries ALL strip tokens + the aria-live twin announces the same string; M5 sheet `lang` = BCP-47 (was 'German'); M4/M6/M7 honesty fixes (welcomeBack de-numbered; classic CTA gated on error-free + missionless; clearedKeys closure-guarded via liveKeyRef); L-fixes: per-grade refetch → refetch-on-sheet-close, queue advances only over real buoys, cooling buoy aria tells the truth, cross-band swell collision (projected Y + bob amplitude), reduced-motion re-place on resize, documentElement.client* everywhere (scrollbar), positionsRef pruned, maxDawn to state, calm line suppressed under preparing hero, de 'Diese Runde'/fr 'Cette session' de-calqued. Deferred with note: sheet focus-trap (parity with frozen TidePracticeSheet), rest-buoy desktop slots, churn announcements (needs a key outside §9's 38), toFixed alloc, inert IntersectionObserver.
- **WordTide.tsx frozen-proof held all session:** `git hash-object` = `d4bcb0929af228c78d7c4f3f56aec8557cbbf095` before, mid, and after (final check at checkpoint).
- **Still in flight at checkpoint:** the §13 fixture-matrix harness (Opus subagent, headless mocked-Supabase, screenshots per fixture) and the Codex xhigh adversarial review (companion job running past the 10-min window). Their verdicts arbitrate against the POST-fix tree; findings will be folded before any commit call.
- **All gates green at checkpoint:** typecheck · eslint 0 new · check:i18n 1594/1594 ·  tests 27/27 · prod build. Nothing committed — commits stay on Robert's call.
- **Robert locked D-02** (A "too frantic", E inferior to D's visible `word · translation` buoys — glance-learning is a locked feature; verbatim in `design/rounds/round-02/VERDICTS.md`).
- **`design/DESIGN_SPEC.md` v2.1 FINAL**, built through the full gauntlet: Fable v1 draft (grounded in fresh reads of WordTide/DashboardPG/useTodayMission/useWordStates/FloatingWords/LingwaveWaves) → Opus 5 adversarial review (27 findings, 3 BLOCKER) + Codex xhigh (14 findings, 4 BLOCKER) → all 41 arbitrated, v2 redraft by Fable's hand → both reviewers delta-checked v2 → 18 remainders folded → v2.1. Convergent blockers that shaped the spec: `dawn` was in the canvas effect deps (lerp = 90 teardowns) → additive in-renderer easing + `clockRef` out-param; `counts.totalDue` excludes `newDue` → single `duePool` from `isLemmaDueNow` everywhere; miss-cooldown stall → reveals count as practice + never-lock-last; Speak completion → speak-code + `message_count>=2`, no `ended_at` requirement; guided 2.8MB chunk newly on glassy Home → disclosed, 800ms recall-first hero commit.
- **Spec architecture:** viewport-anchored fixed buoy/strip layer at the exported `WAVE_HORIZON_FRACTION`; projection API moves to `lib/waveField.ts`; 10 new files under `components/home/` + 8 edits (incl. additive-only `LingwaveWaves` changes — no rendered-output change, pixel-identity gated); `useTodayMission` gains `lessonId` + strict-active-language as DEFAULT; in-memory visit record, monotonic dawn `0.15 + 0.85·min(1, graded/proposed + 0.1·lesson + 0.1·speak)`, state line says "practiced" not "recalled"; two completion events (segment vs water-clear); 38 i18n keys ×3; full adversarial fixture matrix. WordTide.tsx stays frozen (verified dirty 149+/23− — wave-rider's); hash-equality acceptance.
- **Next:** Robert green-lights implementation (fresh session recommended; spec is self-contained). No product code touched this session.

## 2026-07-24 — Three-model home-redesign arbitration complete; Design OS Round 02 on disk (First Light / One Wave / A′); Robert's lock pending
- **Fable 5 + Opus 5 reviewed Codex's Round 01 independently, then Codex counter-verdicted (job task-mrzbn7qw-o5qq2t, xhigh):** both reviewers converged unprompted on "the sea is the progress instrument" — `LingwaveWaves` already exposes `dawn`/`energyRef`/`windRef`/`ripplesRef` (all unused on Home) and `FloatingWords.tsx` proves DOM words can ride the wave field. Codex conceded: the three-arc ring is withdrawn permanently (not deferred); the full-viewport ocean becomes the stage; bounded recall stays in place on Home (contract now "Home recommends and may start a bounded retrieval; destinations sustain"); Remember/Discover/Use stays internal vocabulary; streak flame and modality picker leave the first screen.
- **New code findings from the review round:** nothing-due Home renders NO primary CTA (`DashboardPG.tsx` dueTotal>0 gate + `WordTide` allClear gate) — Robert's "lots of wave where there is nothing" in its purest form; `grade()` visually retires missed words for the session; `useTodayMission` can recommend a lesson in a different language than the active pill; the near-opaque glass (`index.css` ~1095–1107, backdrop-filter can't sample the fixed canvas) means every stacked panel occludes the ocean — the card-stack IA is *why* Round 01 lost the sea. Codex verified 6/8 review claims CONFIRMED, refuted 2 (a per-language recall join already exists inside `compute_word_states`; `set-c` webp are curriculum word renders, not ocean plates — Fable's memory index corrected).
- **Arbitrated top 3 → Round 02 generated by Codex (design-os, resumed thread, one desktop regen):** `design/rounds/round-02/` — **D-02 First Light** (recommended: continue card in the sky, gold waterline CURRENT strip, ≤5 labeled word buoys riding real swells, session-local dawn echoed in text), **E-02 One Wave** (minimal: one sentence + one CTA + 3 bare links over near-uninterrupted water), **A-02 A′ Daily Current de-ringed** (conservative control), plus **D-02-desktop** (horizon-anchored, no 8+4 grid). All four verified by Fable's own image read: real contour-line ocean at ~40% horizon, visible gold, zero forbidden motifs. Round-01 `VERDICTS.md` filled with keep/change per mockup + binding cross-round rulings; caveat noted: renders run more ember-orange than the real plum-trough ramp (implementation uses the real component anyway).
- **Artifacts:** `investigations/FABLE5_HOME_REDESIGN_VERDICT_2026_07_24.md`, `investigations/OPUS5_HOME_REDESIGN_REVIEW_2026_07_24.md`, `investigations/CODEX_HOME_REDESIGN_COUNTERVERDICT_2026_07_24.md`; `design/rounds/round-02/{D-02,E-02,A-02,D-02-desktop}.png + PROMPTS.md + VERDICTS.md`.
- **Next:** Robert views Round 02 and locks D / E / A (or briefs an iteration). Implementation stays Design-OS-blocked; no product code, schema, or translations touched; `WordTide.tsx` (wave-rider's) untouched.

## 2026-07-23 — Home redesign investigation complete; Daily Current recommended; Design OS Round 01 awaits Robert + Fable verdict
- **Read-only product/learning-science investigation completed:** current default glassy Home, classic-vs-glassy drift, Word Tide behavior, SRS state, Today bridge/local persistence, Speak history, first-run/multi-language edge cases, and iPhone/desktop constraints mapped with file-and-line evidence. No product code or schema changed; the pre-existing `frontend/src/components/dashboard/WordTide.tsx` diff was left untouched.
- **Recommendation:** Home becomes the language-scoped orchestrator — one bounded best-next-action with reason + duration, then two to four alternatives. User-facing loop: Remember / Discover / Use. Today remains the guided-course owner; Media/Library/Lens/Speak keep their existing jobs.
- **Design OS Round 01 generated outside the deployable repo:** A Daily Current (recommended), B Living Lexicon, C Session Mixer. A composite daily-goal ring is explicitly deferred until guided progress, recall counting, Speak duration, language scope, and day-boundary rules are honest.
- **Artifacts:** `D:\CODING\ResonanceTEST\investigations\LINGWAVE_HOME_ORCHESTRATOR_REDESIGN_2026_07_23.md`; `D:\CODING\ResonanceTEST\design\rounds\round-01\{A-01,B-01,C-01,PROMPTS,VERDICTS}`.
- **Next:** Robert reviews the report and three directions with Fable, then returns a lock/iteration verdict. Design OS blocks implementation until that verdict; user explicitly requested no code in this phase.

## 2026-07-18 — B1 TTS BATCH COMPLETE: 958 generated / 0 failed, verification PASSED, de B1 ids FROZEN — commit call pending
- **Owner applied `20260717160000_guided_tts_b1_surfaces.sql` in the SQL editor** ("success") and said rerun. Resume verification green first: guided_tts pytest 59/59, typecheck clean, git status matched NEXT_STEP exactly.
- **Rerun `run_guided_bright_batch.py --level b1 --languages german --commit`:** in-run dry-run showed rows 1,120 / ready 69 / missing 1,051 (the aborted run's clips reattached — 69 ready rows vs "67 assets" because ready is counted per inventory row). **COMMIT: generated=958, failed=0, deduped_usages=93, run_id a7431310**, 37,560 provider chars paid (35,179 unique-text metric). No constraint errors — the migration fix held.
- **Verification PASSED (both scripts):** 0 failed assets globally, 12,541 bright playback rows total, **german-b1 P1–P10 = 1,804 rows** (168–192 per path, all 10 present), 0 rows missing public_url, 13/13 audio spot-checks incl. a german-b1 dialogue clip (83.6KB) and chunk clip. NEXT_STEP's "~1,120" was the inventory-row estimate; playback rows are per lesson-surface occurrence and legitimately land higher (repeated texts share one asset across rows).
- **de B1 ids now TTS-FROZEN:** germanB1.ts header gained the TTS-FROZEN block (corePhrase/chunks/trophyWord + dialogue turn-1/3/4 + pattern ex-1..N; profiles `german_b1_bright_p{n}_multiv2_v1`); typecheck re-run clean. Reminder: no frontend component requests turn-4 yet (future you₂ affordance, wiring optional).
- **UNCOMMITTED (owner commit call):** inventory.py, generate.py, dump script, both batch scripts, both pytest files, the migration file, germanB1.ts header (+ brain/protocol; WordTide.tsx stays unstaged — wave-rider's).

## 2026-07-17 (late evening) — B1 PAID BATCH FIRED AND ABORTED at 67/1,025 clips: prod DB check constraint rejects the new surfaces — migration written, crash-path hardened, rerun blocked on owner SQL
- **Owner said "fire"; batch ran and CRASHED in P1 L6:** `guided_tts_asset_usages_surface_check` (rescued-draft `20260517010000_guided_tts_v1.sql:181`) allows only corePhrase/chunk/trophyWord/speakTarget — every dialogue/pattern usage upsert 23514'd. Fable's miss: taught the pipeline the surfaces without checking the DB constraint. **67 clips paid + kept ready (2,331 chars, all P1, 0 failed) — no money lost;** 38 usage rows attached. Crash mechanics: unique-text usage failures were caught (13 warnings), but the same-run **dedupe branch's upsert_usage was unwrapped** (generate.py:394) and the first deduped pattern ex-1 killed the run.
- **Fixes:** (a) migration `frontend/supabase/migrations/20260717160000_guided_tts_b1_surfaces.sql` — drop+re-add the check with dialogue+pattern (playback view needs no change, it joins unfiltered); (b) generate.py ready-branch + dedupe-branch upsert_usage wrapped in the same never-abort try/except as the post-generation one; (c) regression test simulating the constraint (paid batch completes, assets stay ready, run marked failed for reattach-rerun) — guided_tts suites **59/59**.
- **BLOCKED on owner:** apply the migration in the Supabase SQL editor (service key can't DDL; migrations owner-kept). Then rerun the same batch command — idempotent: ~958 remaining clips generate, the 67 ready ones reattach usages only.
- **Committed + pushed `3a4712d8`** on the owner's call (11 files, +4,910: germanB1.ts, guidedLessons.ts, b1 validator, today-data baseline, SKILL.md, ru headers + 2 python rosters as ride-alongs, brain files; WordTide.tsx left unstaged). Owner skipped the device check and green-lit the TTS-seeder step directly.
- **Surface enumeration taught end-to-end** (per spec §5, playback contract `guidedAudio.ts`): `inventory.py` VALID_SURFACES + `dialogue` (emits turns 1/3/4 as `turn-1/3/4`; turn 2 = corePhrase, skipped) + `pattern` (emits `ex-1..N`); `guided-tts-lessons-dump.ts` now exports the two B1 fields (they never reached Python before); `run_guided_bright_batch.py` `--level b1` (B1_LANGUAGES=german only, level-scoped surfaces, b1 char cap 45k on the row-sum metric); `seed_guided_bright_rotation.py` `--level b1` (rotation offset = 2×paths, B1_SLUGS guard). NOTE: no frontend component requests `turn-4` yet (you₂ playback affordance TBD) — generated anyway per spec.
- **4 new pytest cases** (turn-1/3/4 emission + core-turn skip, ex ordering, A1-emits-nothing, full-scope count) — guided_tts suites 58/58. Frontend typecheck + eslint clean. **Mojibake incident owned:** a PS5.1 `Get-Content|Set-Content` round-trip genuinely corrupted the batch runner's em-dashes + added a BOM; repaired byte-exact via cp1252-reverse, verified 0 bad sequences (the standing rule "repo files only via Edit/Write" exists for exactly this).
- **Measured scope vs forecast:** 1,120 rows → **1,025 provider calls / 35,179 unique paid chars** (39,973 on the cap's double-counting row-sum metric) — spec §5's 20–25k forecast was pre-P2–P10. Pattern ships 2–3 examples per lesson (84×2, 16×3 — 216 rows, nothing dropped). First dry-run (pre-seed) proved the guardrails: static_thematic_de_laura_raw_v1 caught winning the resolver + wrong model + cap breach.
- **Owner approved the seed via AskUserQuestion** → 10 profiles `german_b1_bright_p{1-10}_multiv2_v1` seeded (Laura/William/Enniah/Helmut, offset 20 → P1 Laura after A2 P10 Helmut, no adjacent repeat; German has no speaker-gender constraint). **Final dry-run CLEAN:** missing_voice_profile 0, all 10 rotation profiles win, eleven_multilingual_v2, 3.1–3.9k chars/path, ready 0 / missing 1,120.
- **UNCOMMITTED:** inventory.py, generate-dump script, both batch scripts, test_guided_tts_inventory.py (+ brain/protocol). **Next: the owner-gated paid batch** `python scripts/run_guided_bright_batch.py --level b1 --languages german --commit` (~35.1k chars) — running it FREEZES the German B1 ids.

## 2026-07-17 (afternoon) — GERMAN B1 P2–P10 COMPLETE: the tier is 100/100 lessons, reviewed + arbitrated, all suites green, UNCOMMITTED
- **Owner pushed `af2ca125`** (B1-P1 batch + ja TTS headers + ja Library) and green-lit P2–P10 drafting; resume verification all green before starting.
- **Pre-drafting spec repair:** three trophy-vs-staging conflicts found and fixed in `tmp\B1_GERMAN_P1_P10_SPEC.md` (P3 KII staging realigned to trophy table — hätte L4/wäre L6/könnte L7/müsste L9; P5 ob→L4; P7 damit↔klappt L1↔L6 trophy swap). Validator gained 8 staged-form tripwires (obwohl P2L4, trotzdem P2L7, 5 KII stems) + a case-sensitive `stand` split (noun "Stand" false positive) + a P8 L8–10 comparative-anchor alternative (spec §2 stages comparatives there, no als/wenn slot exists).
- **Three sequential Codex batches at xhigh** (P2–P4 task-mroj57m3, P5–P7 task-mrokbumc, P8–P10 task-mrolfx0z; prompts tmp\B1_DE_P{2_4,5_7,8_10}_BATCH_PROMPT.md with full 10-beat arcs per path; results saved to tmp immediately). Gate results: batch 1 two fails (bare-article blank P3 L1 → infinitive blank; Stand false positive), batch 2 CLEAN on first landing, batch 3 two fails (both the P8 schema coarseness, fixed in the validator not the content).
- **Fable read all 90 lessons personally** (owner rule): staging honest in every path, episode shapes correct, P9 saga coherent Mon-breakdown→Fri-refund. Own-read fixes: P2 L3 them₂ deshalb-antecedent reframe, P3 L2 + P5 L2 terms not in episode, P6 L7 you₂ assert→question (learner asserting the shop's action).
- **Tier-wide adversarial review** (task-mron25yg, read-only xhigh, prompt tmp\B1_DE_P2_P10_REVIEW_PROMPT.md with per-shape dramaturgy checks): **67 findings (60 HIGH/7 MED) → 64 accepted/adapted, 2 rejected** (P2 L5 you₂ proposal self-contradictory; P3 L10 you₂ churn), 1 adapted to a lemma swap (bereitgelegt→bereitgestellt). Big classes: 5 more assert-class you₂ turns (P6/P9 → question-form passives), 5 punctuation-only um…zu chunk glosses (P7), 2 als/wenn chips not slot-forced (→ Immer wenn / zum ersten Mal), 4 "Ich fahre gestern"-template ungrammatical distractors (P3), P9 L6 timeline bug (gestern on a Monday beat = Sunday → am Freitag + slug rename), Modell trophy dilution (P3 L5→Variante, P4 L2→Version), non-native collocations (Kino→Kinobesuch absagen, Wasser behalten→war noch voll, an der Beratung→Beratungstisch …), 2 also-correct recall fallback sets, 2 pattern labels → real grammar terms, P10 L10 meta-language leak. All fixes applied by Fable's hand; 2 slugs renamed (ids unfrozen, no TTS yet).
- **ALL SUITES GREEN post-fixes:** typecheck ✓, validate-guided-draft-b1 **9,680/0** (100 lessons), today-data **9,807/0** (germanB1PathIds now 10, all-paths episode-field asserts), matrix 7,117/0, eslint 0 new, i18n 1,556/1,556, prod build 7.9s. guidedLessons.ts integrated all 9 new paths (imports/spreads/metadata list).
- **Skill accretion:** 8 new P2–P10 tripwires in author-guided-tier SKILL.md (trophy-vs-staging reconciliation, assert-class you₂, um…zu chunk gloss trap, als/wenn slot-forcing, present+gestern distractor template, lesson-range anchor schemas, saga day-word calendar checks, per-shape review-prompt checks).
- **UNCOMMITTED (owner commit call):** germanB1.ts (~4,800 new lines), guidedLessons.ts, validate-guided-draft-b1.ts, test-guided-today-data.ts, spec + 3 batch prompts + review prompt/findings/results in tmp\, SKILL.md. Plus the ru-session files and WordTide.tsx still pending from before. **B1 ids remain UNFROZEN** — TTS batch still needs the seeder/batch-runner taught the dialogue/pattern surfaces first (~20–25k chars forecast).
- **Next: owner commit call + device check of P2–P10 on the deploy**, then the B1 TTS decision.

## 2026-07-17 (midday) — RUSSIAN TTS LIVE (A1+A2, 878 clips, 0 failures, ids FROZEN) — the last owner-side TTS blocker cleared
- **Owner added the 4 ru voices to `public.voices`** (Alan, Maria, Mark, Nina — 2M/2F), unblocking the batch. Verified all four resolve to exactly one ru row each (voice_ids EDpEYNf6…/zWSsRd3J…/N8lIVPsF…/OwKgYRjZ…).
- **Roster wired [F, M, F, M] = Maria/Alan/Nina/Mark** in `seed_guided_bright_rotation.py` (ROSTERS) + `run_guided_bright_batch.py` (LANGUAGES ru=10). The order is deliberate: A2's rotation offset (=10 A1 paths) means A2 Pn = names[(10+n-1)%4], so [F,M,F,M] lands **odd paths female / even paths male** exactly per `tmp\A2_RUSSIAN_VOICE_GENDER_PLAN.md` — verified all 10 A2 paths (P1 Nina-F, P2 Mark-M, P3 Maria-F, P4 Alan-M … P10 Mark-M). Russian past tense agrees with the speaker, so a wrong roster order would desync every A2 past-tense line from its voice. A1 is gender-free so its rotation is unconstrained.
- **20 profiles seeded** (`russian_{a1,a2}_bright_p{n}_multiv2_v1`), dry-runs clean (missing_voice_profile 0, every rotation profile won the resolver, per-language chars under 12k cap). **Owner approved the paid run via AskUserQuestion** (878 clips / 14,193 chars, eleven_multilingual_v2). Commit runs: **A1 393/393, A2 485/485 generated, 0 failed** (run_ids 827ffa78, 6f6cf2b9; 21+15 deduped usages).
- **Verified:** 914 `guided_tts_playback` bright rows across all 20 ru paths (414 A1 + 500 A2 — matches dry-run rows exactly), **0 failed assets globally**, public_url spot-checks 200/audio-mpeg with real content-length. **ru A1+A2 ids now TTS-FROZEN** — both module headers (russianA1.ts / russianA2.ts) updated (A2 header notes the per-path voice gender is now locked to the canon).
- **UNCOMMITTED (owner commit call):** the 2 header edits (russianA1.ts, russianA2.ts) + the 2 python-script roster additions (shared with the ja-session's uncommitted state — additive, ja roster untouched). Batch output itself is already live in Supabase, not a git artifact.
- **Next: owner device-check ru A1+A2 audio on the deploy** (real ElevenLabs voices now, not browser speech; check an A2 P3/P9 past-tense lesson to hear the female narrator agree with the -ла canon).

## 2026-07-17 (morning) — B1 DEVICE GATE PASSED → GERMAN B1 P1 COMPLETE (L4–L10 drafted, reviewed, arbitrated) + build-step cue contract fix + EN-locale German-leak i18n repair | UNCOMMITTED, owner: device check + commit call
- **Owner device-checked the 3 pilots (desktop, voice feedback): content approved, drafting green-lit.** Issues found: (a) Build-the-sentence never SHOWS the sentence to translate — at B1 the scene withholds you₁, so the learner reached the chips with no cue (only a robotic browser-speech readout); (b) robotic voice noted — confirmed as the by-design pre-TTS fallback, owner's "TTS for everything you listen to" question answered yes (dialogue/pattern surfaces, ~20–25k chars, owner-gated post-review); (c) mic orb slightly cut off on desktop → parked as polish backlog.
- **Interaction-contract fix (per NEXT_STEP's "fix before drafting"):** `BuildPhraseStep` now renders a "Your line" cue card — `corePhrase.baseText` resolved to the user's base language, guarded on the validator-pinned `build.targetText === corePhrase.targetText` identity. New key `today.build.cueLabel` ×3.
- **i18n repair the owner's screenshots exposed ("4 von 7", "Leeren" in the EN UI):** ~30 German values leaked in the EN block's today.* keys (progressLabel, clearAnswer, step labels, speak/review feedback…), FR block worse (German + English leaks incl. `today.path.overviewLabel: 'Today'`), plus prototype-era strings hardcoding the en↔de pair in ALL THREE locales ("Say the sentence in English", "German cue", "Was passt auf Englisch?") — all fixed/neutralized ("Say the sentence out loud", "Cue"/"Hinweis"/"Indice"…). "Arrange the chips into the complete **polite question**" → "complete sentence" ×3. i18n 1,556/1,556.
- **Codex draft batch (task-mrnuudd0-9y71tk, xhigh, 14.6 min):** L4–L10 appended to `germanB1Practical1Inputs` per `tmp\B1_DE_P1_L4_L10_BATCH_PROMPT.md`; validator 0-fail on first landing; staging honest (L4–5 war/hatte, L6–7 modal Präteritum, L8–10 mixed 2-event); all 7 reserved trophies verbatim-anchored; register mix 4 du / 3 Sie; no template collapse.
- **Fable read all 7 + adversarial review (task-mrnvjn4f-m7r8jy, read-only xhigh): 14 findings → 13 accepted/adapted, 1 rejected** (stylistic churn on natural English). Real catches: L8 you₁ "musste den falschen Bus nehmen" semantically off (necessity ≠ accident) → rewritten "…zur Bushaltestelle laufen und bin falsch eingestiegen" with full cascade (slug renamed `ich-bin-falsch-eingestiegen`, chunks 5, recall re-derived to `eingestiegen` — also breaks the musste×3 recall monotony, speakRequired, pattern example + label `Konnektoren`); sein-Perfekt blank had haben-participle chips (bin+eingekauft) → sein-verb set; `hatte` as modal chip (ungrammatical, tests nothing) → `durfte` ×3; L7 cloze 3→2 blanks (its connector blank had only ungrammatical distractors). **Fable-only findings the reviewer missed:** L5 answered a live present-tense them-turn with past narration → them-turns reframed as next-day retrospective (you-turns + mechanics untouched); L8's SECOND connector blank also mixed dann/danach. **Arbitration lesson: the reviewer's own replacement chips (deshalb/später) were causally plausible in-slot — every proposed chip re-checked; dann/danach never in one chip set (Codex did it 5×).** Tripwires accreted to author-guided-tier SKILL.md (chips, episode-tense coherence, codex registry paths, rescue-agent-can't-babysit).
- **Suite baselines grown:** today-data B1 count 3→10, trophy matrix segment-1 cards 3→5. **ALL GREEN:** b1 validator 1,006/0, typecheck ✓, today-data 9,762/0, matrix 7,117/0, a2 56,040/0, eslint clean on changed files, i18n 1,556/1,556, prod build 9.4s. B1 ids still UNFROZEN (no TTS). Artifacts: `tmp\B1_DE_P1_L4_L10_{BATCH_PROMPT,CODEX_RESULT,REVIEW_PROMPT,REVIEW_FINDINGS}.md`.
- **Next: owner device-checks P1 L4–L10 on the live deploy after commit** (path now shows all 10 lessons; segment reviews/path check no longer "unavailable") **+ commit call for this session's files** (germanB1.ts, BuildPhraseStep.tsx, translations.ts, 2 test baselines, skill accretion).

## 2026-07-17 (early morning) — JAPANESE TTS LIVE (897 clips, 0 failures, ids FROZEN) + JAPANESE LIBRARY TIER 1 (all 1,850 concepts translated by Fable's hand), Codex adversarial review IN FLIGHT | concurrent with the B1 Phase-0 session
- **Owner green-lit both ja tracks by voice** (ru library already existed; ja was missing from the Library entirely). Session ran concurrently with the B1 Phase-0 session in the same tree — guided engine files were deliberately never touched.
- **Japanese guided TTS (owner-approved via AskUserQuestion, 8,320 chars):** owner had added 4 ja voices to `public.voices` (Akane/Koichi/Konoha/ishibashi); ROSTERS/LANGUAGES wired in `seed_guided_bright_rotation.py` + `run_guided_bright_batch.py` (order alternates F/M-leaning; ja gender-unconstrained per plan), 20 per-path profiles seeded (`japanese_{a1,a2}_bright_p{n}_multiv2_v1`, A2 rotation offset 10 → no adjacent repeats), dry-runs clean, then commit runs: **A1 409/409, A2 488/488 generated, 0 failed** (run_ids 5f2c2295, d2997254). Verified: 991 `guided_tts_playback` rows across all 20 ja paths (473 A1 + 518 A2 — matches dry-run rows exactly), audio spot-checks 200/audio-mpeg. **ja A1+A2 ids now TTS-FROZEN** — both module headers updated to say so (the old "not frozen" notes were a live landmine).
- **Russian TTS BLOCKED owner-side:** `public.voices` has ZERO ru rows. Seeder carries a comment: add ≥2F+≥2M, map odd-F/even-M per tmp\A2_RUSSIAN_VOICE_GENDER_PLAN.md, then seed+batch is one command each.
- **Japanese Library Tier 1:** all **1,850 concepts × 19 theme packs** translated personally (no subagents), category-by-category with en/de/fr/ko/ru as sense cross-reference. Contract mirrored the guided-ja spec: natural everyday register, standard orthography (core-domestic-animal kanji, katakana species/loanwords, kana where normal), never romaji; nouns citation form, verbs dictionary form, adjectives plain form. 70 `needsReview` flags, 335 `reviewNote`s — many documenting SOURCE sense errors corrected against category context (seal=stamp, honey=darling, fly=verb, sole=only, fine=okay, organ=Orgel…) and deliberate near-duplicate splits (mouse/rat, crocodile/alligator, tangerine/mandarin/satsuma, bill/invoice, checkout/cash-register, reservation/booking→予約/ブッキング). Merge via scratchpad script (JSON parse→inject ja after ru→re-stringify; diff purely additive, 5,955 insertions). Registry: `'ja'` in `StaticCategoryTargetLanguageCode` + metadata entry 日本語 (experimental), script 'Japanese' — Library dropdown picks it up automatically; cards silent until Tier 2 (by design).
- **Stale-list fix:** `test-static-category-translations.ts` hardcoded expected languages ending at 'ko' — had been failing since Russian Tier 1 shipped (same class as the A2_LANGUAGES tripwire). Extended with 'ru','ja'; now green.
- **All gates green:** typecheck ✓, i18n 1,555/1,555, static-library-integrity OK (0 fallback translations = full ja coverage), static-category-translations OK, vocabulary-library OK, guided-today-data 9,762/0, eslint clean on changed lines (>500KB Babel note only), prod build ✓. Pre-existing failure NOT from this work: `test-generate-category-picker-flow` asserts a `/generate` entry point the nav restructure removed (stale source-string class, documented in STATE Known problems).
- **Codex adversarial review (xhigh) DONE same session: 33 findings (13 wrong-sense / 12 unnatural / 1 orthography / 7 image-dependent), zero same-level ja duplicates, wiring clean.** Arbitration: ALL 33 accepted or adapted (none rejected — reviewer quality was high), plus the pill→丸薬 counterpart the tablet fix required = 34 applied. Notable: tablet タブレット→錠剤 (device false-friend on a health card), infection 感染症→感染, date ナツメヤシ→デーツ, painter 画家→塗装工 (trades cluster + ru), moonlight 月光→月明かり, movie ムービー→映画 (cross-category dup with film is fine — dedupe is per level, カヌー precedent), talent_show のど自慢大会→特技発表会; 4 adapted rather than copied (doubtful=半信半疑だ, heel=靴のかかと parallel to 靴底, earrings/bottle_gourd kept with needsReview+image-check notes since the replacement is image-dependent). Post-fix gates re-run green: typecheck ✓, static-category-translations OK, static-library-integrity OK (0 fallbacks, duplicate count unchanged 258 = no new collisions). **Library ja is now review-clean — awaiting owner commit call** (scoped: categories.ts, staticCategoryTranslations.ts, test-static-category-translations.ts, japaneseA1/A2.ts headers, 2 python scripts).
- **Owner signed off all six §8 decisions** (voice): flow + spine + embedded grammar trusted as designed; **progress persistence = localStorage-only through the B1 era** (no users yet; Supabase progress revisit later, explicitly open to it); TTS cadence unchanged (same as German A1/A2 — later, post-review).
- **Engine (design §4 items 1–5, 8–9), all keyed on `lesson.level`:** `GuidedLessonLevel` union + episode types (`GuidedDialogueTurn`/`GuidedPatternSpotlight`/`GuidedCloze` (+blank taxonomy)/`GuidedRegister`) + optional variant fields copied through BOTH resolved layers (`GuidedLesson` + `resolveGuidedLessonVariant`) + `getGuidedClozeText`; `getSessionSteps()` returns the 7-step B1 sequence; TodaySession gained clozeState/rolePlayState, canContinue rules, B1→legacy result mapping (cloze→type fields summed-per-blank, rolePlay→speak fields worst-of-two + new optional `clozeBlanksTotal/clozeBlanksFirstTry/rolePlayTurnsPassed`), dynamic rail/pill, icons/titles/visual-states; NEW components `PatternStep` (spotlight card, reusable), `ComplicationStep` (them₂ auto-reveal+audio, multi-blank cloze, fallback chips on miss, "show pattern again"), `RolePlayStep` (two-part GuidedSpeechPrompt generalization, them-audio cues each part); SceneStep/TodayHero B1 branches hide you₁ (them₁ preview instead — the delayed-production contract now holds pre-lesson too); CompleteStep episode recap (chat-bubble 4-turn); GuidedCheckpoint path-check mode renders before/blank/after for B1; `GuidedAudioSurface` += `dialogue`(turn-1/3/4) + `pattern`(ex-1..3); B1 matchPairs draws from lessonItems (whole episode) not chunks; 23 i18n keys ×3 locales.
- **germanB1.ts scaffold + 3 handwritten P1 pilots** (the device-gate lessons, design §6): lost key (du, Perfekt, taxi/colleague follow-up), stopped S-Bahn (Sie, separable participles, "warum haben Sie nicht angerufen?"), concert weekend (du, sein-Perfekt, interested listener). Trophies verloren/plötzlich/danach (verified free against 203-word German corpus). Registered after German A2 P10 — A2 flows into B1 automatically.
- **validate-guided-draft-b1.ts (316/0):** episode shape, you₁≡corePhrase, cloze-concat≡you₂, blank taxonomy vs per-path schemas (+anchor-answer whitelists P2/P3/P4/P7/P8), pattern honesty (token-subset episode-reuse check), per-lesson register consistency, stage-gated war/hatte/es-gab/modal-Präteritum by path+lesson, weak+strong Präteritum and person-complete future bans, cross-corpus trophy uniqueness.
- **tmp\B1_GERMAN_P1_P10_SPEC.md** (anchor staging L1–L10 per path, episode shapes, blank schemas, register tendencies, **100-trophy pre-allocation** — zero collisions) + **author-guided-tier SKILL.md B1 branch** (scaffold/spec/batch-prompt/review-prompt/validator/TTS deltas + the level-keying tripwire).
- **Codex adversarial review of the diff: 15 findings (1H/11M/3L) → 14 applied, 1 rejected.** The HIGH was real: RolePlayStep published turn-status 'passed' after turn 1, unlocking completion without turn 2 — fixed (aggregate never 'passed' until both turns pass). Also applied: matchPairs source, TodayHero you₁ leak, situation resolved to base language (B1 scene + hero), them-audio autoplay on step entry (best-effort), multi-word blank answers flattened into rolePlay required tokens, per-blank attempt summing, validator anchor/rail/pattern strengthening (gab-without-es, weak-Präteritum list, staged forms, token-subset), 3 also-correct distractor chips swapped in the pilots, L3 pattern-rule + gloss precision, skill wording, i18n copy (en "They respond", de b1Goal rephrase, fr Réplique). Rejected: partial-path consumers (segment-2/path-check land on graceful "unavailable" — transient Phase-0 state, gone when P1 fills; owner told).
- **All green post-fixes:** typecheck ✓, b1 316/0, a2-phase2 56,040/0 (untouched), today-data 9,762/0 (+B1 rows), matrix 7,109/0 (+[B1 matrix] block), eslint 0 (1 pre-existing warning in untouched LevelDetailPage), i18n 1,555/1,555, prod build 8.9s. TTS note: B1 ships silent-safe (browser speech); seeder/batch-runner surface enumeration must be extended before the first B1 TTS run.
- **Next: owner device gate** — run the 3 pilot lessons on iPhone (B1 P1 appears after German A2 P10 in the path selector; cloze keyboard/blank flow + two-part rolePlay ASR are the contract under test). Codex P1 L4–L10 batch only after that gate.

## 2026-07-16 (night) — B1 TIER DESIGNED: FABLE_B1_LEARNING_PATH_DESIGN.md v2 written, Codex-adversarially-reviewed (17 findings, 17 arbitered accept/adapt), owner sign-off pending | Fable designs, Codex reviews
- Owner directive (voice): design B1 with its own, more sophisticated but repeatable lesson system; answer the grammar question from language science; pilot **German** (owner verifies natively); skip Tagalog/Dutch/Hindi/Arabic for now.
- **The design** (`docs/Product/FABLE_B1_LEARNING_PATH_DESIGN.md`): persona ladder Traveler→Regular→**Local**; 10-path spine each = one discourse function + one staged German **anchor family** (P1 narration/Perfekt+Präteritum-of-sein/haben/modals, P2 obwohl/trotzdem, P3 Konjunktiv II, P4 relatives Nom/Akk, P5 indirect questions, P6 Passiv, P7 um…zu/damit, P8 als/wenn, P9 mixed complaint capstone, P10 consolidation — mirrors A2's P9/P10 rhyme). **Four-turn episode** (them/you/them/you — learner produces BOTH turns) with **delayed complication**: them₂ lands only after the learner builds you₁. New 7-step session: scene(setup+them₁) → matchPairs → pattern (embedded grammar spotlight) → build(you₁) → complication(them₂ reveal + multi-blank cloze of you₂) → rolePlay (speak both turns cued by them-lines) → complete(episode recap + trophy). Three licensed episode shapes (Complication/Listener/Negotiation) assigned per path prevent template collapse at lesson 47. Cloze blank taxonomy: form (lemma-cued), connector, choice-chips. Grammar question answered: **no standalone grammar lessons** — explicit-but-embedded pattern spotlights + staged anchor families + pushed-output cloze (Norris&Ortega/Swain/DeKeyser/Schmidt, with the honest caveat that "embedded beats isolated" is a design choice, not a meta-analytic result).
- **Codex adversarial review (fresh thread, 17 findings: 6H/10M/1L) — all 17 arbitered accept/adapt, v2 rewritten.** Biggest: v1's scene revealed the whole episode up front (temporal inversion killing communicative need) → the delayed-complication flow; one-template-fits-all → episode shapes; "exactly one lemma blank" contradicted my own Präteritum rail (Gestern kaufte…) → blank taxonomy; resolved `GuidedLesson` type + `resolveGuidedLessonVariant` hand-copy fields (guidedLessons.ts:551-567) and `GuidedAudioSurface` closed union (guidedAudio.ts:5) both must widen — v1 missed both (verified by hand before accepting); checkpoint path-mode prompt needs a B1 before/blank/after branch (GuidedCheckpoint.tsx:409-483); author-guided-tier skill is A2-shaped end-to-end → B1 pipeline branch is now rollout Phase 0; localStorage-only progress at 300 lessons/language escalated to owner decision.
- Engine survey findings baked into §4: nothing reads `lesson.steps` (key on `level`), typeRecall stays in B1 data purely for review/checkpoint machinery, TodaySession plumbing enumerated (canContinue/state/icons/titles/visual-states/rail), new validator `validate-guided-draft-b1.ts` speced, TTS ~20–25k chars for German B1, ids unfrozen until batch.
- Rollout: Phase 0 = engine + pipeline branch + spec + **device-check of cloze/rolePlay on 2–3 handwritten lessons BEFORE any Codex batch**; Phase 1 = German P1 pilot; Phase 2 = P2–P10; other languages later. **Next: owner decisions §8 (spine, flow, grammar approach, pilot gate, progress persistence, TTS cadence).**
- State verify at session start: typecheck clean, a2 56,040/0, today-data 9,757/0, matrix 7,093/0, git clean except WordTide.tsx + memory + accreted SKILL.md.

## 2026-07-16 (evening) — RUSSIAN + JAPANESE A2 COMPLETE AND SHIPPED `2b58fd94`: 200/200 lessons drafted, gated, Fable-read, reviewed, arbitrated, integrated, ALL SUITES GREEN | Fable orchestrating parallel Codex batches
- **The A2 tier now covers 12 languages.** Both new tiers ran the full author-guided-tier pipeline in ONE session with ja and ru batches drafting in parallel (distinct files) the whole way: 8 draft batches (P1–P3/P4–P6/P7–P9/P10 ×2) at xhigh, each validator-gated + Fable-read in full (200/200 lessons personally read). First-pass validator quality was extraordinary: ja 1,632/0→3,264/0→4,896/0→5,440/0 with ONE false positive (my かな regex matching 静かな — fixed sentence-final-only); ru 1,602/0→3,203/1→4,806/0→5,340/0 with the single failure being my own готов speak-token tripwire hitting subject-agreement (Мой телефон уже готов — validator can't know the referent; swapped token to уже, kept the tripwire strict). Total hand fixes across all 8 batches: TWO cosmetic ja edits + ONE ru token swap.
- **Russian gender plan executed flawlessly:** every speaker past/short-adjective in canonical text matches the path's pinned gender (odd F / even M), `genderForms {voiced, other}` declared on every gendered lesson, pair-teaching glosses (Frau/Mann) with alsoAccept everywhere, recall/speak never gendered, futures gender-free carried P4. The scaffold's mechanical swapped-phrase speak variants worked exactly as designed.
- **Adversarial reviews arbitrated:** ja 30 findings (17H/13M) → 23 accepted/adapted (2 corePhrase rebuilds: P1·10 大丈夫→よく わかります, P6·9 two-turn answer-then-ask; 町の中心 fix; 友達への beneficiary; 2 staff captions; the ね my own P8 arc specified; de/en calques; budget-leak pedagogicalGoal; synonym fallback) / 7 rejected (ましょうか contract-literal ban would break sizing; 4× P9 per-lesson-past over-generalization — the capstone mix is PATH-level, Korean precedent; chunk re-split churn; idiomatic-en nitpick). ru 30 findings (22H/8M) → 28 accepted/adapted (Какая футболка caption fixing the синяя agreement mismatch + das blaue gender fix — reviewer's capitalization claim itself wrong; 2 pedagogically-better re-blanks собой/дня; P4 caption returning proposal-origination to the learner; trophy-example naturalness ×5; caption swaps adapted to avoid duplicating sibling lessons' content; 2 authoring-leak fields; de/en calques) / 2 rejected (both = caption-whitelist reviewer failure mode; one had fine coherence). ALL fixes applied by Fable's hand, re-gated after.
- **Integration single-pass:** guidedLessons.ts imports+spreads+path options (ru A2 after ru A1, ja A2 after ja A1), today-data selector allowlist + V0-path allowlist + 80 new asserts (9,757/0), trophy matrix A2_LANGUAGES += Russian/Japanese (7,093/0), full a2-phase2 validator 56,040/0, typecheck, speech suite, eslint (only the known >500KB Babel note), i18n 1,532/1,532 ×2, prod build 5.9s — guided chunk lazy at 6.29 MB, main bundle untouched. Committed `2b58fd94` scoped (6 files; WordTide.tsx + memory files excluded) + pushed.
- **No TTS by design** — ids unfrozen, text fixes cheap. Owner picks ElevenLabs voices later: ru roster must supply ≥2F+2M mapped odd-F/even-M per tmp\A2_RUSSIAN_VOICE_GENDER_PLAN.md; ja roster unconstrained.
- Skill accreted (A2-for-new-languages block: verify-loop post-read edits, false-positive regex classes, path-level capstone phrasing for review prompts, sibling-duplication check on caption swaps, parallel-language batching, npx-rg trap) + ru/ja per-language notes updated with A2 state.
- Earlier this session (see prior entry): A1 commit `353253cf`, glass-blur + Library fixes `e7b2c98e`.

## 2026-07-16 — A1 frontier COMMITTED+PUSHED `353253cf`; desktop glass-blur + Library-language bugs fixed `e7b2c98e`; ru/ja A2 pipelines OPEN (ja batch 1 gated 1,632/0 + Fable-read; ru voice-gender plan written, batch 1 drafting) | Fable + codex investigations/drafts
- **Owner call executed:** ru/ja A1 frontier batch committed `353253cf` + pushed (scoped add, wave-rider's WordTide.tsx excluded). Русский/日本語 A1 + hiragana module live on the deploy; ids still unfrozen (no TTS).
- **Desktop glass blur regression — root cause was NOT the header redesign per se:** the CSS minifier (Tailwind v4/LightningCSS pipeline) treats `backdrop-filter` and `-webkit-backdrop-filter` as alias duplicates and keeps only the LAST declaration of a pair. Our convention wrote standard-first/webkit-last → bundle shipped **webkit-only blur on 59 of 73 declarations**, and current desktop Chrome has dropped the `-webkit-` alias → frosted glass dead in production app-wide (dev unminified = invisible locally; iOS Safari unaffected, still reads webkit). It "regressed at the header redesign" because `1834646f` removed the Tailwind `!backdrop-blur-3xl` utility that had been emitting the standard property as an accidental mask (same mechanism explains the mysterious May `66cea8ff` "cascade override" fix). **Fix: reordered all 48 source pairs webkit-first/standard-last** (node script, UTF-8-safe) — minified bundle now keeps BOTH (59+59). Verified empirically: headless-Chrome harness loading the real compiled dist CSS with the replicated app-shell/nav/main DOM — before: crisp stripes through the nav + computed `backdrop-filter: none`; after: full frosted smear. Codex investigation located the commits; Fable diagnosed the minifier mechanism (Codex's !important cascade theory was wrong — nothing fights the cascade) and fixed.
- **Library ignores active language (owner: home=Korean, Library=German):** `readStaticLibraryTargetLanguage` precedence was query → stored Library key → active language, so any old stored pick shadowed the current study language forever; plus `CategoryListPage` froze the language in a one-time `useState` initializer evaluated before LanguageProvider settled. Fix: precedence now query → **active language** (skipped when it has no static pack, e.g. Japanese Tier 0) → stored key; list page derives the language live with the in-page selector as session-only override; first-visit chooser now waits for `languageReady` in all non-query cases (kills the stale-language flash). Detail/Level pages derive live already — the shared-helper change fixes their queryless entry too. Codex located it; Fable implemented (slightly different shape than Codex's proposal).
- **Alphabet-for-ru/ja owner ask = already satisfied:** Script Lab is registry-driven (StudyModeSelector tile + /alphabet both render from `getScriptsForLanguage`); Cyrillic + Hiragana packs registered → identical surfacing to Korean's Hangul. The owner saw it "missing" because the Library bug had them on German (no script pack). No code needed.
- Both fixes committed `e7b2c98e` + pushed (typecheck, eslint, i18n 1,532/1,532 green).
- **Russian A2 UNBLOCKED — voice-gender plan written** (`tmp\A2_RUSSIAN_VOICE_GENDER_PLAN.md`, inverted Polish §5.3: plan dictates the future TTS roster): **odd paths FEMALE / even paths MALE** (P3/P9 female past -ла, P10 male Я приехал callback — mirrors Polish anchors); canonical text matches path gender, `genderForms: {voiced, other}` scaffold field generates swapped-gender accepted answers mechanically; recall/speakRequired/trophies never gendered; **бы banned outright; future is gender-FREE in Russian** (буду+inf, perfective non-past) so P4 uses real futures — easier than Polish. Roster consequence: ≥2 female + 2 male ElevenLabs voices when the owner buys TTS; A1 unconstrained (gender-free).
- **A2 pipelines stood up for both:** scaffolds `japaneseA2.ts`/`russianA2.ts` (koreanA2 compact pattern; ja carries targetKana/kana fields → kana speak+type variants; ru carries genderForms + ё-fold + NFD-fold answers), specs `tmp\A2_JAPANESE_P1_P10_SPEC.md` (wakachigaki carried, です・ます all paths incl. friends, past ました whitelist P3/P9, ています named-budget, から-only reasons — ので banned, kanji-numeral rule) + `tmp\A2_RUSSIAN_P1_P10_SPEC.md` (вы all paths, gender plan woven through, case-correctness checkpoint, потому-что-only), validator configs japanese+russian in a2-phase2 (ja: floating-か/keigo/plain-form/digit tripwires — かな needed a sentence-final-only fix, 静かな false positive; ru: ты/бы/давай bans + whitelist-enumerated gendered-form recall/speak bans — raw -л/-ла endings aren't regex-distinctive like Polish -łem). Forbidden-trophy lists extracted by module import (100 ja / 100 ru → tmp).
- **ja batch 1 (P1–P3) DONE by Codex at xhigh: validator 1,632/0 (one false positive = my かな regex), and Fable read all 30 lessons — quality at the Korean bar,** trophy traps respected, natural 方が/から/にします grammar, correct particles, kana variants everywhere. 2 surgical fixes queued for when the job's verify loop closes (P1·9 caption spacing いくつ ですか→いくつですか; P3·8 chunk gloss carries the negation: 行って いません。 glossed "war ich./I have been."). Batch 2 prompt (P4–P6 + batch-1 trophy embargo) pre-staged.
- **ru batch 1 (P1–P3) drafting** (task-mrnc3t9k-uhladt); ja job task-mrnb5z1r-1mk8nu still in self-verify. Background watcher polls the registry.
- Ops: `npx rg` is NOT ripgrep (bogus rg@0.0.2 npm package — use the Grep tool); trophy-matrix A2_LANGUAGES must gain Japanese+Russian rows in the same commit as integration.
- **Owner call (voice):** Japanese as a brand-new language AND Russian Tier-5 ungated, A1+A2 both, in parallel, Codex-heavy. TTS deliberately absent (no ja voices exist yet; ru TTS is a later paid decision) — all content `contentStatus: 'draft'`, NO ids frozen, text fixes stay cheap.
- **Japanese foundations:** Tier 0 wired (`languages.ts` 日本語/ja/#eab308 wizard tile, 6 langName keys en/de/fr, FlagIcon JP; ja already CJK-font- and transliteration-covered). Hiragana Script Lab pack shipped to tree (`data/scripts/japaneseHiragana.ts`, 46 gojūon kana / 5 quizzable sections, Hepburn validator-enforced, registry entry; dakuten/yōon = documented follow-up; script-lab suite 3997/0).
- **Japanese hard case SOLVED by design** (the §5.3 analog): ja-JP ASR returns unspaced text → `guidedSpeechCheck.ts` gained a CJK branch (space-insensitive exact/variant matching, substring requiredTokens with in-order check; Hangul deliberately excluded — Korean ASR emits spaces); `test-guided-speech-check.ts` +7 cases all green. Content-side: **wakachigaki** (learner-textbook word spacing, particles attached to hosts) makes Japanese mechanically identical to spaced languages — BuildPhraseStep's `join(' ')` needs zero changes.
- **Pipeline stood up for both:** specs `tmp\RUSSIAN_A1_P1_P10_SPEC.md` (kill-rule: 100% gender-free learner lines — no past tense, no рад/рада; вы-register; ё kept in target text with е-fold acceptedAnswers) + `tmp\JAPANESE_A1_P1_P10_SPEC.md` (wakachigaki, survival-kanji orthography with Hepburn readings in every item gloss, conjugation whitelist です/ます + fixed て-ください chunks + budgets ね≤1/ましょう≤2/ています named-only, kana acceptedAnswers everywhere, ASR-stable speak tokens); scaffolds `russianA1.ts`/`japaneseA1.ts` (koreanA1 pattern; `russianAccepted` ё-fold, `japaneseAccepted` kana variants; builders set draft status + 0.65 threshold); type unions + `GUIDED_TARGET_LANGUAGE_SPEAK_LOCALES` (ru-RU/ja-JP) + `api/guided-transcribe.ts` Whisper map extended — NO todayLanguage/path-options exposure until review-clean; per-language draft validators (korean-a1 pattern + language tripwires: ru gendered/ты/slang token+pattern bans, ja romaji-ban/floating-particle/wakachigaki/conjugation-budget checks).
- **8 Codex draft batches at xhigh (P1–P3/P4–P6/P7–P9/P10 ×2 languages), each validator-gated + Fable-read in full (200/200 lessons personally read):** ru validator 3,412/0 and ja 4,493/0 final; ja batches 2–4 were validator-clean FIRST PASS. Hand fixes were tiny: ru 3 (2 stilted trophy examples, 1 same-meaning build distractor), ja 4 (spaced-か joined ×19 — my own spec induced it, convention corrected to attached か; ASR meta-leak in one whyThisWord; 1 same-meaning distractor; clunky de gloss ×2). Validator itself accreted real-language exemptions: reflexive -ться/-тись, -ти/-чь infinitives, adjective two-char stems (спокойный→спокойн), ja dictionary-form verb stems (書く→書).
- **ru adversarial review ARBITRATED (~90 findings → 18 accepted, ~72 rejected):** accepted 2 corePhrase rebuilds (P7 bus-stop Извините opener; P10 «Я всё лучше понимаю русский» word order), 4 trophy-example naturalness fixes, de/en indirect-question gloss set (~10), Piroschki/Piroschok, interjection recall re-blank, секунду same-meaning fallback swap. **Rejected as a NEW reviewer failure-mode class (55):** buildChips flagged for forming valid *different-meaning* sentences — corpus convention allows grammatical distractors (build step is meaning-anchored); reviewer's replacement chips were deliberately ungrammatical Russian (anti-pedagogical). Also rejected 7 adjective-blank recall findings (contract-literal, pedagogy-false → spec rule 8 amended: adjective blanks allowed when the adjective IS the teaching point) and 4 idiomatic-corePhrase churns. Fixes applied UTF-8-safe (python io, never PS piping), re-validated 3,412/0. NOTE: companion `result` output truncated mid-LOW-section — visible LOWs handled.
- **State at save:** BOTH A1 tiers fully drafted (ru 100 review-clean; ja 100 gated+read, adversarial review task-mrmd1d5c-iv1soe RUNNING). Integration deliberately deferred until ja arbitration (single baseline-churn pass); integration map ready (guidedLessons.ts imports/spreads/path-options after Korean A1 block; todayLanguage TARGET_LANGUAGES += Russian, Japanese; no useTodayMission bridge needed — names match; today-data suite needs ru/ja per-path asserts mirroring Korean A1's).
- **Ops:** codex-companion job registry prunes records (day boundary?) — `result` can return "No job found" after completion; content is disk-verified via validators, so gate on files not registry. Mid-write states of a running content job transiently break `tsc -b` (known class — wait for the job's monitor before diagnosing). Rescue agents return job ids immediately; Fable monitors via `status <id> --json | grep '"status"'` loops.
- **SESSION CLOSE — BOTH LANGUAGES INTEGRATED, ALL GREEN:** ja review arbitrated (63 findings → ~43 accepted incl. 3 corePhrase rebuilds honoring the 3-token constraint, ワイファイの spec restoration, topic-gloss class ×17, 6 interlocutor scene reframes; ~20 rejected incl. trophy-strip auto-rejects and the redundant-topic class), 7 goal remnants swept, ja validator 4,491/0. Integration: guidedLessons.ts imports+spreads+path-options (ru/ja after Korean blocks), todayLanguage TARGET_LANGUAGES += Russian/Japanese, today-data baselines extended (allowlist, selector, +60 per-path asserts → 9,677/0). Full battery green: typecheck, a2 45,260/0, ru 3,412/0, ja 4,491/0, matrix 6,173/0, speech, script-lab 3,997/0, eslint 0 (removed my unused normalizeWs), i18n 1,532/1,532 ×2, prod build 7.9s — guided chunk stays lazy (now 6.0 MB / 1.43 MB gzip; main index unchanged 343 kB). Skill accreted (new-language-A1 block + ru/ja per-language notes). ALL UNCOMMITTED — owner commit call pending; next frontier step = ru/ja A2 (Russian needs the voice-gender plan first).

## 2026-07-15 — A2 Phase 4 Polish P10 authoring batch landed: 10 lessons, static gate PASS | Codex
- Appended `GUIDED_TODAY_PATH_POLISH_A2_TEN_METADATA`, 10 handwritten `makePolishA2CompactLesson` inputs, and `POLISH_A2_PRACTICAL_10_LESSONS` after P9 in `frontend/src/data/guided/polishA2.ts`; no existing lesson content was changed and no commit was made.
- P10 is informal `ty` throughout for Wojech's male voice; consolidation arc covers origin, work, motivation, `Jestem tu od dwóch tygodni.`, family, hobbies, routine, progress, one masculine `nauczyłem się` callback (term accepts `nauczyłam się`), the single perfective-future `wrócę`, and a warm station goodbye. New trophies: `mieszkam`, `biurze`, `mówić`, `tygodni`, `rodzina`, `gotować`, `pracuję`, `prawie`, `wrócę`, `ważne`.
- Static verification loop passed: 10 lessons; core/chunk and recall concatenation exact; 3–6 trimmed chunks; 5–7 terms; exactly four fallback choices and three non-gendered speech tokens per lesson; identical Polish prompt quotes in German/English captions; no formal-register leaks, banned conditional/`ponieważ`/`będę`+participle forms, trophy collisions, A1 trophy reuse, or mojibake. Per the batch prompt, no `tsx` command or test suite was run.
- Files touched: `frontend/src/data/guided/polishA2.ts`, `memory/LOG.md`, `memory/STATE.md`. Next: Fable runs the language-filtered validator and reads P10 in context before the adversarial-review/integration stages.

## 2026-07-15 — A2 Phase 4 COMPLETE: pl/id/ceb A2 (300 lessons), TTS live 1,476 clips / 0 failed, committed `efdbfe82` | via codex + sonnet agents, Fable-arbitered
- **The A2 tier now covers ALL TEN guided languages.** All 12 draft batches (4 per language) gated green essentially first-pass — the only mechanical failure across 300 lessons was one bare-"Ini" chunk (id P3), hand-fixed. Suites after integration + fixes: typecheck, validator 45,260/0, data 9,617/0 (+120 Phase-4 asserts), matrix 6,173/0 (A2_LANGUAGES extended to all 10 incl. de/en/ko backfill — phase 3 never added them), eslint 0 new, i18n 1,530/1,530, prod build ✓.
- **Adversarial reviews: 108 findings (pl 44, id 32, ceb 32), Fable arbitered 104 applied / 4 rejected.** All 4 rejections = the known interlocutor-whitelist reviewer failure mode (caption lines aren't bound by learner tense/aspect whitelists; „Jak minęła noc?", „Nakatilaw na ka og lechon?" etc. are natural staff speech). Top accepted classes: Polish leak in a .de caption (Sprzedawca), banned constructions smuggled into trophy examples (padał/podano/oświetlony/ustalony — participial/impersonal past), id `tersambung` B1 ter- nuance as trophy → `pakai` (+ slug rename pre-freeze), ceb calques (Natulog og dili maayo → Wala ko katulog og maayo; mahimo → idiomatic kalugar), two authoring-rule LEAKS into learner-visible German („lizenziert/lizenzieren", „A2-Moment"), pedagogy re-blanks (recall targets moved onto the lesson's new element). Fixes applied via 3 surgical Codex prompts with exact final wording (14 corePhrase rebuilds specified field-by-field); Fable verified every rebuild string-by-string + hand-fixed 3 remnants the fix lists missed (a distractor and two pedagogicalGoals still carrying old wording).
- **TTS live on owner approval (AskUserQuestion, ~31.4k chars forecast = dry-run to the char):** 30 profiles seeded (`{lang}_a2_bright_p{n}_multiv2_v1`), commit run pl 496 / id 498 / ceb 482 = **1,476 generated / 0 failed**, idempotency re-run 0 missing, verify_guided_tts_batch PASSED (13/13 audio spot-checks incl. all three new languages). **ALL pl/id/ceb A2 ids TTS-FROZEN** (module headers updated). Polish per-path voice gender (Marta/Wojech/Maria/Rysard rotation) confirmed by the seeder dry-run to match the gendered-past table the content was written against.
- Committed `efdbfe82` scoped to 7 files (3 new modules, guidedLessons.ts, validator, data suite, matrix suite); WordTide.tsx (other workstream) untouched. Pushed on owner call.
- Ops notes: companion status JSON nests under `.job` (top-level `.status` parse reads 'unknown' forever); PS5.1 Select-String mojibake scare re-confirmed display-only (Node byte-check = 0 hits); seeder/batch `--languages` takes a comma-list, not repeated args.
- Accepted debt: some .de captions close quotes with ” instead of „…“-style “ (validator-accepted, cosmetic); id P2 „Saya pilih X karena…" ×7 + P4/P8/P10 kamu-tag-question tic and pl P9 czy-można formula repetition kept (reviewers didn't flag; churn > value); 2 slugs still carry pre-fix words (dili-mahimo-karon, nindot-nga-kaonon) — ids frozen now, never rename.

## 2026-07-15 — A2 Phase 4 (pl/id/ceb) IN PROGRESS: stages 0–3 done, batch 1 (P1–P3 ×3 langs) gated green, batch 2 drafting | via codex + sonnet agents, Fable-arbitered
- **Stage 0 via 3 parallel sonnet Explore agents + direct reads:** pl/id/ceb A1 all carry BOTH .de+.en base fields, German pedagogicalGoal, baseLanguage 'German'; A1 titles German-style („Polnisch A1 Praxis 1") — A2 metadata follows each language's own A1 (unlike Korean's English titles). Thresholds match A1 STT reality: pl/id 0.65, ceb 0.55 (+maxRecordingSeconds 12 for A2). Rosters: pl Maria/Rysard/Marta/Wojech, id Gavrila only, ceb Mayumi only. **Polish voice-gender math (rotation continues past A1: A2 Pn = names[(10+n-1)%4]): P1 Marta f, P2 Wojech m, P3 Maria f, P4 Rysard m, P5 Marta f, P6 Wojech m, P7 Maria f, P8 Rysard m, P9 Marta f, P10 Wojech m** — P3/P9 corePhrases use -łam, P10's callback -łem. Polish A1 verified to contain ZERO past/gendered forms — A2 P3 introduces gender for the first time.
- **§5.3 hard case resolved by design:** gendered corePhrase forms match the path's TTS voice gender; gendered terms teach the pair via new optional `alsoAccept` (scaffold builders fold it into acceptedAnswers); recall NEVER blanks gendered forms, speakRequired never pins them (validator-enforced via new bannedRecallAnswerPatterns/bannedRequiredTokenPatterns config fields); conditionals (all -by- forms) and będę+participle BANNED outright (gendered) — future = perfective non-past; ponieważ banned (bo only). polishFold helper because NFD accent-stripping misses ł (no combining decomposition) — validator gained recallVariant 'polish'.
- **Scaffolds** polishA2/indonesianA2/cebuanoA2.ts (koreanA2 pattern + alsoAccept; id/ceb helpers add hyphen-fused answer variants); **specs** tmp\A2_{POLISH,INDONESIAN,CEBUANO}_P1_P10_SPEC.md (id: atemporal base-tense licensing rule — every past/future base needs an explicit target marker; Malay + Jakarta-slang + telah bans; ceb: aspect licensing ni-/naka-+na / wala pa / mo-/mag-+time-word, Tagalog bans po/opo/pakisuyo/kayo/hindi, tungod-kay ban, friendly particles uy/lagi/bitaw only in friend paths ≤1/lesson, locale hygiene = batch-rejection material); **validator configs ×3** with per-config bareFunctionWords (shared set frozen so green languages can't regress). All 12 batch prompts + 3 adversarial review prompts pre-staged in tmp\.
- **Batch 1 (P1–P3 ×3) drafted in parallel ~25min each, gate:** pl 1,572/0 and ceb 1,512/0 first-pass clean; id 1,541/1 — one bare "Ini" chunk, Fable re-chunked by hand (Saya sudah | bayar, Bu. | Ini buktinya.) → 1,542/0; typecheck clean; full validator 29,840/0 preserved. Read-through: PL P1+P3 read — natural Polish, case-correct glosses, P3 gender discipline executed exactly (all -łam, alsoAccept pairs with both-gender glosses everywhere); CEB P3 sample — real German everywhere (A1's systemic failure absent), aspect licensing correct, niabot term carries alsoAccept Miabot/Naabot; ID P3 sample strong. Known cosmetic: some .de captions close quotes with ” or " instead of “ — normalize in the arbiter fix pass.
- **Batch 2 (P4–P6 ×3) launched** (pl=task-mrlqeb5a-rsnpg0, id=task-mrlqee2j-gomvsk, ceb=task-mrlqegsm-js61kz). Codex-ops: status JSON nests under `.job` (a watcher parsing top-level `.status` reads 'unknown' forever).

## 2026-07-15 — Korean A2 COMPLETE: 100 lessons, TTS live 474 clips / 0 failed, committed+PUSHED `482dc69b` | via codex, Fable-arbitered
- **The seventh A2 language shipped.** P10 landed clean (one 내년에→내년 trophy de-particle); integrated into guidedLessons.ts after Korean A1 (imports + spreads + path options, de/en precedent); data suite grew 42 Korean A2 asserts + both active-path allowlists (9,497/0); full validator 29,840/0 (Korean 5,140); typecheck, matrix 3,413/0, i18n 1,530/1,530, eslint 0 new, prod build ✓ guided chunk intact.
- **Adversarial review — 75 findings (39 HIGH / 36 MED), Fable arbitered 67 applied / 8 rejected / 1 inverted.** Dominant class (~20 findings, all accepted): interlocutor service speech needed customer-directed honorifics (필요하세요/가세요/어떠세요 — matches A1 caption evidence); genuine 에/에서 particle errors ×2; distractor sets that would ALSO have completed the sentence correctly ×9; two-turn repairs (P5·1 커피 말고 rebuild, P4·7 non-echo response, P9·4 같이 찾아 주세요, P9·6 거스름돈 subject); P8·3 늦게 잤어요 = "went to bed late" base-text fix; slug romanization fix (ingi-inneun-kape). REJECTED: Hangul-naming-the-trophy in whyThisWord ×2 (A1+A2 precedent), caption 취소됐어요 (learner whitelist doesn't bind interlocutors), farewell caption, friend-voice gendering (module-wide de voice), 2 stylistic-churn items, shared-아쉬워요 empathy. INVERTED: reviewer flagged the Sie-form chunk gloss in P7·8; the du-form baseText toward a stranger was the actual bug. Findings: tmp\A2_KO_REVIEW_FINDINGS_RAW.md.
- **Codex-ops resilience lesson (now in DECISIONS + skill):** BOTH plugin-managed review jobs were killed mid-run by companion-runtime restarts (stale logs, dead pids, registry amnesia). Recovery that worked: `codex exec resume <threadId> "finish and output findings"` as a direct child process of this session — immune to runtime restarts, reused the thread's 100-lesson reading. Draft batches survived as orphans because the codex process outlives the registry; reviews died because their processes were children of the restarted runtime.
- **TTS live on owner approval (AskUserQuestion):** 10 rotation profiles `korean_a2_bright_p{n}_multiv2_v1` (Yuna/Selly/Kanna/Emily/Sola/Jini — continues A1 rotation with no adjacent repeats), dry-run 474 calls / 4,222 chars matched forecast, commit run **474 generated / 0 failed** (deduped 32), idempotency re-run 506 ready / 0 missing / 0 calls, verify_guided_tts_batch PASSED (13/13 cross-language audio spot-checks). Note: verify_guided_tts_counts is A1-scoped (misleading for A2 — english still shows 453). **All Korean A2 ids TTS-FROZEN** (module header updated).
- Commit `482dc69b` scoped to 5 files (koreanA2.ts, guidedLessons.ts, validator, data suite, author-guided-tier SKILL.md); WordTide.tsx (other workstream) untouched. Pushed on owner call, Vercel deploying. Owner device-check: Korean A1 P10 → A2 P1 auto-flow, rotated voices per path, two-turn scenes.
- **Next frontier: A2 Phase 4 (pl/id/ceb)** — trophy corpora pre-extracted (tmp\A2_PHASE4_FORBIDDEN_TROPHIES.md); Polish gendered past (-łem/-łam) is the §5.3 hard case.

## 2026-07-15 — Korean A2 IN PROGRESS: pipeline captured as skill; P1–P9 drafted+read (90/100, uncommitted); P10 drafting | via codex, Fable-arbitered
- **Pipeline is now a permanent skill:** `orchestrator/.claude/skills/author-guided-tier/SKILL.md` (owner ask — no more re-stumbling). Stages 0–9 encode the whole Codex⇄Fable motion: A1-evidence gathering (base locales VARY per language — Korean carries de+en both; forbidden-trophy lists by module import only), Fable-owned scaffold+spec, validator config with per-language regex tripwires, 2–3-path Codex batches at xhigh with the accreted known_failure_modes block, per-batch validator gate + Fable read-through, adversarial review + arbitration (known reviewer failure modes listed), TTS freeze rules, scoped commit. Per-language notes section accretes scars; three NEW tripwires added this session: resolution-in-caption (see below), no proper-name/particle-attached trophies, no synonym fallbackChoices.
- **Korean A2 P1–P9 drafted, validated, read** (koreanA2.ts, 'draft' status, NOT yet integrated into guidedLessons.ts): scaffold matches koreanA1 conventions (baseLanguage German, de+en on every base field, German pedagogicalGoal, speak ko-KR threshold 0.65, romanized slugs); spec `tmp\A2_KOREAN_P1_P10_SPEC.md` — 해요체 in ALL 10 paths incl. friend paths (반말 out of scope; A1 taught only 요-form), past = -았/었어요 whitelist with 벌써/아직, future -(으)ㄹ 거예요, N 말고 N (P5), -(으)ㄹ까요/-(으)ㄹ게요, -(으)ㄴ 지 두 주 됐어요 as the P10 signature, numbers as Hangul words (Sino for 원, native+개/잔 counters), no digits. Validator gained the korean config (습니다/합니다 bans with 죄송/감사/알겠 lookbehinds, 십시오/겠 bans, minCoreTokens 5 — eojeol pack particles): 4,626 checks / 0 failed on P1–P9.
- **Batch quality curve:** B1 (P1–P3) validator-clean first try, 8 arbiter fixes by hand (마틴 proper-noun trophy→이름, particle-attached trophies 동네를/신발이/과일을 de-particled, 전부-synonym fallbackChoices 모두/전체/다 replaced — they'd also have been CORRECT answers, caption naturalness ×2, recall re-blank). B2 (P4–P6) ZERO fixes. B3 (P7–P9) 9 fixes — new systematic failure: **five P9 sceneCaptions carried the RESOLUTION of the learner's request ("다른 택시를 불러 드려요", "다른 방이 있어요") instead of the two-turn PROMPT** → all rewritten as prompts (무엇을 도와드릴까요? / 택시가 아직 안 왔어요? / 잘 주무셨어요?), tripwire added to skill + batch-4 prompt; also 고장 났어요 chunk split repaired, P8 news-captions naturalized (약속이 취소됐어요 / 가족이 서울에 와요).
- **Codex ops gotchas:** companion runtime restarts orphan the job registry ("No jobs recorded yet" / "No job found") while the codex process keeps running — verify on DISK (target-file exports + mtime quiescence + pid check), never trust the registry alone. B2/B3 both completed fine as orphans. Completion watches: poll job status AND a file marker (grep for the batch's final export), then confirm the job (not just the marker) before touching the file — B3's marker fired mid-verification-pass.
- **Phase 4 prep done:** pl/id/ceb forbidden-trophy corpora extracted to `tmp\A2_PHASE4_FORBIDDEN_TROPHIES.md` (100 each; Polish A1 has multi-word trophies occupying exact strings).
- Session verify-at-start was green (typecheck, 24,700/0, 9,457/0, 3,413/0); state delta vs NEXT_STEP: streak-pill removal now committed as `078f2a8f` atop `6d8b0080` (STATE had it uncommitted).
- **Next:** P10 gate + read → integrate into guidedLessons.ts after Korean A1 → full suites → Codex adversarial review (prompt ready: tmp\A2_KO_ADVERSARIAL_REVIEW_PROMPT.md) → arbiter fixes → TTS via AskUserQuestion (rotation continues Jini/Yuna/Selly/Kanna/Emily/Sola) → scoped commit. Then A2 Phase 4 (pl/id/ceb; Polish gendered past -łem/-łam = doc §5.3 hard case).

## 2026-07-14 — A2 Phase 3 COMPLETE: German + English A2, committed+pushed `6d8b0080` (also pushed Phase 2 `d8e5f531`)
- The A2 tier now covers all six core languages. 200 lessons (germanA2.ts / englishA2.ts, koreanA1-pattern modules) via the proven pipeline: per-language specs in tmp\ from A1 evidence → Codex drafts at xhigh (4 batches/language, both languages in parallel) → standing validator gate → Fable read every lesson → per-language Codex adversarial review → arbiter fixes with exact wording → TTS (owner-approved) → scoped commit.
- Evidence-driven conventions: German A2 is base-ENGLISH (.en fields only, matching German A1) with a Sie/du register split locked per path (Sie service P1-3/5-7/9, du friend P4/8/10 — from A1's counter-Sie/social-du evidence); Perfekt whitelist allows sein-verbs (German participles never agree — no Romance être-ban needed); war/hatte only Präteritum; weil verb-final + verb-second checked line-by-line. English A2 is base-GERMAN, American (en-US; A1 "center"/"to go" evidence), going-to future (standalone "will" banned, I'll as fixed chunk), present perfect contained to I've-already / I-haven't-yet chunks; its forbidden-trophy list was 271 words (A1 shipped 3 vibes) — most connector trophies taken, spec routed around it.
- Pipeline earned its keep again: batch-1 English had 22 two-chunk lessons (resplit fix); Codex habitually emitted leading-space chunk strings (validator tripwires added + idempotent module-wide trim script; final batch clean after hardened instruction); German P4L7 separable-prefix chunk fixed by hand. Draft validator extended to de/en (base-locale flip, capitalized German noun trophies, digraph recall fallback, register/variety/tense bans): 4,840 checks per language, 0 failed.
- Adversarial reviews: German 10 findings → 9 accepted / 1 adapted (kept comparative pedagogy in the P2 juice lesson; wärmen→aufwärmen needed an arbiter trophy swap the reviewer missed). English 15 findings → 12 accepted, 3 REJECTED (reviewer wanted du toward clerks/servers — wrong direction; German renders service speech as Sie, A1 precedent „Hallo, sprechen Sie Englisch?"). Fable sweeps folded in: module-wide Americanization of the German module's .en glosses; module-wide du→Sie service-speech sweep in the English module's .de glosses. Arbiter record: tmp\A2_DE_EN_FABLE_READTHROUGH.md.
- TTS live: 20 rotation profiles (`{lang}_a2_bright_p{n}_multiv2_v1`, continuing A1 rosters — de Laura/William/Enniah/Helmut, en Elisa/Peter/Serafina), 973 clips / 0 failed at exactly the forecast 19,502 chars (under the 12k/language cap); both verify scripts exit 0 (13/13 audio spot-checks). **All de/en A2 path/lesson/chunk ids are TTS-frozen.**
- Suites: typecheck ✓, data 9,457/0 (grew with de/en A2 assertions + active-path lists + one caption reword for the German-transliteration screen), matrix 3,413/0, i18n 1,530/1,530, eslint 0 errors; trophy-uniqueness shows 6 pre-existing A1-only failures (documented staleness incl. wistful variants — untouched by this work). Commit scoped to 5 files; wave-rider's tree changes (WordTide, dashboard pages, css, translations, DashboardStreak deletion) left unstaged.
- Codex-job gotcha for next time: this session's rescue-agent jobs register under the `orchestrator-*` workspace state dir (`~\.claude\plugins\data\codex-openai-codex\state\orchestrator-c8f76527e4ac5c41\jobs\`), not the repo-root slug — read review findings from the job JSON's `result.rawOutput`.

## 2026-07-14 — Streak pill removed from home screen, both skins (uncommitted)
- Owner call: a streak with no attached reward is noise ("what do I get for it?"). The prominent "1 DAY STREAK" pill under the language cluster is gone from DashboardPG (glassy) and Dashboard (classic); the compact flame chip in the mobile HomeAccountStrip (next to coins) stays as the only streak surface — desktop now shows no streak at all. If streaks ever get real mechanics (freezes/rewards), `useStudyStreak` still computes and the chip is the re-entry point.
- Deleted `components/dashboard/DashboardStreak.tsx`, its `dashboard-streak-row`/`dashboard-today-streak*` CSS blocks (incl. the mobile hide rule), and the orphaned `dashboard.streak.label` key ×3 locales (`dashboard.streak.aria` kept — chip uses it). Done directly, no subagents (~30 lines). typecheck ✓ eslint 0 ✓ i18n 1530/1530 ✓. Commit = owner call.

## 2026-07-13 (7) — A2 Phase 2 COMPLETE: French + Italian + Portuguese A2, 300 lessons, TTS live (main) | via codex, Fable-arbitered
- **All three Romance siblings shipped in one session** per FABLE_A2_LEARNING_PATH_DESIGN.md §6 Phase 2: per-language modules `frenchA2.ts` / `italianA2.ts` / `portugueseA2.ts` (scaffolds + compact builders mirroring spanishA2), 10 paths × 10 lessons each, integrated into GUIDED_LESSONS + path options directly after each language's A1 P10. Registers from A1 evidence: fr=vous, it=Lei, **pt=BRAZILIAN você** (pt-BR speak code; EP forms banned). New per-language gender-safety device: **avoir/avere-only past whitelists** for fr/it learner lines (être/essere participles agree with speaker); third-person être allowed („Le taxi n'est pas encore arrivé"). Specs in `tmp\A2_{FRENCH,ITALIAN,PORTUGUESE}_P1_P10_SPEC.md`.
- **Pipeline scars this round:** batch 1 fr+pt REJECTED for template collapse (identical scene captions, placeholder glosses, German fallback-choices in fr; fused chunks + malformed trophyWord nesting in pt; stray `+` diff markers) → scaffold restore + hardened v2 prompts at xhigh = high quality after. Codex "mojibake" alarms were twice PS5.1 ANSI-read artifacts (files were clean UTF-8 — measure with explicit UTF8 decode). Validator hardened along the way: `scripts/validate-guided-draft-a2-phase2.ts` (per-language dynamic imports, A2 sizing 6–16 tokens, template-collapse tripwires, lowercase-trophy/proper-noun ban, punctuation conventions, register/variety bans) — final 13,820/0.
- **Adversarial reviews (Codex xhigh, per language):** IT 13 findings → 12 applied, 1 rejected (institutional-voi „Ne avete?" is idiomatic); FR 24 → 23 applied (trophy-preserving arbiter rewordings; suivi rejected as non-whitelist); PT 12 → 9 applied, obrigado-removal rejected on A1-precedent (documented in tmp\A2_PT_REVIEW_FINDINGS.md). Trophy swaps where rewording removed the trophy: it serratura→porta, diverso→ancora, intervento→funziona; fr prenez→total; pt caneca→presente. Review-output retrieval gotcha: reviews run in a read-only Codex sandbox (findings files impossible) and >10-min jobs outlive the rescue-agent window — findings recovered from the session tasks-dir stdout files.
- **TTS live for all 30 paths (owner-approved):** 30 rotation profiles seeded (`{lang}_a2_bright_p{n}_multiv2_v1`, rotation continuing each A1 roster: fr Lilly/Adam/Stephyra/Guilamme, it Rosanna/Marco/Samanta, pt Raquel/Lair/Carla), Spanish-A2 idempotency gate 0-missing, dry-run 30,067 chars matched the computed scope, commit run **1,487 generated / 0 failed** (dedup saved 49 calls), PER_LANGUAGE_CHAR_CAP raised 8k→12k for this batch (owner approved totals). Verified: verify_guided_tts_batch + counts green, targeted probe **1,536/1,536 playback rows across all 30 paths, 0 missing URLs, 9/9 audio spot-checks audio/mpeg**. All fr/it/pt A2 ids now TTS-FROZEN (headers updated).
- **Suites:** data 9,377/0 (per-path A2 asserts + allowlist + selector order), trophy matrix generalized to 4 A2 languages 3,413/0, cloze 41/0, songs 216/0, typecheck clean, eslint 0, i18n 1531/1531 ×2. Trophy-uniqueness suite's 6 fails are the documented pre-existing A1/wistful staleness class (no A2 involvement). Zombie Korean Codex job from the morning session found still "running" in the registry → cancelled, koreanA1.ts verified untouched.

## 2026-07-13 (5) — Wave Rider v1.5: three.js visual rebuild + adversarial hardening (wave-rider, uncommitted) | via codex, Fable-reviewed
- **Owner device verdict on v1 rejected the visuals** ("scripted canvas, not a game"; red-dot rider; static waves) → full render-layer rebuild in **three.js 0.185** (new dep, lazy-chunked into SurfGame only, 148 kB gzip): `src/games/surf/scene/` (Phaser) deleted, `src/games/surf/renderer/` created (SurfRenderer/world/water/rider/buoys/wordPanel/effects/textures/palettes). Engine untouched (13/13). Phaser remains for slicer only. Codex implemented to a full visual spec; Fable fixed 3 type errors + React-hooks mount wiring (`runActive` so pause never remounts the renderer), then iterated look via headless screenshots.
- **Fable fix rounds from screenshots:** lighting rig (palette-tinted lights lerp toward white — raw dusk palettes render the sea black; exposure 1.3), water vertex math moved to WORLD z (was local — sea sagged 12 units at the rider and mismatched buoy floats), round wake sprites + dead-particle hiding (squares froze mid-air), foam distance falloff (far crests merged into a giant white blob), `fog:false` on sky sprites (sun beyond fog far plane rendered as flat purple disc), clouds unbent, sky glow band tightened to the real sea junction, camera follow 0.5 (lane 3 kept rider in frame), portrait camera (0,6.6,14) FOV 68, buoy spawn z −95, micro-swell octave for near facets, vermillion/gold cap on the rider.
- **Codex adversarial review → 5/5 findings fixed by Fable:** HIGH word-panel texture cache unbounded (~2.4 MB GPU each) → LRU cap 24 with dispose; MED GL-context-loss kept game time running → contextlost/restored listeners pause/resume (manual pause still wins); MED water 48k verts/frame → 64×72 segments (~27.6k); MED changed `?deck=` while playing left the old session running → remount via `key` on deck/due params (lint-approved pattern; setState-in-effect was rejected by react-hooks rule); LOW AudioContext never released → `SurfSfx.dispose()` on unmount (getContext recreates after close — replay/StrictMode-safe).
- **Verification (all green):** typecheck, eslint 0, engine 13/13, adapters 5/5, i18n 1531/1531, prod build, and headless mocked-Supabase playtests (puppeteer harness in session scratchpad; no real accounts — PII-safe): deck deep-link auto-start with deck-scoped query, static-audio attach observed (game fetched the concept wav on resolve), cruise + rush auto-commit, pause/resume via ESC+button, 3-lives session complete card, replay on a fresh renderer, bare picker with primary Play card, 120 fps desktop, 0 console errors, screenshots reviewed at 1440×900 + 390×844.
- Plan doc updated (status block + renderer architecture tree). Root-cause probe for the audio defect: owner's "Tiere · Level 1" Bisaya deck = 0/10 `tts_audio_url` but full `metadata.curriculum` concept ids (import-time attach never ran for old imports) — game-side read-only attach chosen over a DB backfill.

## 2026-07-13 (4) - Wave Rider deep-link and imported-deck audio repair (uncommitted)
- Scoped `/games/surf?deck=<id>` runs now load the requested deck, use its name as the label, and auto-start Cruise. Scoped due sessions use the due pool first and fall back to the full deck only below three usable cards; other queue values are ordinary deck sessions.
- Older curriculum imports with null word TTS now use existing static thematic playback in-memory at session start, grouped from curriculum metadata by category/level. No database writes; normal word TTS and browser speech fallback remain.
- Picker now prioritizes one localized Play action and `scripts/test-surf-adapters.ts` covers grouping, mappings, selective attachment, and preserved word TTS.
- Static verification: edited files re-read, import/type paths and test signatures inspected, `git diff --check` clean. Sandbox cannot spawn esbuild/tsx, so typecheck, eslint, i18n, and TSX scripts remain orchestrator gates. Pre-existing package.json/package-lock changes were not touched.

## 2026-07-13 (3) — Spanish A2 COMPLETE (P2–P10) + Korean A1 COMPLETE (P2–P10): 180 lessons authored, reviewed, integrated, TTS LIVE (main workstream)
- **Owner resumed mid-pause ("finish it off") and approved the TTS+push gate via AskUserQuestion (all three: seed, paid batch, push).** Working split held all session: Codex drafts to spec, Fable arbiters every batch, Sonnet applies mechanical patch lists.
- **Spanish A2 P5–P10 drafted in 3 Codex runs** (P5–P10 single run REJECTED for P7–P10: Codex invented `makeSpanishA2RapidLesson` stamping full-sentence translations onto every chunk/item — matchPairs would be unusable; P5–P6 kept, P7–P8 and P9–P10 re-run under anti-shortcut hard constraints, both accepted after Fable review). Review fixes: P5L7 factura→`hay un error en la cuenta` (restaurant register), P6L8 scene non-sequitur, P6L9 `Vengo por mi móvil. ¿Ya está reparado?`, 3 learner-facing meta-leaks ("geschlechtsneutral") reworded, `fuera/fuerte`-in-German ASCII-detector false positives reworded around.
- **Codex adversarial content review of all 90 Spanish lessons: 7 findings → 6 applied** (`Estos no, prefiero aquellos` repair idiom; P7 scene tense; `¿Qué habitación?`; Donnerstagnachmittag; P10 finale recast `Gracias por todo, he estado muy a gusto estas semanas.` adding the missing perfecto callback), **1 rejected** (P7L8 fountain→plaza re-alignment = stylistic churn). P3/P4/P8/P9 clean.
- **Korean A1 P2–P10 drafted in 3 Codex runs of 3 paths** (first 90-lesson attempt had been killed mid-run for degenerate shared glosses — the batch-cap memory). Fable fixes: item-densify follow-up run (3→4+ items ×30), dup item id, trophy 시간→오늘 (not in phrase), comma cascades on 3 two-clause phrases, `Hinweisschild` ASCII false positive, P6L1 German. Screens: 0 meta-leaks, 0 shared glosses, 100/100 unique pedagogical goals.
- **Both integrated into guidedLessons.ts** (imports/spreads/path options after each language's existing paths) + baselines extended (data test per-path asserts + allowlist + selector; trophy-fallback matrix now loops A2 P1–P10). **Draft validators committed as repo scripts** (`validate-guided-draft-spanish-a2.ts` 2802/2802, `validate-guided-draft-korean-a1.ts` 2712/2712, both with post-integration corpus-exclusion guards).
- **Checks at close:** typecheck ✓ eslint ✓ i18n full ✓ guided data 9287/0 ✓ matrix 2033/0 ✓ directory/segments/cloze/vibes/i18n/tts-inventory/cross-vibe/songs all green. Remaining fails are pre-existing staleness only: trophy-word-uniqueness 6 (A1 repeats incl. wistful), a1p1-cafe-wiring 10 (pins pre-fix-batch English titles), audio-playback 1 (English wistful chunk ids). `npm run build` fails only on wave-rider's in-progress `games/surf/ocean.ts` (not ours; their file is uncommitted so deploys are unaffected).
- **TTS LIVE (owner-approved):** rotation seeded (spanish a2 p1–p10: Veronica/El Farao/Lia/David rotation, P1 frozen; korean a1 p1–p10: Jini P1 frozen + Yuna/Selly/Kanna/Emily/Sola). Batches: **spanish 456 generated / 0 failed / 7,530 chars** (P1's 60 skipped ready — rerun-safety held), **korean 390 / 0 failed / 2,123 chars** (P1's 50 skipped). Verify scripts PASSED (13/13 spot-checks, 0 failed assets, 0 missing urls) + targeted probe: **918 playback rows across all 18 new paths, 6/6 A2/Korean audio HEADs 200 audio/mpeg**. Total spend ~9.7k chars (~4% of credits).
- **Ops scars this session** (saved to auto-memory): PS5.1 UTF-8 round-trip near-disaster on spanishA2.ts (recovered via git + context reconstruction; never touch repo text files with PS file APIs again); Codex content batches cap at 2–3 paths with anti-shortcut constraints; companion 10-min foreground timeout → run detached.

## 2026-07-13 (2) — Wave Rider game built end-to-end in one session (wave-rider workstream, El Taxico-inspired)
- **New workstream** (`protocol/workstreams/wave-rider/`): owner brief = El Taxico-style lane word game with a Lingwave twist, minimal features, bug-free, mobile-first, autonomous build. Concept chosen: **Wave Rider** (`id 'surf'`, route `/games/surf`) — surf a pseudo-3D ocean toward a domed horizon, 3 lanes, base-language prompt, steer into the buoy with the correct target term. Design doc `docs/Product/FABLE_WAVE_RIDER_PLAN.md`.
- **Excavated the deleted runner** (`e3fce8f0` autopsy) before building: it was the same concept, killed by fixable flaws (two copy-pasted 800-line scenes, per-frame Graphics redraws, 2–3MB bundled PNGs, hour-long-timer easy mode). All avoided by design: ONE scene, pure seeded engine, cached canvas textures, tiny programmatic art.
- **Built via Codex packets (A engine / B scene / C shell+data) on one resumed thread, Fable reviewed everything and ran all checks locally.** Fable fix rounds: test-script stale-stats capture bug; ocean palette crossfade was a hard cut (new visuals now fade in); viewport-relative sign scaling (unscaled 520px signs filled a phone screen); 0.18 horizon lane spread (signs no longer stack at the vanishing point); ESC pauses instead of exiting mid-run; HUD Pause/Mute moved bottom-right (collided with prompt bar); adversarial-review fixes (visibility-resume no longer overrides manual pause via `manualPaused`, ocean retiring-visuals leak on mid-transition teardown, degenerate config validation).
- **Structure:** `src/games/surf/` — engine/ (pure TS: types frozen by Fable, mulberry32 rng, sequencer with distractor+lane-streak rules, sessionEngine), scene/ (SurfScene, ocean w/ palette crossfade, perspective, signTexture cache, SurfSfx), components/ (SurfPicker/SurfHUD/SurfSessionComplete), adapters/ (deck+pack). Word sources: user deck / due (`?queue=due&lang=` auto-start, mirrors slicer; records `recall_attempts` `study_mode='surf'` — feeds SRS deliberately, only 'runner' is excluded) and static category packs (no recording; static thematic audio attached async, browser-speech fallback). 33 i18n keys ×3 locales. Registry+route exposed; tile icon rendered from the game's own canvas art via headless Chrome.
- **Verification:** engine tests 13/13 (`scripts/test-surf-engine.ts`), typecheck ✓, eslint ✓, check:i18n 1530/1530 de+fr ✓, prod build ✓, live headless-Chrome playtests (fixture flow, grading/lives/combo, completion+replay, pack flow es Animals with correction chips, pause/resume, keyboard+touch, 0 console errors). All UNCOMMITTED (owner commit call).
- **Blocked owner-side:** ElevenLabs key lacks `sound_generation` permission → 7 SFX clips not generated (`scripts/generate-surf-sfx.ts` ready, idempotent; game verified silent-safe). Owner: enable the permission, run the script, re-verify sound.
- **Gotchas learned:** codex-companion `--resume` fails in ~1s if the rescue subagent launches from `orchestrator/` instead of repo root (job registers in the wrong workspace registry); concurrent Codex agent's mid-write states break `tsc -b` transiently (koreanA1.ts/guidedLessons.ts) — check mtimes, retry when quiet.

## 2026-07-13 (1) — Pilot pushed; Spanish A2 P2–P4 drafted (Codex) + Fable review round 1; paused on owner call
- **Owner calls (voice):** push everything (→ `743323b2` + pilot commit `a0b5d928` pushed, Vercel deploying — A1 P10 now auto-flows into live A2 P1); full Spanish A2 buildout approved (P2–P10), then Korean P2–P10 and next guided languages; working split set: **Codex drafts content, Fable is quality arbiter**, Opus/Sonnet for TTS legwork; mid-session "hold off — review batch 1, push, pause".
- **Authoring spec written:** `D:\CODING\ResonanceTEST\tmp\A2_SPANISH_P2_P10_SPEC.md` — per-path grammar contracts (past whitelist, gender-safe production rules), 10-lesson arcs for P2–P10, 107-word forbidden-trophy list (extracted by module import; found 3 pre-existing A1 trophy dupes: hasta/aquí/habitación — not in scope), all DECISIONS 2026-07-12 review-policy items baked in. Spec now also carries the batch-1 conventions + the 30 new P2–P4 trophies for batch 2+.
- **Codex batch 1 (P2–P4, 30 lessons):** thread 019f5708-455d-7a81-9190-4a7af67af06e drafted all 3 paths into `spanishA2.ts` via a new `makeSpanishA2CompactLesson` builder (its own invention — accepted after hardening). The 10-min Bash timeout killed the companion process during Codex's self-check (job registry lost the job) but the file edit had already landed complete.
- **Fable review round 1 (all 30 lessons read):** (a) **engine bug fixed** — builder derived `speakTarget.requiredTokens` from lessonItems incl. multi-word phrases; `checkGuidedSpeechAnswer` compares whitespace-split transcript tokens, so multi-word tokens can NEVER match → speak step unpassable except by exact transcript. New required `speakRequired` field (3 single words/lesson, all 30 set by Fable) + `spanishA2SpeakTokens()` derives optionals from the phrase. (b) `spanishA2Answers()` now emits lowercase variants — sentence-initial recall answers (Llegué/Todavía/Entonces) rejected lowercase typing. (c) 21 chunk re-splits — bare article/preposition chunks ('en'|'la'|'caja.') merged to natural phrase boundaries (avoids junk TTS clips + noisy build chips). (d) Content: tarde→noche time alignment (P3L2), camisa comparison size→looks (P2L3), colon→two sentences (P4L6), trophy ocho→plaza (P4L4), term 'no'(=oder)→'a las ocho' (P4L9), distractor/chunk dupe fixed (P3L7). Sonnet applied the mechanical patch list (30 speakRequired + 21 chunk arrays + 5 content fixes); Fable-authored validator verified everything.
- **P2–P4 deliberately NOT integrated** into guidedLessons.ts (exports only): contract says adversarial review + owner spot-check + TTS come before exposure, and main auto-deploys. Integration + test-baseline extension happens when P2–P10 are complete and reviewed.
- **New repo tool:** `frontend/scripts/validate-guided-draft-spanish-a2.ts` — 936 mechanical checks on the un-integrated draft paths (concat/recall/chips/tokens/trophy-cross-corpus/ids/locale screens + P1-intact guards). 936/936 green; typecheck ✓ eslint ✓ guided data 9251/0 ✓ (P1 unchanged), pytest 54/54 at session start.
- Next: batches P5–P7 and P8–P10 (same Codex→Fable motion), then integration + baselines, adversarial content review, owner spot-check, TTS batch (AskUserQuestion), then Korean A1 P2–P10.

## 2026-07-12 (4) — Owner approved all A2 decisions → Spanish A2 pilot AUTHORED, reviewed, and with LIVE TTS in one session
- **Owner decisions (voice):** §9 all answered — themes approved CONDITIONAL on institute grounding ("researched, not vibes"), pilot = Spanish, bright-only, A2 directory badge yes, Korean A2 waits, TTS per phase after sign-off. Plus: commit call for the previous batch ("number nine, yeah it's fine" → committed `743323b2`; PUSH was classifier-blocked — still local), Codex delegation standing, and explicit "build it out as much as you can including the TTS".
- **Grounding pass (web-verified):** doc gained §2.1 — per-path mapping to CEFR-CV 2020 A2 descriptors, PCIC A1–A2 (funciones/gramática confirm indefinido+perfecto, comparatives, ir-a future, polite refusal AT A2), Cambridge A2 Key topics, Goethe A2 Wortliste, Waystage 1990 — and §5 rule 8 (A2 vocabulary must be inventory-anchored; per-language source named). Honest caveats recorded: full complaint rhetoric is B1 → paths 5/9 stay inside fixed formulas / single repair exchanges.
- **Pilot shipped: `src/data/guided/spanishA2.ts`** (10 lessons, `spanish-a2-practical-1`, "Back again" two-turn spine, usted register, present-only, object pronouns ONLY as fixed chunks, trophies collision-checked against all 100 ES A1 trophies by module import). Type widened `'A1'|'A2'` (both sites, runtime-inert as designed). **Landmine found+fixed:** `guidedPathLabels.ts` hardcoded "A1" in 3 helpers — A2 paths would have been labeled "A1 P1" everywhere; now level-aware (this IS the §9.4 badge: directory/hero/checkpoint show "A2 P1"). Integration exactly per doc §4: metadata inserted after Spanish A1 P10 (pickMissionPath auto-flows A1→A2), lessons spread into GUIDED_LESSONS. Tests: A2 rows added to trophy-fallback matrix; guided-today-data path allowlist+selector baselines extended.
- **Codex adversarial review (task-mrhj5vlq-ck8zu6, read-only):** 19 findings (2 HIGH / 7 MED / 10 LOW); mechanical invariants + A2 level fit fully clean. Both HIGHs were typeRecall blanks isolating object pronouns (paradigm-drill contract violation) → recast to content-word blanks (móvil, nombre). MEDs: L1 colon reorder, L6 rewritten to the natural check-in formula `Sí, tengo una reserva a nombre de García`, L8 `la conozco` → `Sí, claro` (conocer≠location answer), L9 scene `¿Cuántos tomates?`→`¿Cuánto quiere?` (count-vs-weight mismatch), L10 `un poco el barrio` word order, tienda/algo trophies not learner-produced → móvil/debo. **One Codex suggestion REJECTED:** trophy `nada` collides with a Spanish A1 trophy (cross-corpus uniqueness Codex wasn't tracking) → `debo` instead. All 19 addressed; suites re-green (data 9251/0, matrix 1619/0, cross-vibe 0 collisions).
- **TTS batch RAN (owner approved both prod writes via AskUserQuestion):** seeder+runner gained `--level a2 --paths N` (rotation continues past A1 → A2 P1 = Veronica, no adjacent-path voice repeat; guardrail pattern level-aware). Profile `spanish_a2_bright_p1_multiv2_v1` seeded; batch: **55 clips / 909 chars / 0 failed** (run 7965e053), 60 playback rows across all 10 lessons, verify scripts PASSED, 5/5 targeted A2 HEAD spot-checks 200 audio/mpeg. Module header now carries the id-freeze warning.
- **Checks at close:** typecheck ✓, 9 guided suites ✓, i18n 1497/1497 ✓, eslint 0 ✓, prod build ✓ (9.6s), pytest guided_tts 54/54 ✓, overview suite unchanged at its 6 pre-existing stale fails.
- Working tree DIRTY on purpose: pilot batch (spanishA2.ts, guidedLessons.ts, guidedPathLabels.ts, 2 test scripts, 2 batch scripts, design doc, memory/protocol) — owner commit call. `743323b2` also still unpushed.

## 2026-07-12 (3) — A2 learning-path design doc written (owner review) + Codex adversarial review of the batch commits: 2 HIGH rerun-safety bugs found and fixed
- **A2 design doc:** `docs/Product/FABLE_A2_LEARNING_PATH_DESIGN.md` — 10-path A2 spine ("the Regular" arc; extends V0's 3 A2 sketches to 10 themes with a per-path grammar ladder), same 6-step engine ZERO engine changes (verified: step order is a hardcoded constant, nothing reads `lesson.steps`/`modeSet`; `level` field is runtime-inert → widening to `'A1'|'A2'` is risk-free), per-language modules on the koreanA1.ts pattern, A2 paths appended after A1 in `getGuidedTodayPathOptions` → `pickMissionPath` auto-advances (also fixes today's finished-A1 dead end), authoring contract distilled from all 193 A1 review findings (tense contract v2, locale hygiene, gender-safe production — critical for gendered past participles in pl/it/pt/fr/es), Spanish-pilot rollout (Polish dogfood alternative), TTS = rerun of existing tooling ~70–85k chars for 9 langs. 6 owner decisions listed in §9. Corpus fact verified by module import: **910 lessons** (9×100 + Korean P1) — an Explore agent's "English paths 2-10 are stubs" claim was a grep artifact, caught against the runtime-verified review + TTS row counts.
- **Codex adversarial review of `96b04391`+`27388103`+`dc89cc70`** (task-mrhghkr9-17ljxy, ~15min, xhigh): verdict "completed batch not disproven" — shipped work stands; findings were latent RERUN hazards. Fable verified each personally and fixed (Codex investigates, Fable fixes):
  - **HIGH storage-path overwrite:** `inventory.storage_path()` omitted `provider_voice_id` while `cache_key()` includes it → reseeding a profile key onto a different voice would overwrite old audio bytes that existing asset rows still reference. Fixed: voice-id segment in the path (new clips only; existing rows keep their stored paths; cache keys unchanged → idempotency intact).
  - **HIGH paid-clip demotion:** `generate.py` commit loop caught usage-upsert failures in the same except that marks assets failed → a ready, paid clip could be demoted and re-bought on rerun. Fixed: asset-failed handling ends at `upsert_ready_asset`; usage failures are counted separately (`usage_failures` in run notes/return, run status failed, rerun reattaches usage via the ready branch with zero spend).
  - **MED provider trust:** ElevenLabs 200 responses now validated (non-empty + audio/octet-stream content-type; retryable) — a proxy error page can no longer become a permanently-skipped "ready" asset. +2 provider tests.
  - **MED verify scripts were printers:** `verify_guided_tts_batch.py` / `verify_guided_tts_counts.py` now exit 1 on failed assets / missing URLs / BAD spot-checks / zero-row languages — green exit IS the certification.
  - **LOW guided-transcribe:** `in`-operator locale check accepted prototype keys (`toString`) → own-property check.
  - Checks: pytest guided_tts 54/54 (2 layout tests updated for the new path segment), api tsc ✓, eslint ✓. Prod dry-run sanity check deliberately NOT run (auto-mode classifier gate; owner-named runs only) — added to the runbook as the owner-side step before the next batch.
- **Accepted as trade-offs / deferred to A2 phase 0:** timeout-retry can double-spend a clip if ElevenLabs completed before the response died (cost ≈ one clip, availability wins); `db.py` unpaginated voice/profile reads + first-row profile upsert on duplicates; probe script 3,000-row cap (already stale for 3,744 assets — informational tool only); Korean lessons skip the semantic assertions in test-guided-today-data; batch-guardrail language-code canonicality. All recorded here deliberately, not silently.
- **Animations sidequest (owner mention, parked by agreement):** the emilkowalski/skills repo is already partially installed here (`emil-design-eng` skill), and the apple-design audit backlog covers motion work — owner will spawn a separate agent; it should start from `investigations/APPLE_DESIGN_SKILL_AUDIT_2026_07_10.md`.
- Working tree DIRTY on purpose: A2 design doc + 8 fix files + memory/protocol — owner commit call (Mode 2).

## 2026-07-12 (2) — Content fix batch (191 findings) + guided TTS batch EXECUTED: 3,744 clips across all 10 languages, verified end to end
- **Owner session brief (voice):** commit the language batch, close the gaps, bright-only TTS on quality model, mixed voices but ONE generation per text ("per ten lessons same voice, then bomb — different voice"), Codex executes / Fable orchestrates+reviews; A2 design next. Content-fix job explicitly pinned to gpt-5.6-sol high/xhigh (already the `~/.codex/config.toml` default — verified in the job log).
- **Commits pushed:** `96b04391` language batch + `b098cb0f` brain save (owner call), `27388103` content fixes + TTS pipeline, `dc89cc70` batch results + verify tooling.
- **Discovery that dissolved decision 1:** prod probe (`scripts/probe_guided_tts_state.py`) showed the guided TTS SQL was ALREADY applied — all 4 tables, playback view, `guided-tts` bucket live, plus the English flash-v2.5 canary assets (368) and 447 usages. The rescued-draft migration was never needed. it/pt/pl voices also already in `public.voices` (plan doc was stale on both).
- **Content fix batch:** Codex (task-mrgppwky-nlrswt, 1h10m) applied 191/193 findings — the 2 skips are the intentional target-equals-base locale fallbacks (contract narrowed, no authoring of 5,115 en + 1,846 de fields). Codex deviations all reviewed and accepted (P5L1 `Sorry, I'm late.`, PL impersonal constructions, PT `Estou aqui. Obrigado.`). **Round 2 was Fable personally** (Codex hit its OpenAI usage cap): the review's own suggestions had collapsed 10 English wistful/sharp variants into bright-duplicates (hedge-strip Levenshtein 1.000) → rewrote each in-voice; trophy collisions/dupes fixed (en P5 double-`tomorrow`, P6 allergy/better, P9 double-`set`, ceb P10 salamat/pwede → nagpasalamat/okey, pt nome/aqui/próxima → qual/estou/até); 5 low-value typeRecall answers restructured; PT diacritic acceptedAnswers verified present; ID P8 checkout: kept the loanword phrase (linguistically right), trophy → `sekarang`, `keluar` item restored, leakage test exempts exactly that lesson. Stale test baselines regenerated programmatically (EN arc titles now German, PR6 phrase lists, trophy sequences, tts-inventory char counts 236→202). All green: 9 suites (data 9250/9250, cross-vibe 0 hard fails 0 collisions), typecheck, i18n 1497/1497, eslint, build, pytest 52/52.
- **TTS pipeline changes:** `generate.py --language` flag (target_language_code was stuck at en-US); provider omits `language_code` for models without enforcement (multilingual v2 400s it — new test); `db.load_existing_assets_by_cache_key` batched (PostgREST URL-length 400 at >~100 keys); `scripts/seed_guided_bright_rotation.py` (91 per-path bright profiles, `{lang}_a1_bright_p{n}_multiv2_v1`, specificity 12 beats vibe-level 8 and static 0); `scripts/run_guided_bright_batch.py` = dry-run + guardrails + commit in ONE process (profile-pattern check, 8k/lang + 60k/batch caps) instead of circular EXPECTED_SCOPES entries.
- **Batch results (eleven_multilingual_v2, bright, corePhrase+chunks+trophyWord):** en 405 / es 417 / it 405 / fr 405 / pt 410 / de 424 / ceb 408+1retry / id 408 / pl 416 / ko 45 = **3,744 clips, 47,024 chars** (~16% of owner's monthly credits). One transient ElevenLabs read-timeout (ceb P1 core), idempotent re-run regenerated just it. **Verified:** 0 failed assets, 3,970 bright playback rows (424–453/lang, ko 50), 0 missing URLs, 13/13 audio HEAD spot-checks (200/audio-mpeg). Guided audio now plays real voices on lingwave.ai for every A1 language once deployed.
- **Permission-gate lesson:** the auto-mode classifier blocks prod Supabase writes unless the user named them → one AskUserQuestion naming tables/spend cleared both seeding and generation.
- **Known leftovers:** English wistful/sharp clips remain old flash-v2.5 single-voice audio; texts changed by the fix batch have stale audio in those vibes only (bright fully regenerated; vibes are discontinuation-bound). Owner has NOT yet made the Japanese call. A2 design is the owner-directed next step.
- **Owner direction (voice transcript):** unify language offering (home must add any offered language), build out A1 guided for more languages (Korean wanted, Japanese maybe), only bright vibe for new content (vibes being discontinued as content-variants), TTS for all A1 paths after languages land (Codex as workhorse), A2 design later. ≥20 languages long-term target.
- **Wizard unification SHIPPED (uncommitted):** Polish registered in `lib/languages.ts` (pl/Polski/#e11d48, wizard-only — NOT isSpeak, api has no 'pl') + isWizard flipped on Portuguese/Dutch/Hindi/Arabic → wizard = 15 languages; `langName.Polish`+`.pl` ×3 locales (i18n 1497/1497); PL flag in FlagIcon; pt/nl into supported-Latin font set (pl deliberately on fallback stack like Cyrillic); Lens prompt: Hindi transliteration + pt/nl articles. Verified: typecheck ✓ api tsc ✓ eslint ✓ tsx round-trips 5/5 ✓. Polish-active Speak degrades to language grid (Speak.tsx:674 verified). Report: `investigations/LANGUAGE_SURFACE_UNIFICATION_2026_07_12.md`.
- **Guided speak-step transcription was BROKEN for 8/9 languages (found + FIXED, uncommitted):** `api/guided-transcribe.ts` 400'd every non-English locale AND hardcoded `language='en'` to Whisper, while `useGuidedSpeechRecognition.ts:112` always sends the lesson locale. Now: `GUIDED_LOCALE_TO_WHISPER_LANGUAGE` accepts all guided locales incl. ko-KR, proper Whisper hints, ceb-PH → auto-detect (Cebuano not Whisper-supported). api tsc ✓, paid-api suite ✓. English-only testing is why this went unnoticed.
- **Codex CLI WORKS now** (v0.144.0 + gpt-5.6-sol smoke-tested) — the 2026-07-10 "too old / rescue unavailable" note is obsolete. Companion runtime used for 2 long tasks this session; status/result via `codex-companion.mjs status|result <task-id>` from repo root.
- **Guided A1 content quality review (Codex, 17min):** all 9 languages = 10 paths × 10 lessons (900 lessons, 1,100 variants, ALL contentStatus 'draft'); wistful/sharp variants exist ONLY for English → owner's "vibes are cosmetic/EN-only" belief confirmed for other languages. 193 findings: 31 HIGH / 131 MED / 31 LOW. Systemic: EN path/lesson metadata `.de` fields hold English (~310), Cebuano+Indonesian `sceneCaption.de` hold target-language text (200 lessons), placeholder captions `.de` English (~180). Real linguistic HIGHs: pt "plataforma do ônibus"→"ponto de ônibus", es/id unidiomatic hotel checkout, it "Sono tardi" (also fails typeRecall compose), pl missing reflexive "się". Report: `investigations/GUIDED_A1_CONTENT_QUALITY_REVIEW_2026_07_12.md` (53KB). Fix batch = next session's prime candidate.
- **Guided TTS batch plan written** (`docs/Product/FABLE_GUIDED_TTS_BATCH_PLAN.md`): frontend playback (`lib/guidedAudio.ts` → `guided_tts_playback` view) and Python ElevenLabs pipeline (`orchestrator/src/services/guided_tts/`, dry-run default) already EXIST; gaps: guided SQL only in rescued-drafts (owner-gated), Python/SQL surface-enum drift (chunks/speak vs chunk/speakTarget), `EXPECTED_SCOPES` en-only, no voice picks for it/pt/pl. ~5k chars/lang/voice → ~90k chars for 9 langs × 2 voices. 4 owner decisions listed in the plan.
- **Korean guided pilot (A1 Practical 1, 10 lessons) SHIPPED, uncommitted** (Codex authored, Fable reviewed every lesson + verified): NEW per-language module pattern `src/data/guided/koreanA1.ts` (648 lines, type-only imports; the 65k-line guidedLessons.ts stops growing) + surgical integration (union, ko-KR, speak-locale map, path options, todayLanguage). Natural 해요체, Korea-native scenes (편의점/삼각김밥/Hongdae), bright-only. All checks green incl. 9 guided suites (data 9250/9250) + prod build (guided chunk 2,874kB, no dashboard leak).
- **Korean-blocker speech fix (Fable, during review):** `guidedSpeechCheck.ts` + `speechRecognition.ts` normalizers stripped all non-ASCII → Hangul transcripts became empty → speak step would always grade Korean 'incorrect'. Now Unicode-aware (`[^\p{L}\p{N}\s]/gu`); also stops splitting umlauts/diacritics into token fragments for existing languages. Speech-check suite ✓.
- Architecture doc updated with a dated delta block (15 registry entries, transcribe fix, tooling-on-main, articleDisplay gone, vibe reality).
- **Owner answers applied:** all 11 leftover worktrees removed (5× C-drive static-thematic — `C:\Users\micha\.config\superpowers\worktrees\` deleted entirely —, orchestrator-id-static-tts, ResonanceTEST-polish-polish, tmp/phase2a×2, tmp/voxtral×2) + 10 orphaned local branches deleted after per-branch origin-containment proof (incl. `static-thematic-ceb-animals-yumi-raw-v1`, contained in origin/static-thematic-id-...); stale copy `orchestrator-origin-main-check` (223MB) deleted; `ltx-worker\.venv` (4.9GB) deleted; `_archive\` (~1.9GB incl. the storage-cleanup git bundle — local ref `backup/storage-cleanup-workflow-4525de38` still exists) deleted AFTER the clean review verdict; `content\`+`curriculum\`+`brand\` media left in place per owner, never pushed.
- **`refactor` branch dropped deliberately (Fable decision, owner delegated):** its 2 "ahead of origin/refactor" commits `e816565a`+`c45dbd43` (April job_runner decomposition + cloud_engines/direct-dispatch) are ancestors of main — only the remote ref was stale. `git branch -D` after merged-to-HEAD proof. origin/refactor left as-is (no pushes).
- **SQL rescue before archive deletion:** 5 files existed ONLY in `_archive` → copied to `frontend/supabase/rescued-drafts/` + README. `20260517010000_guided_tts_v1.sql` kept on owner decision (raw material for future guided-path listen-to-phrases audio; plan-first, no implementation). The 4 April root drafts (study_mode, cost_tracking×3) were probably applied manually to Supabase (typed mode + costs dashboard live in prod) — kept as the only schema record instead of permanent delete.
- **Codex adversarial review of the 4 cleanup commits (task 10m29s, read-only):** checks 1/3/4/5 HOLD (zero deleted-module refs in tree/deploy config; Dockerfile.cloud runs only start_cloud.py; 114 moved doc paths unreferenced; guided-TTS tooling imports clean); check 6 env-blocked in Codex sandbox (tsbuildinfo EPERM — known limitation; both projects pass read-only, and Fable's local typecheck is clean). **1 LOW finding, fixed by Fable:** 3 stale `returnTo` fallbacks to the deleted `/games` hub (GameShell.tsx:16, SlicerGame.tsx:59, DeckPicker.tsx:44) → `/dashboard`. typecheck ✓ eslint 0 ✓. UNCOMMITTED (with rescued-drafts/) — owner commit call.
- Noted, not acted on: `D:\CODING\ResonanceTEST\tmp\` still holds ~35 loose scratch files (logs/patches/screenshots, worktrees now gone) — not on the decide list.

## 2026-07-11 (4) — Repo-wide cleanup pass EXECUTED (5 analysis agents + 2 executors, Fable verifying): 4 commits pushed, ~900MB archived, DAW deleted, worktree jungle cleared
- **Full report:** `investigations/CLEANUP_PASS_2026_07_11_REPORT.md`. Policy archive-first: un-versioned → `D:\CODING\ResonanceTEST\_archive\` (manifests); tracked → `git rm` in revertable commits.
- **Commits (all pushed):** `c16e83d7` frontend dead code (26 zero-importer files ~1,850 LOC + orphaned /games hub + wordLibrary.* i18n family); `be208ef6` DAW chain deleted (app.py, routers/, state/csv_import/presets/voices, start*.bat, song_engine/language.py) + 64 setc one-off scripts/tests archived, seed_guided_voice_profiles.py now tracked; `8a1c20a8` docs restructure (Infrastructure tracked — was memory-cited but untracked!, historical families → docs/archive/, README refresh); `9067e188` pricing contract test repointed at live wizard pages + investigation/ baselines.
- **Verified by Fable:** typecheck ✓ i18n 1495/1495 ✓ build 3.97s ✓ pytest collect 689/0 errors ✓ 140 live backend tests ✓. **14 pre-existing stale frontend-contract test failures documented** (verified failing at pre-cleanup commit too — music_lyrics 6, profile_base_language 2, gpt_premium 2, admin_gpt_image 1, duration_fields 1, phase1b_retry 1, concept_lyric 1+env) — backlogged.
- **Worktrees/branches:** 19 clean worktrees removed + 37 merged branches deleted (git -d self-check). Crown jewels rescued FIRST: push-speak-scroll-fix + french-a1-p1-final/pilot pushed; diverged codex/unify → origin `archive/codex-unify-generation-loaders-local-20260711`; backup/storage-cleanup (workflow-scope push block) → verified bundle in _archive/git-bundles + local ref kept. Dirty-worktree scratch salvaged to _archive/worktree-salvage. **Memory correction: static-TTS multi-language tooling (61f3c245) IS on origin** (ancestor of origin/static-thematic-id-...) — the "unpushed worktree branches" memory was stale.
- **Root:** ~895MB archived (90 loose handoffs/investigations, 25 dirs incl. engines/, migration/, test outputs); LINGWAVE_LEARNING_STATE.md rescued to investigations/. content/ (13.2GB) + curriculum/ (15.1GB) + brand/ left in place — owner call.
- **Owner decide-list (also in NEXT_STEP):** 11 leftover worktrees need explicit naming (permission classifier), orchestrator-origin-main-check stale copy (223MB), content/curriculum/brand fate, _archive deletion after review, ltx-worker/.venv (4.9GB), refactor branch (2 unreconciled commits), root supabase/ + guided_tts_v1 sql drafts (verify never applied).

## 2026-07-11 (3) — Nav-restructure batch COMMITTED+PUSHED `a1068fc9` on owner call; docs/dead-code cleanup program kicked off
- Owner gave the commit call → 24-file nav batch committed as `a1068fc9` and pushed with the brain save. Old untracked `frontend/scripts/test-daily-habits.ts` (mtime 2026-06-23) noticed during staging — pre-existing clutter, deliberately left out; feeds the cleanup task.
- Owner kicked off the next big task: repo-wide documentation + dead-code cleanup ahead of TestFlight (Codex analyzes, Opus/Sonnet executors, Fable decides). Hard constraint: the video pipeline stays — it's unexposed but shares Supabase table/JobRunner infrastructure the product needs. Temporary breakage acceptable (no users), but end state must work.
