# Current State
> 2026-07-28 (later) — **TIER BATCH REVIEW-CLEAN AND COMMIT-READY (uncommitted, Robert's call).** Codex xhigh returned 2 blockers / 9 highs / 6 med / 3 low, "not safe to commit" — arbitration: 13 accepted/adapted + 4 rejected with rationale (record: LOG 2026-07-28 later). Both blockers fixed (tier refunds claw back plan_credits via new `record_stripe_plan_refund`; checkout 409s active subscribers — no double billing), all accepted highs fixed (stale-period grant guard, monotonic checkout status, music-retry premium gate, Speak debits atomically post-STT with a one-turn-crossing ceiling + 15s flat for audio-less turns, Live refunds failed mints + client auto-ends at 10 min since the token TTL doesn't kill an open socket). Gates re-run ALL GREEN (typecheck · api tsc · i18n 1627/1627 · eslint 0 · stripe-shell · visual-scan 24/24 · capacitor · oauth 29/29 · first-light 27/27 · build · diff --check). Earlier same-day build entry follows.
> 2026-07-28 — **TIER SYSTEM BUILT AND GATE-GREEN (uncommitted; Codex xhigh review in flight). ⚠️ superseded → review arbitrated, see above.** Robert locked prices (Standard $7.99/mo·$2.99/wk, Premium $14.99/mo·$4.99/wk — weekly raised for Stripe fixed fees) and gave the GO; the full subscription layer now exists: migration `20260728090000` (plan_credits expiring balance spent-first, usage_counters + atomic consume RPC, live_sessions, plan-aware Stripe RPCs with reset-not-add grants, premium-gated music RPCs), planCatalog/entitlements API layer (4 price env vars, `STRIPE_BILLING_ENABLED` switch, usage reads fail open / debits fail closed), Speak metered in STT seconds (free 60s lifetime trial), Grok Live premium-only at 10 min per token mint with server-side session+cost logging ($0.05/min verified), Lens scans debited, `/plans` page both skins (native = informational, contract-asserted), totalCredits() everywhere, 31 i18n keys ×3 (1625/1625). ALL GATES GREEN (typecheck · api tsc · i18n · eslint 0 · stripe-shell · visual-scan 24/24 · capacitor · oauth 29/29 · first-light 27/27 · build · WordTide `d4bcb092`). NEXT: arbitrate the Codex review → Robert's commit call → his Stripe setup (4 prices + env vars + migration, migration ideally before deploy). Full detail: LOG 2026-07-28. Earlier headline follows.
> 2026-07-27 (later) — **BETA COHERENCE BATCH + iOS IDENTITY COMMITTED `eedddf8f` AND PUSHED (Robert's call; both migrations applied by him first). PRICING MODEL NOW COST-VERIFIED; ROBERT'S PRICE CALL IS THE OPEN ITEM.** One scoped commit (49 files, +942/−260; WordTide.tsx hash-verified `d4bcb092` and left out) carrying both verified batches — they share package.json + the capacitor contract test and were gated together. Robert's pricing directives recorded (DECISIONS 2026-07-27): ~3x measured cost, two paid tiers + weekly option, no rollover, simplified generation UX, growth over margin. Two-agent cost research reconciled code vs live provider pricing: **async Speak ≈ $0.01–0.02/min** (Groq Whisper + Llama-3.3-70B + Voxtral/Gemini TTS — no xAI/ElevenLabs in the live loop); **Grok Live $0.05/min flat VERIFIED** (code's constant was right, but sessions still log no cost row); standard card $0.0045; **premium card $0.03 verified at 1K** (cost_logger's $0.05 is the 2K rate — overstates); music $0.06/2 tracks; Lens ≈ $0.0003/scan; **1 product credit ≈ $0.005–0.006 provider spend — the coin is already cost-true**. Proposal on the table: Standard $5.99/mo·$1.99/wk (100 credits, 60 async min, 100 Lens), Premium $14.99/mo·$4.99/wk (300 credits incl. songs, 240 async min, 60 Live min post-logging, 300 Lens), Free = 10 credits + 1 trial min + 3 scans; weekly grants = ¼ monthly. Implementation prerequisites regardless of numbers: Speak minute metering, Live cost logging, credit ledger + one entitlement policy (Codex plan §2), Stripe web-only products. Earlier same-day close entry follows.
> 2026-07-27 (close) — **BETA COHERENCE BATCH REVIEW-CLEAN AND COMMIT-READY (uncommitted, Robert's call). ⚠️ superseded → committed `eedddf8f`, see the entry above.** The Codex xhigh review returned 0 blockers / 3 high / 9 medium / 4 low and a "not safe to commit" verdict; all three highs were fixed same session and every finding arbitrated (record: LOG 2026-07-27 arbitration bullet). The highs, now closed: Today could freeze a stale language on cold load and bounced legacy ko/ru/ja/pl learners into English (resolution now spans all guided languages; the beta trim applies to the switcher only; one-shot adopt-canonical effect); `profiles.target_language` now actually hydrates LanguageProvider on a fresh device (stored → profile → newest deck); Generate's auto-seed no longer lets a legacy active language skip the picker and mint a new non-beta deck. Plus: Spanish `isBase` trim (my miss, caught by review), `cebuano→Bisaya` canonicalize alias, legacy values visible in Library/CategoryPicker selects, `cap:sync:ios` now guarded on Windows, storage writes try/caught, deep-link hash kept. Post-fix gates ALL GREEN (typecheck · eslint 0 · i18n 1597/1597 · capacitor + oauth-onboarding 29/29 + library contracts · first-light 27/27 · fixtures 17/17 · prod build · WordTide `d4bcb092` · diff --check). Earlier same-day details follow.
> 2026-07-27 — **BETA COHERENCE BATCH BUILT (uncommitted; Codex xhigh review was in flight at this point — superseded by the close entry above).** Robert ran the prod queries and answered every open product question in one pass; the batch implements the results. **Prod facts:** `base_language` default IS live (newest profile: 'English', 0 credits → onboarding was dead code, audit confirmed) · `enforcement_enabled` = **TRUE** (rate limits ON — the audit's "every limit is a no-op" is corrected: bad default, but armed in prod) · query 3 needs the corrected column `target_language_code` (in NEXT_STEP). **Built by Fable's hand, all gates green** (typecheck · eslint 0 · i18n 1597/1597 · capacitor contract · oauth-onboarding 29/29 · first-light 27/27 · fixtures 17/17 runnable · pixel-identity PASS · prod build · WordTide `d4bcb092` untouched): onboarding resurrected as native(en/de/fr)+target(8 tiles) gated on `profiles.target_language`+sticky local flag; **one canonical target language** via `lib/targetLanguage.ts` fan-out in LanguageProvider (owns the only Bisaya↔Cebuano bridge; Today/Library pickers are now full language switches; Decks no longer nulls at zero decks); **`BETA_TARGET_LANGUAGES`** = en/de/es/fr/it/pt/Bisaya/id filters every picker (resolvers untrimmed — legacy ko/ru/ja/pl decks keep working); classic skin retired (`CLASSIC_SKIN_RETIRED`, stored choices preserved); `/a` `/b` `/landing/*` unshipped; typed recall hidden for non-Latin (`isLatinScriptLanguage`); ko/ru/ja raw-key fix; profile fetch → `select('*')` (schema-drift-safe). **Two migrations AWAIT ROBERT** (apply in order, ideally before next deploy; frontend degrades gracefully if late): `20260727090000` target_language column + allowlist-trigger recreation (without it the column is un-writable) + signup credits 0→10; `20260727091000` drops the 4 ownerless storage read policies (kills anon enumeration, playback unaffected). **Tier design:** Codex (gpt-5.6-sol) proposal saved → `investigations/CODEX_TIER_DESIGN_2026_07_27.md` (Free/Standard/Premium, credits-as-creation-wallet + allowances + quotas-as-abuse-rails; Live off until cost logging) — input for the NEXT session's pricing conversation, nothing implemented. Its strongest new flags: wrong-sense Library senses amplified by the SRS loop (7 of 8 beta languages affected) and no audio parity matrix across the 8. Decision record: DECISIONS 2026-07-27. Known-stale tests verified failing on HEAD identically: dashboard-home-glass, speak-polish, category-picker's last assert, rebrand-sweep, hybrid-a/b. Earlier headline follows.
> 2026-07-26 — **iOS IDENTITY SETTLED; THE PRODUCT, NOT THE SHELL, IS WHAT BLOCKS THE BETA.** Robert confirmed he has a Mac mini and an active membership, and that App Store Connect holds **only Canta** — no `pro.resonanz.app` record, which disproves the 2026-07-25 "probably not a first upload" inference and with it the standing "keep the bundle id" recommendation. **Bundle id renamed to `ai.lingwave.app`** (reverse-DNS from lingwave.ai) across `capacitor.config.ts`, `project.pbxproj` ×2, `ios/App/App/capacitor.config.json`, the `Info.plist` CFBundleURLName, and both AASA copies as `ZL69STWQHV.ai.lingwave.app`; **`DEVELOPMENT_TEAM = ZL69STWQHV` set in both build configurations**, which clears B3, the last code-side archive blocker. The `resonance://` scheme was deliberately NOT renamed (load-bearing for OAuth, not permanent, invisible). Contract test extended to assert the bundle id in all four places, both team entries, and AASA/bundle agreement. Gates: contract PASS · typecheck · eslint 0 · i18n 1596/1596. **Then a 12-agent adversarial product audit (6 surveys, each re-refuted; 44 CONFIRMED / 19 downgraded / 1 upgraded / 0 wrong; 7 re-verified by hand) → `investigations/BETA_READINESS_PRODUCT_SCOPE_2026_07_26.md`.** Headline: **the app very likely never asks a new user anything at all** — `handle_new_user` omits `base_language`, the column defaults to `'English'`, and the only route to the wizard is `!profile.base_language?.trim()` at `App.tsx:109`, so `Onboarding.tsx` may be dead code; four surfaces then guess the target language independently and Today's guess is an English course whose base strings exist only in German. Six more blockers: guided progress is **localStorage-only** (no table in 89 migrations) so Home never leaves first-run and the streak is permanently 0; **every rate limit is a no-op** (`api_quota_settings.enforcement_enabled` default false); new accounts start at **0 credits** (the "10" is `invite_codes.credits`) with a 403 purchase path; the Classic skin is one tap away; **four storage buckets (`videos`, `audio`, `tts-pronunciations`, `voice-samples`) are world-readable with no owner predicate**; ko/ru/ja lessons print raw i18n keys. **Three production SQL queries gate the next build decisions** (profiles default, quota flag, `static_tts_playback` rows) — see Part 3 of the report. Security posture otherwise verified good: no secrets in the bundle, RLS on all 33 tables, privilege escalation closed. Nothing above is fixed yet; the report is a scope, and Part 9 holds 16 owner decisions for the strategy conversation. Earlier headline follows.
> 2026-07-25 — **TESTFLIGHT SCOPED; THE "RELATIVE `/api/*`" BLOCKER IS RETIRED.** Re-verified end to end: 14 `/api/*` fetch sites, all via `publicApiUrl()`, nothing relative left; CORS still allows `capacitor://localhost`; PKCE + Preferences + `appUrlOpen` intact; contract test green; `npm run build:ios` works, **dist 82.6 MiB**. Also cleared: browser SpeechRecognition has zero call sites (no WKWebView trap — guided speak is MediaRecorder → Whisper), and `src/api.ts`/Railway is admin-only. **What actually blocks a build:** (1) **the app icon + splash are the stock Capacitor placeholder** — verified by looking at the pixels, INV-IOS-001's size-based guess was wrong; needs a 1024 source (repo max is `android-chrome-512x512.png`, no `sharp`); (2) **`DEVELOPMENT_TEAM` still unset** (Mac/Xcode only); (3) **`cap sync ios` on Windows corrupts `ios/App/CapApp-SPM/Package.swift`** with backslash paths that Swift cannot parse — found by running it, reverted, and now blocked by `scripts/guard-cap-sync-platform.ts`; **`build:ios` is Mac-only**; (4) owner console work. **`ZL69STWQHV` is already live** in `public/.well-known/apple-app-site-association` as `ZL69STWQHV.pro.resonanz.app`, and INV-IOS-001's *"app no longer available"* is expired-TestFlight wording — this is probably not a first upload. Applied uncommitted (decision-free): platform guard, dead `applinks:resonanz.pro` pruned, `ITSAppUsesNonExemptEncryption=false`, three contract-test assertions updated/added. Full scope + risk register: `investigations/TESTFLIGHT_READINESS_SCOPE_2026_07_25.md`. **Robert chose EXTERNAL testers (invite), so Beta App Review applies and the conditional work is DONE:** (i) **the Stripe button would have shipped to every tester** — `isBillingTester()` reads `VITE_STRIPE_BILLING_SANDBOX_ENABLED`, which is `true` in this machine's `.env`, and `.env` loads in `ios` mode, so the bundle compiled the gate to `function x(){return "true"}` (verified in the built chunk); **the iOS bundle inherits the `.env` of whatever machine builds it**, so `isBillingTester()` now returns false on native before reading anything (invite-code redemption untouched); (ii) **icon + splash replaced with the real brand mark** (ffmpeg from `android-chrome-512x512.png`, 1024 RGB no-alpha + 2732 on black), contract test asserts dimensions and no-alpha; (iii) **`PrivacyInfo.xcprivacy` filled in** with seven linked/non-tracking types grounded in what the code stores (`speak_conversations`/`speak_messages` transcripts, `pipeline_events` usage; audio/images are never persisted but are declared because they reach providers) — **Robert still owns the matching App Store Connect answers + privacy policy**; (iv) `capacitor.config.ts` gained `ios.backgroundColor: '#0e0810'`. Gates: contract PASS, typecheck, eslint 0, i18n 1596/1596, first-light 27/27, prod build. **Still awaiting Robert: bundle id (recommend keep), background audio, a Mac + Apple-account confirmation, and the Beta App Review inputs (demo account, feedback email, beta description).**
> 2026-07-24 late — **TTS back-fill APPLIED to production on Robert's call (`c6cecfb2`): 30 rows** (fr animals L1, ceb animals L1 — his Home buoys — id animals L4). Robert's Bisaya words now carry real audio on the row itself; no play-time lookup. Left alone on purpose: 80 en rows in categories the animals-scoped en library never covered, and 86 rows whose curriculum metadata has no concept id (guided/enrichment content, not the static library). **Correction: the forward import path was already correct** — `importStaticCategoryLevel` resolves audio before the RPC and `attachStaticThematicAudioToImportedDeck` patches after; every audio-less row was a legacy import (≤2026-06-29, pre-`source_*` metadata) while de 2026-06-30 and ko 2026-07-06/16 landed with audio. `submit_curriculum_import` still doesn't write the column, but nothing downstream needs it to. Re-run `scripts/backfill-curriculum-tts-audio.ts` (dry run by default) after any new static-TTS language batch.
> 2026-07-24 late — **First Light survived its first real session; `5eeb1aed` pushed to main.** The practice card now HOLDS: any resolution settles in place (picture + word are replay controls) and the queue only advances on Next/Done. The "robot voice" was root-caused against prod — these curriculum word rows carry NO `tts_audio_url`, so every Home word resolves through the async `static_tts_playback` lookup and the first card of a visit played from an effect, outside the user gesture; recordings are now prefetched per visible buoy, and `playPronunciation` will stay silent rather than substitute a synthetic voice for a word it knows was recorded. Also fixed: the late-TTS effect speaking the NEXT card's answer on the lemma-swap commit (resolution now carries its card key). Polish: mobile language pill sits in the header, desktop buoy labels a band larger, hero CTA wears the sea's colour ramp (`.lw-swell-cta`). All gates green (i18n 1596/1596, fixtures 17/17 runnable, pixel-identity PASS, WordTide `d4bcb092` untouched). **Next: Robert's phone verdict → TestFlight preparation.** Open: prod back-fill of `words.tts_audio_url` from `static_tts_playback` (needs Robert's call); Codex xhigh job `bvm693p21` still uncollected; compact-desktop card/waterline overlap.
> 2026-07-24 late night — **FIRST LIGHT COMMITTED + PUSHED `d8339ea1` on Robert's call** (48 files, +6442/−235; scoped add — WordTide.tsx left unstaged for wave-rider; fixture screenshots gitignored). Vercel deploying to lingwave.ai; Robert's iPhone look is next. Open: Codex xhigh verdict (job bvm693p21) + a focused Opus delta-review of the post-review fixture-fix batch (launched post-push); findings arbitrate as follow-up commits. Details in the entry below.
> 2026-07-24 night — **FIRST LIGHT IS BUILT (uncommitted → now `d8339ea1`).** All six spec-§14 steps implemented by Fable's hand and verified: pixel-identity gate PASS on the LingwaveWaves additives (deterministic headless harness, `scripts/wave-pixel-gate/run.mjs`), 27/27 unit tests (`node scripts/run-home-first-light-test.mjs`), i18n 1594/1594 (+38 exactly), typecheck/eslint/build green, §11 budgets MEASURED (entry delta −119B; guided 6.96MB chunk off /dashboard's critical path — 187.6ms vs /today's 1382.5ms; time-to-hero cold 1876ms/warm 90ms), WordTide.tsx hash `d4bcb092…` unchanged throughout. Opus 5's adversarial implementation review (4H/10M/15L) fully arbitrated — all blocking findings fixed (reactive never-lock-last, hero-CTA fallback, slot-band label measurement, pool-empty dawn arm via `visitForDawn`, SR-twin/fold unification, BCP-47 sheet lang, honesty fixes) — record in memory/LOG.md. §13 FIXTURE MATRIX NOW GREEN: the harness (scripts/first-light-fixtures/, headless mocked-Supabase, screenshots in out/) found 6 product defects — 2 introduced by the review-fix batch itself — all fixed (sampled-field sustained-proximity collision, sheet double-advance guard, lesson-done hero race, strip placement + card-measured fold, true-visible backlog count, 150pt fitted card) and re-run 17/17 runnable PASS (2 not-run: UTC wall-clock crossing, true multi-tab). STILL OPEN: Codex xhigh review verdict (companion job bvm693p21 — collect + arbitrate), then Robert's commit call + iPhone visual sign-off. New files: components/home/×7, hooks useHomeVisit/useHomeRecommendation, lib/homeWordDetails; edited: the spec's 8 + languages.ts (§4-mandated export, disclosed). LOG 2026-07-24 (night).
> 2026-07-24 late update — **D-02 "FIRST LIGHT" LOCKED BY ROBERT; `design/DESIGN_SPEC.md` v2.1 FINAL; NEXT GATE = ROBERT'S IMPLEMENTATION GO.** The spec survived two full adversarial rounds + two delta rounds (Fable 5 pen, Opus 5 + Codex xhigh reviews — 41 findings arbitrated, then 18 delta remainders folded; record in `investigations/OPUS5_SPEC_REVIEW_2026_07_24.md` + `CODEX_SPEC_REVIEW_2026_07_24.md`). Key contracts: single `duePool`, monotonic in-renderer-eased dawn (recall alone reaches full dawn), additive-only LingwaveWaves changes (pixel-identity gated), strict-language default in useTodayMission + `lessonId`, "practiced" copy, 38 i18n keys, full fixture matrix, WordTide.tsx frozen (hash-equality proof). Implementation: fresh session recommended, spec is self-contained. No product code touched. LOG 2026-07-24 (later).
> 2026-07-24 update — **HOME REDESIGN ARBITRATION COMPLETE; DESIGN OS ROUND 02 ON DISK; ROBERT'S LOCK PENDING.** Fable 5 + Opus 5 independently reviewed Codex's Round 01, Codex counter-verdicted (all three in `investigations/*_2026_07_24.md`); consensus: the composite ring is REPLACED (never built) by wave-native progress — `dawn`/`ripplesRef`/`energyRef` already exist in LingwaveWaves and are unused on Home; the full-viewport ocean is the stage; bounded recall runs in place on Home. Round 02 verified on disk: `design/rounds/round-02/` D-02 First Light (recommended) / E-02 One Wave / A-02 A′ de-ringed + D-02-desktop. Binding cross-round rulings in `design/rounds/round-01/VERDICTS.md`. Review round also surfaced live Home bugs for any implementation: nothing-due state has NO CTA, misses visually vanish, useTodayMission can cross languages. No product code changed. LOG 2026-07-24.
> 2026-07-23 update — **HOME ORCHESTRATOR INVESTIGATION COMPLETE, NO PRODUCT CODE CHANGED.** Comprehensive handoff: `D:\CODING\ResonanceTEST\investigations\LINGWAVE_HOME_ORCHESTRATOR_REDESIGN_2026_07_23.md`. Design OS Round 01: `D:\CODING\ResonanceTEST\design\rounds\round-01\` (A Daily Current recommended; B Living Lexicon; C Session Mixer). Core recommendation: Home owns language-scoped prioritization/resume, not curriculum/library content; one best next action plus 2–4 alternatives; Remember / Discover / Use; Word Tide becomes labeled feedback or optional exploration. Composite goal ring deferred until Today/SRS/Speak accounting is trustworthy. Awaiting Robert + Fable verdict; implementation is blocked by Design OS and the user's read-only scope. LOG 2026-07-23.
Last updated: 2026-07-18 (main: **GERMAN B1 TTS IS LIVE AND de B1 IDS ARE FROZEN — owner applied the surface-check migration, rerun completed 958 generated / 0 failed / 69 ready-reattached (run_id a7431310, 37,560 chars paid), both verify scripts PASSED: 1,804 bright playback rows across german-b1 P1–P10 (168–192/path), 0 failed assets, 0 missing public_urls, 13/13 spot-checks incl. b1 dialogue+chunk clips; germanB1.ts header now TTS-FROZEN (typecheck clean). All TTS tiers now frozen: 10 A1/A2 languages + ja + ru A1+A2 + de B1. Remaining on this thread: OWNER COMMIT CALL for the pipeline files + migration + header; B1 P2–P10 device check (deferred). LOG 2026-07-18. Earlier headline follows.** Previous: **B1 PAID BATCH ABORTED AT 67/1,025 CLIPS — prod `guided_tts_asset_usages_surface_check` predates B1 and rejects dialogue/pattern; 67 clips paid+kept ready (2,331 chars, P1, 0 failed), migration `20260717160000_guided_tts_b1_surfaces.sql` WRITTEN AWAITING OWNER SQL-EDITOR APPLY, generate.py ready/dedupe usage upserts hardened never-abort, regression test added (guided_tts 59/59). Rerun after migration is idempotent (~958 clips + usage reattach). ids still UNFROZEN. Earlier: **B1 P2–P10 COMMITTED+PUSHED `3a4712d8` (owner call; device check skipped by owner) and the TTS pipeline now speaks B1: dialogue turn-1/3/4 + pattern ex-1..N surfaces taught to inventory.py/dump-script/batch-runner/seeder (`--level b1`, german-only guards, 45k row-sum cap), 4 new pytest cases (guided_tts 58/58), 10 rotation profiles seeded (Laura/William/Enniah/Helmut, offset 2×10 — owner-approved via AskUserQuestion), final dry-run CLEAN: 1,025 provider calls / 35,179 unique paid chars (39,973 on the cap metric; spec forecast 20–25k was pre-P2–P10). Pipeline-teaching files UNCOMMITTED. NEXT: the owner-gated paid batch (`run_guided_bright_batch.py --level b1 --languages german --commit`) — running it FREEZES the de B1 ids. No component requests turn-4 yet (generated per spec for the future you₂ affordance). Earlier headline follows.** Previous: **GERMAN B1 IS CONTENT-COMPLETE — ALL 100 LESSONS (P1 committed af2ca125; P2–P10 drafted this session in 3 sequential Codex batches at xhigh, validator-gated per batch, ALL 90 read by Fable, tier-wide adversarial review 67 findings → 64 applied/adapted + 2 rejected by arbitration, fixes by Fable's hand incl. 5 assert-class you₂→question-form passives, als/wenn slot-forcing, P9 timeline repair, Modell-dilution removal). Spec trophy-vs-staging conflicts fixed pre-draft (P3/P5 staging realigned, P7 damit↔klappt swap); validator +8 staged-form tripwires, stand noun-split, P8 L8–10 comparative anchors. ALL SUITES GREEN: b1 9,680/0, today-data 9,807/0 (10 paths), matrix 7,117/0, typecheck, eslint 0 new, i18n 1,556/1,556, prod build. UNCOMMITTED — owner: commit call + device check P2–P10; B1 ids UNFROZEN (TTS needs seeder taught dialogue/pattern surfaces, ~20–25k chars). Skill accreted 8 new tripwires. Earlier headline follows.** Previous: B1 DEVICE GATE PASSED (owner approved the 3 pilots) → GERMAN B1 P1 IS COMPLETE: L4–L10 Codex-drafted at xhigh, validator-gated, Fable-read, adversarially reviewed (14 findings → 13 applied/adapted, 1 rejected + 2 Fable-own fixes incl. the L5 live-scene/past-narration reframe and the L8 corePhrase rewrite `ich-bin-falsch-eingestiegen`), ALL SUITES GREEN (b1 1,006/0, today-data 9,762/0 with count 10, matrix 7,117/0 with 5 segment-1 cards, typecheck, i18n 1,556/1,556, prod build) — UNCOMMITTED, owner: device check L4–L10 + commit call.** Same session, from the owner's device feedback: BuildPhraseStep now shows the base-language "Your line" cue card (new `today.build.cueLabel` ×3 — at B1 the learner previously reached the chips with no visible cue), and the EN locale's ~30 German-leaked today.* values + FR leaks + the prototype-era en↔de-hardcoded strings ("Say the sentence in English", "German cue") were repaired/neutralized in all 3 locales; "polite question" prompt → "complete sentence". Mic-orb desktop clipping parked as polish. Robotic audio = by-design pre-TTS fallback; B1 TTS still needs seeder/batch-runner taught dialogue/pattern surfaces (ids UNFROZEN). New cloze-chip + episode-tense tripwires accreted to author-guided-tier SKILL.md. Earlier: B1 PHASE 0 COMMITTED+PUSHED `1009945e`. CONCURRENT SESSION same night: **JAPANESE TTS LIVE — A1 409 + A2 488 clips, 0 failures, 8,320 chars, ja A1+A2 ids now TTS-FROZEN** (4-voice roster Akane/Koichi/Konoha/ishibashi from public.voices, 20 per-path profiles, 991 playback rows verified + audio spot-checks; module headers updated to say FROZEN) **+ JAPANESE LIBRARY TIER 1 — all 1,850 concept terms translated by Fable's hand** (registry ja=日本語 experimental, stale test language-list fixed; Codex adversarial review DONE — 33/33 findings arbitrated accept/adapt, 34 fixes applied, gates re-run green; review-clean and UNCOMMITTED awaiting owner commit call). **⚠️ superseded 2026-07-17 midday — RUSSIAN TTS NOW LIVE + FROZEN: owner added 4 ru voices (Maria/Alan/Nina/Mark), roster wired [F,M,F,M] so A2 lands odd-F/even-M per the plan, batch ran A1 393/0 + A2 485/0 = 878 clips, 914 playback rows verified, 0 failed assets, ru A1+A2 ids FROZEN. See LOG 2026-07-17 midday.** Working tree dirty: wave-rider's WordTide.tsx + the ja-library/TTS session's files (categories.ts, staticCategoryTranslations.ts, test-static-category-translations.ts, japaneseA1/A2.ts headers, the two guided-TTS python scripts) awaiting review + owner commit call. Owner signed off all six §8 decisions (progress = localStorage-only through the B1 era; TTS cadence unchanged). Built per doc §6: level-keyed 7-step engine (episode types through BOTH resolved layers; Pattern/Complication/RolePlay components; Scene/Complete/TodayHero B1 branches hide you₁ until build; checkpoint before/blank/after branch; GuidedAudioSurface +dialogue/pattern; B1 matchPairs = lessonItems; 23 i18n keys ×3), `germanB1.ts` scaffold + **3 handwritten P1 pilots** (lost key / stopped S-Bahn / concert — trophies verloren/plötzlich/danach), `validate-guided-draft-b1.ts` **316/0** (episode shape, cloze-concat identity, per-path blank schemas + anchor whitelists, stage-gated Präteritum, register consistency, cross-corpus trophies), `tmp\B1_GERMAN_P1_P10_SPEC.md` (staging, shapes, blank schemas, 100-trophy pre-allocation, zero collisions) + author-guided-tier B1 branch. **Codex adversarial review of the diff: 15 findings → 14 applied (incl. HIGH: rolePlay unlocked completion after turn 1 — fixed), 1 rejected (partial-path consumers degrade gracefully, transient until P1 fills).** ALL SUITES GREEN post-fixes: typecheck, b1 316/0, a2 56,040/0, today-data 9,762/0, matrix 7,109/0, eslint 0 new, i18n 1,555/1,555, prod build. B1 ships silent-safe (browser speech; TTS seeder/batch surface enumeration still to extend before any B1 batch run; ids unfrozen). **Owner next: device-check the 3 pilots (German B1 P1 sits after German A2 P10 in the selector) — cloze keyboard/chip flow + two-part rolePlay ASR are the contract under test. Codex drafts P1 L4–L10 only after that gate.** LOG 2026-07-16 late-night entry; DECISIONS B1-Phase-0 block. Earlier same day: **RUSSIAN + JAPANESE A2 SHIPPED `2b58fd94` — the A2 tier covers 12 languages.** 200/200 lessons drafted (parallel ja/ru Codex batches at xhigh), validator-gated per batch, ALL read by Fable, adversarially reviewed (ja 30→23 applied/7 rejected; ru 30→28 applied/2 rejected — recurring reviewer failure modes: caption-whitelist, path-level-capstone over-generalization), fixes applied by hand, INTEGRATED single-pass. All suites green: typecheck, a2-phase2 56,040/0, today-data 9,757/0 (+80), matrix 7,093/0 (+ru/ja rows), speech, eslint, i18n 1,532/1,532, prod build — guided chunk lazy 6.29 MB, main untouched. TTS update 2026-07-17: **ja A1+A2 TTS is LIVE and ids are FROZEN** (897 clips, 0 failures — see LOG 2026-07-17); **ru A1+A2 TTS is now LIVE and ids are FROZEN** (2026-07-17 midday — owner added the 4 voices, roster [F,M,F,M]=Maria/Alan/Nina/Mark, A2 odd-F/even-M per the plan, 878 clips/0 failures; see LOG). Earlier same session: A1 frontier committed `353253cf` (owner call); desktop glass-blur (minifier alias-dedupe + Chrome dropped -webkit-backdrop-filter; 48 pairs reordered webkit-first, verified via headless-Chrome harness) + Library-follows-active-language fixes `e7b2c98e`; alphabet-for-ru/ja needed no work (registry-driven, hidden by the Library bug). Skill accreted (A2-for-new-languages block). Owner next: device-check ru/ja A1+A2 + hiragana module + header glass + Library language on the deploy; then the TTS voice-pick decision. LOG 2026-07-16 evening entry.** Previous: A1 frontier detail in LOG 2026-07-15/16. Previous: A2 Phase 4 COMPLETE — pl/id/ceb A2, 300 lessons, reviews arbitered 104/108 applied, TTS live 1,476 clips/0 failed, ids FROZEN, committed `efdbfe82`+pushed** — **the A2 tier now covers ALL TEN guided languages** (es/fr/it/pt/de/en/ko/pl/id/ceb); Polish's §5.3 gendered past solved by matching per-path TTS voice gender + alsoAccept both-gender answers — LOG 2026-07-15 Phase-4 entries ×2. Next content frontier: new guided languages (Tagalog/Dutch/Hindi/Arabic; Russian owner-gated; Japanese = owner decision). Owner device-checks pending: ko A2 `482dc69b`, de/en A2 `6d8b0080`, now pl/id/ceb A2. Earlier today: Korean A2 `482dc69b`. 2026-07-14: de/en A2 `6d8b0080`; streak-pill `078f2a8f`; wave-rider v1.5 `b215a4c0`)

> 2026-07-13 update (LOG 4+5+6): after the owner rejected v1's visuals, Wave Rider got a full v1.5 overhaul, committed+pushed on owner call as `b215a4c0` (scoped add — the tree also holds main's untouched A2 Phase 2 fr/it/pt drafts from the concurrent Codex agent): (a) render layer rebuilt in **three.js** (`renderer/` replaces the Phaser `scene/`; low-poly ocean, jet-ski rider, pontoon word buoys, day-cycle skies; Phaser stays for slicer); (b) `?deck=` deep-link auto-starts the deck (no second picker); (c) library-imported decks with null `tts_audio_url` get static thematic audio attached in-memory from `metadata.curriculum` concept ids; (d) picker leads with one Play card. Codex adversarial review 5/5 findings fixed (texture LRU, context-loss pause, water vert budget, param-change remount, AudioContext dispose). All gates green (typecheck, eslint, engine 13/13, adapters 5/5, i18n 1531/1531, prod build) + headless mocked-Supabase playtests incl. rush, replay, pause, 120fps, 0 console errors. package.json gained `three` + `@types/three` 0.185.1. **Next: owner iPhone re-test on the live deploy.**

## What this is
Lingwave, a cloud-first language-learning app (guided lessons, SRS cards, AI music, voice tutor). Live frontend in `frontend/` (Vercel + Supabase); Python generation backend (`job_runner.py`, `src/`, `cloud_engines/`) on Railway. Target: TestFlight/private beta this month (July 2026).

## Working now
- Frontend product surface: guided Today mission, dashboard (glassy + classic skins), decks/study modes (image, audio, flashcard, canvas), music (level-song / song-only), Speak voice tutor, games hub, admin suite. Actively developed (last commit 2026-07-04, landing redesign).
- Cloud generation pipeline: `job_runner.py` v2 pipelined orchestrator + `src/orchestration/*` workers + in-process `src/cloud_dispatcher.py` → `cloud_engines/*`. Orchestration last active 2026-06-28.
- Phase 1 stabilization program (docs/Stabilization/): role/credit hardening, RPC-only invite redemption, atomic `submit_generation` + `request_word_retry`, worker `pre_bootstrap`. Has a `PHASE1_FINAL_CLOSEOUT_REPORT.md`.
- Static thematic TTS batches (es/de/fr/ceb/id/ko).
- **Capacitor iOS shell (audited 2026-07-08, ~95% done):** all 14 `/api/*` call sites via `publicApiUrl()`, CORS allows `capacitor://localhost`, Bearer-token auth with PKCE + `resonance://auth/callback` deep link, camera/mic Info.plist strings, PrivacyInfo.xcprivacy, contract test green. Fixes `cf6081d8`: lingwave.ai default origin, committed `.env.ios` + `--mode ios` build, voice-sample CORS normalized. Blockers are owner-side only (see Next actions). Bundle cost SOLVED: slimming phase 1 (`26255d8f`) + phase 2 (`aeb8cf2b`) — `publicAssetUrl()` resolver funnels curriculum, vibes-emblem, and trophy-song assets; `scripts/strip-ios-bundle.ts` in `build:ios` strips `dist/curriculum` + `dist/guided/{trophy-songs,vibes}` (frees 623.3 MiB; **dist 77.7 MiB** measured). Small guided UI assets (today/reviews/trophies/lesson-numbers, ~5M) stay bundled + unwrapped on purpose (offline). Native device smoke pending first iOS build. **2026-07-25 re-verification:** the `/api/*` routing, CORS, PKCE/Preferences/deep-link auth and the contract test all still hold; `build:ios` re-measured at **82.6 MiB**. The "~95% done, blockers owner-side only" framing was too generous — it missed that **the app icon and splash are still the stock Capacitor placeholder** and that **`build:ios` must not run on Windows** (see the 2026-07-25 headline and `investigations/TESTFLIGHT_READINESS_SCOPE_2026_07_25.md`).
- **Skins (decided 2026-07-08, delegated to Fable):** both skins stay selectable; new users/devices default to glassy (`a2ee4e16`); `profiles.skin` now restored when localStorage is empty. PG/classic consolidation parked until beta feedback.
- **Usage/cost observability (committed `02760527` after audit):** the five formerly-blind `/api/*` provider endpoints + `src/cost_logger.py` rate table now log per-call usage/cost; migration `20260619120000_pipeline_events_usage_cost_columns.sql` committed but NOT applied to Supabase yet (graceful until then).
- **Script Lab / Alphabet module (2026-07-06):** `/alphabet` teaches writing systems; Korean/Hangul V1 live (47 symbols, Learn/Build/Quiz, 141 static audio clips + browser-speech fallback, pushed to main) and **Russian/Cyrillic** (33 letters, browser-speech audio). Registry-driven — new scripts are data packs (`.claude/skills/add-script-lab-language`). Manual mobile QA pending.
- **Language-expansion system (2026-07-06):** `docs/Product/FABLE_LANGUAGE_ARCHITECTURE.md` (4 disjoint registries, launch tiers 0–5, landmines; 2026-07-12 delta block prepended) + three skills — `.claude/skills/add-target-language`, `add-base-locale` (RTL blocked), `review-language-addition`. Proven with **Russian**, now shipped through **Tier 0 (wizard) + Tier 1 (categories: all 1,850 concept terms, experimental status, 462 needsReview) + Tier 3a (Speak via Gemini/Grok, code-only) + landing showcase + Cyrillic Script Lab pack** (commit `6f81f2a3`). NOT yet done for Russian: **Tier 5 guided** (scoped, ~5k lines flagship content, pending owner go-ahead — see LOG) and **Tier 2 static category TTS** (owner-run paid batch; until then Russian category cards are silent, by design). Base/UI locale in Russian is a separate, unrequested effort (`add-base-locale`).
- **Language-surface unification step 1 (2026-07-12, UNCOMMITTED):** wizard/home now offers **15 languages** — Polish registered in `lib/languages.ts` (wizard-only, deliberately NOT isSpeak: api has no 'pl'; Speak degrades to grid, verified) + isWizard flipped on Portuguese/Dutch/Hindi/Arabic; langName ×3 locales, PL flag, font routing, Lens prompt rules. **Guided speak-step transcription FIXED** — `api/guided-transcribe.ts` had 400'd all non-English locales and hardcoded 'en' to Whisper (broken for 8/9 guided languages, unnoticed because only English-target was tested); now accepts every guided locale incl. ko-KR, ceb auto-detects. Full inventory + remaining mismatches (voice-sample allow-set, Bisaya/Cebuano naming, landing gaps): `investigations/LANGUAGE_SURFACE_UNIFICATION_2026_07_12.md`. All checks green (typecheck, api tsc, eslint, i18n 1497/1497, paid-api suite).

## In progress
- **Home orchestrator redesign (home-orchestrator workstream, 2026-07-24):** **D-02 First Light LOCKED by Robert; `design/DESIGN_SPEC.md` v2.1 FINAL** (two adversarial + two delta review rounds, Opus 5 + Codex xhigh; binding rulings in `design/rounds/round-01/VERDICTS.md`, lock in `round-02/VERDICTS.md`). Awaiting Robert's implementation go — build per spec §14 sequencing in a fresh session; WordTide.tsx stays frozen until wave-rider lands.
- **B1 tier: content COMMITTED `3a4712d8`; TTS LIVE + ids FROZEN 2026-07-18** — batch completed 958/0 after the owner applied the surface-check migration, verification passed (1,804 bright playback rows on the 10 german-b1 paths, 0 failed, 13/13 spot-checks), germanB1.ts header TTS-FROZEN. Remaining: (a) owner commit call on the pipeline files + migration + header; (b) the deferred device check of P2–P10.
- **Wave Rider arcade game (wave-rider workstream, 2026-07-13, UNCOMMITTED):** second game after slicer — `/games/surf`, 3-lane surf word game (El Taxico-inspired, built from the deleted runner's autopsy). `src/games/surf/` (pure tested engine + one Phaser scene + picker/HUD/complete), registry+route exposed, tile icon in `public/games/surf/branding/`. Sources: user deck / due (`?queue=due&lang=`, records `study_mode='surf'`, feeds SRS) / static category packs (no recording, static audio + speech fallback). 33 i18n keys ×3. Checks: engine 13/13, typecheck, eslint, i18n 1530/1530, prod build, headless-Chrome playtests all green. Design doc `docs/Product/FABLE_WAVE_RIDER_PLAN.md`. **Owner: (a) enable `sound_generation` permission on the ElevenLabs key then `npx tsx scripts/generate-surf-sfx.ts` (7 clips, ~140 credits, game is silent-safe until then); (b) device-test on iPhone (tap lanes, due mode with real words, SRS rows); (c) commit call.**
- **Guided TTS: LIVE for all 10 A1 languages (2026-07-12 evening, committed `27388103`+`dc89cc70`):** 3,744 bright clips on `eleven_multilingual_v2` (corePhrase+chunks+trophyWord; speakTarget deliberately skipped — near-dupe of core; playback never requests it). One generation per text; voice rotates PER PATH from hand-picked rosters via 91 `scope_path_id` profiles (`{lang}_a1_bright_p{n}_multiv2_v1`; id = Gavrila only — Blasto gain landmine; ceb = Mayumi; ko P1 = Jini). Prod schema was ALREADY applied (probe script proved it — the rescued-draft migration was never needed). 3,970 bright `guided_tts_playback` rows verified (424–453/lang), 13/13 audio spot-checks. Batch tooling: `scripts/run_guided_bright_batch.py` (guardrailed dry-run+commit, idempotent — reruns only missing clips), `seed_guided_bright_rotation.py`, `probe_guided_tts_state.py`, `verify_guided_tts_*.py`. English wistful/sharp keep old flash-v2.5 audio (stale for changed texts; vibes discontinuation-bound). Provider gotchas: multilingual v2 rejects `language_code` (now gated); PostgREST in-() lookups 400 above ~100 keys (now batched). **Rerun-safety hardened 2026-07-12 night (UNCOMMITTED, post-Codex-review):** storage paths now include voice id (reseed can't overwrite old clips), paid clips can't be demoted to failed by usage-upsert errors, provider validates 200 payloads, verify scripts gate via exit codes; before the NEXT batch run a one-language dry-run and expect 0 missing (owner-side — classifier gates prod runs). Known accepted risks in LOG (3): timeout-retry double-spend, db.py pagination caps, probe 3k-row cap.
- **Guided A1 content fix batch DONE (2026-07-12 evening, committed `27388103`):** 191/193 review findings applied (Codex gpt-5.6-sol xhigh to spec; 2 skips = intentional target-equals-base locale fallback, contract narrowed). Fable round 2 fixed what the review's own suggestions broke: 10 English wistful/sharp variants rewritten for cross-vibe distinctness (bright untouched = TTS baseline), trophy dedupes (en/ceb/pt), ID P8 checkout kept as loanword with trophy `sekarang` + leakage-test exemption for that one lesson, 5 low-value typeRecall answers restructured, stale test baselines regenerated (EN arc titles now German, tts-inventory 236→202 chars). All 900 lessons still 'draft' status but content is now review-clean; 9 suites green (cross-vibe 0 hard fails / 0 collisions).
- **Guided language buildout (2026-07-12, owner-directed):** order was more A1 languages → guided TTS (both DONE) → **A2 design (next)**. **Korean A1 Practical 1 SHIPPED (uncommitted):** new per-language module `src/data/guided/koreanA1.ts` (pattern for all future guided languages — type-only imports, bright-only) + integration; Fable reviewed all 10 lessons; 9 guided suites green, prod build ✓ (guided chunk intact). **Speak-step scoring was Hangul-fatal** (ASCII-only normalizers in `guidedSpeechCheck.ts`/`speechRecognition.ts` emptied non-Latin transcripts) — fixed Unicode-aware same session. Remaining guided-less wizard languages: Tagalog, Dutch, Hindi, Arabic (+ Russian, owner-gated; Japanese = new-language decision). Korean P2–P10 next once owner approves the pilot on device. **Guided A1 quality review DONE:** 900 lessons / 9 languages all have 10 paths ×10 lessons; wistful/sharp only exist for English; 193 findings (31 HIGH: pt bus-stop term, es/id checkout calques, it "Sono tardi" compose-fail, pl missing "się"; systemic: EN metadata .de in English ~310 fields, ceb/id sceneCaption.de in target language, placeholder captions .de English) → fix batch is the next content step, report `investigations/GUIDED_A1_CONTENT_QUALITY_REVIEW_2026_07_12.md`. **Guided TTS plan** at `docs/Product/FABLE_GUIDED_TTS_BATCH_PLAN.md` — 4 owner decisions pending (apply guided SQL, scope/voices-per-lang, it/pt/pl voice picks, bright-only confirm); playback + Python ElevenLabs pipeline already exist, ~90k chars for 9 langs ×2 voices. Japanese = owner decision (brand-new language, Tier 0 + kana Script Lab needed first).
- **Nav restructure + slicer due-words mode (2026-07-11 evening, COMMITTED+PUSHED `a1068fc9` on owner call — 24 files, details LOG 2026-07-11 (2)):** owner-confirmed IA implemented: 6-tab nav from single `primaryNav.ts` config — Home / Today / **Media** (Cards+Music; /decks + activePaths /deck,/generate,/music; `MediaSegments` Cards|Music pill atop all 4 collection pages) / **Library** (/categories, owner's addition) / **Lens** (flag-gated) / Speak; **Study tab removed** (routes stay; study entries: home Dive-in/preset, mastered pill → configurator, deck Study buttons → /study?deck=). Glassy home dropped the 3 quick-practice tiles + Library/Lens tiles (tide + Dive-in + mastered pill remain); classic home keeps its tiles (only study entry there), drops Library/Lens tiles. Playful Dive-in → `/games/slicer?queue=due&lang=X` slices the Word Tide's due pool directly (useWordStates `fetched` flag + shared `isLemmaDueNow` + derived `dueChoice`/`inDueSession`; empty pool → picker fallback); study SessionComplete labels now say "Back home" (they navigate to /dashboard). Checks: typecheck ✓ eslint 0 ✓ i18n 1502/1502 ✓ nav contract test ✓ prod build ✓. Codex adversarial review + re-review: HIGH fallback-filter bug and a tailwind-merge padding regression found and FIXED; adding `image_url(s)` to WORD_SELECT declined (columns unproven on `public.words`, a bad column 400s all slicer loads). **Next: owner commit call + iPhone re-test of nav/home/dive-in.**
- **Owner mobile-feedback fix batch (2026-07-11, COMMITTED+PUSHED `40ef3aff` on owner call):** TidePracticeSheet portalled to body (z-[60], centered, scroll lock, no autofocus — root causes: main's z-10 stacking context under the z-50 bottom nav + iOS keyboard viewport pan), home presets gated on asset probes (dead stored preset → picker), deck dropdown hidden in home-launched sessions (`deckScopeLocked`), /study/type fixed-height layout, slicer DeckPicker back honors `returnTo`, streak as compact chip in HomeAccountStrip. **Owner iPhone re-test after next deploy still pending.** Details in LOG 2026-07-11.
- **Owner UX batch (2026-07-10 session C, COMMITTED+PUSHED as `670f7a08` + brain save `39a174ad`):** (1) **Typed-recall study mode** — `/study/type` both skins (`src/pages/StudyType.tsx`), Write tile (✍️) in StudyModeSelector, forgiving matcher `src/lib/typedAnswer.ts` (article-drop, variant split, OSA distance ≤2 by length, diacritics preserved; 17-case behavioral run ALL PASS), `study_mode='typed'` into recall_attempts (CHECK constraint already dropped), success burst + TTS + reduced-motion fallback, `study.typed.*` keys ×3 locales. (2) **Library card viewer prev/next** — `CardDetailModal` `navigation` prop (chevrons/arrow keys/swipe/counter) wired into static + curriculum LevelDetailPage paths; static modal centered + Play i18n'd. (3) **Train→Audio pool-mismatch fix** — StudyModeSelector Image/Audio probes scoped to the queue's word ids (≤150) so home tiles can't offer a mode whose session serves different words. (4) **Word Tide home redesign (owner GREEN-LIT V1 mid-session, built):** glassy DashboardPG restructured — greeting + TodayMissionCard/useTodayMission removed from the populated home (guided lives on the Today tab; perf win), new `WordTide.tsx` (≤8 drifting orbs = today's due pool, image-coated or hue-hashed, tap→inline typed-practice sheet, `study_mode='tide'`, orbs pop on grade + backfill, all-clear state) + `experiencePreset.ts` (per user+language localStorage: visual/listening/writing/playful → one-time picker card, "Dive in — N to go" CTA and quick-practice tiles deep-link straight into the preset's study mode, configurator stays on the Study tab). Checks: typecheck ✓ i18n 1496/1496 ✓ eslint 0 ✓ prod build ✓. Owner visual verdict came back 2026-07-11 (feedback round 1 → see the fix batch above). ~~Codex CLI too old~~ → upgraded: v0.144.0 + gpt-5.6-sol verified working 2026-07-12 (companion runtime `status`/`result` from repo root).
- **Apple-design fluidity pass (audit `investigations/APPLE_DESIGN_SKILL_AUDIT_2026_07_10.md`): Tiers 1+2-core SHIPPED 2026-07-10.** Tier 1 `6cec01ef`: press states on shared Button/nav/tiles/grading circles, reduced-motion + reduced-transparency fallbacks, 7s error toasts. Tier 2 `526dfbaa`: SRS flashcard 280ms grading lockout removed (grade+advance same tick, decorative pulse, 260ms repeat guard, direction-aware exits) + new `SwipeGradeCard.tsx` swipe-to-grade (1:1 drag, momentum projection d=0.998, velocity handoff, reduced-motion safe, i18n'd aria). Codex adversarial review: no blockers, 3 hardening fixes landed. Tier 2 item 3 also shipped (`1834646f`): nav `!bg-black/40`/blur overrides deleted — theme-contract `--nav-bg` + fallbacks now paint the nav on every theme. **Next: owner iPhone feel-test of the deployed swipe/instant grading + new nav chrome on all themes; Tier 2 rest (water carousel physics, card viewer modal, bottom sheets) + Tier 3 in the audit backlog.**
- **iOS/mobile UX polish batch (2026-07-06, committed `e3fce8f0`; add-language follow-up fixes `99dbb499`):** 14 fixes from owner iPhone testing — dashboard "+ Add language" (localStorage-persisted, empty-state landing), Decks collapsed language selector + accent Generate CTA, runner game fully deleted (slicer stays; `study_mode='runner'` data guards kept), Delete-account behind Advanced options, study-mode 2-col mobile grid, level-song request confirmation + toasts moved above bottom nav (old `--bg-card` var was undefined → invisible toasts), centered route loaders, centered mobile header brand + icon-only coin, welcome spacing floors, landing gradient-wave headline + deduped wall images + "Ready to learn in waves?"/"Start here" copy. All checks green (typecheck, i18n, lint, build). Details in LOG. **Next: owner commit call + iPhone re-test.** Not addressed: home-page loading perf.
- **Lens camera feature: phases 2A–2D BUILT, VERIFIED, and PUSHED (commit `0d42e989` on `main`, Vercel deploying).** Capture-first camera page `/lens` (flag `VISUAL_LENS_ENABLED=true`, protected fullscreen route, lazy, dashboard tiles in both skins) → `/api/visual-scan` (Gemini 2.5 Flash-Lite, quota action `visual_scan` 30/day, cost/usage metering, strict contract normalization, safety stripping) → save via new `submit_lens_save` RPC into auto "Lens — {Language}" `card_text` decks (find-or-create, in-RPC `word_slug` dedupe, transliteration in metadata). Per-row save for text/menu results, bulk save, recap, duplicate hints, source-language prompt rules. All checks green (typecheck, api typecheck, lint, check:i18n, 18/18 endpoint tests, 17/17 mapping tests, 11/11 mock-provider tests). Owner has applied both migrations (`20260706020000_visual_scan_quota_action.sql`, `20260706030000_submit_lens_save.sql`) to Supabase and confirmed `GOOGLE_AI_API_KEY` is set in Vercel. Docs: `docs/Product/FABLE_VISUAL_LENS_MASTER_PLAN.md` (status: built; owner answers recorded) + discovery report in `D:\CODING\ResonanceTEST\investigations\`.
  - **Commit was scoped by hand**, not `git add -A`: the working tree had ~136 dirty entries at push time, including unrelated pre-existing uncommitted diffs in `api/{extract-vocabulary,guided-transcribe,suggest-words,translate-and-ipa,voice-chat}.ts`, `src/pages/Onboarding.tsx`, `src/utils/lazyWithRetry.ts`, `../src/cost_logger.py`, and a partial fix to misplaced `langName.Russian`/`langName.ru` keys in `translations.ts` (from the concurrent language-expansion session) — all left untouched/unstaged at the time. (Audit 2026-07-07: the langName repair has since landed as `b1737149`; the api/*.ts, `Onboarding.tsx`, `lazyWithRetry.ts`, `../src/cost_logger.py` diffs are STILL uncommitted in the tree.) Used a hand-built `git apply --cached` patch to stage only the `lens.*` hunks out of `translations.ts` since the Russian langName fix hunks lived in the same file. Also skipped `ADVERSARIAL_REVIEW_CAMERA_VOCABULARY_FINDINGS.md` (unrelated slicer-game doc, not Lens despite the name).
  - **Owner live-tested on iPhone Safari (lingwave.ai) 2026-07-07 — works end to end, but recognition-quality/trust issues found, NOT yet fixed:** camera→scan→save→deck loop confirmed working (digital piano, lamp, headphones, German article chip + example sentence all correct); one transient "Vision service unavailable" on a first attempt that succeeded on immediate retry (cause unconfirmed — cold start vs real intermittent failure); Korean tap-to-speak silently produced no audio while German did (suspect Web Speech API voice-availability, same class of bug the Script Lab audio work already solved with a voice-list warm-up — see [[DECISIONS]] 2026-07-06 Script Lab entry); no in-Lens target-language switcher (must exit to `/dashboard` to change language then re-enter); **safety-relevant finding:** the on-screen reticle circle is cosmetic only — `frameToCanvas()` in `Lens.tsx` captures the FULL camera frame, not a crop of the reticle region, and the model twice returned a wrong answer (photographed a bedside scene → "nightstand"; another photo → "suit") while still self-reporting **high confidence** on both — confidence is a model-self-reported label, not calibrated, so wrong guesses currently look exactly as trustworthy as right ones, which is actively bad for a vocabulary-learning product; owner also wants the reticle bigger (had to back away from objects repeatedly) and a way to remove/discard an item from the in-memory recap before it can be swept into a later bulk "Save unsaved" (every successful scan — even wrong ones — auto-appends to the recap today, with no per-item discard).
  - **2G hardening pass BUILT + VERIFIED 2026-07-07, committed+pushed as `ae8ace4e` (audit-corrected — a later session committed it without brain-save):** scan payload now crops to the reticle (+35% context, cover-math mapping, full-frame preview kept), reticle enlarged to `min(74vw,23rem)`, confidence chip removed (only `low` surfaces, as a caution pointing at alternates; prompt gained framing + calibration rules), server-side single silent retry for transient Gemini 502s, in-Lens language switcher (per-language recap tagging so bulk save can't cross decks), recap per-item discard, Korean TTS fixed via `getLanguageCode` normalization + explicit voice pick + mount-time voice warm-up in `usePronunciation`/Lens. Card_text display cleaned up alongside: translation centered on the black card face (`revealed`-gated; StudyFlashcard front pinned false), duplicate word renders removed (viewer modal → speaker icon + IPA; PG carousel → speak icon), "Generated" badge hidden for text decks in both skins. All checks green: typecheck, api typecheck, eslint (changed files), check:i18n, 24/24 visual-scan (6 new retry tests), 17/17 mapping, 11/11 mock provider, 69/69 word-display-metadata. Photo-on-card DECLINED at MVP (see [[DECISIONS]] 2026-07-07). **Next:** owner iPhone re-test of the deployed 2G behavior (watch menu/text scans under the new crop). Device-only Capacitor/WKWebView matrix (torch, orientation, backgrounding, offline-mid-scan) still separately untested — Safari testing doesn't cover the native shell.
- **TestFlight UX-coherence pass (session B):** audit complete, 4 plan docs in `docs/Product/FABLE_*.md`; Speak Phase 1 re-skin landed; **StudyCardFrame + SessionComplete extraction DONE 2026-07-08 night, committed `7ac30d21`** — new `src/components/study/StudyCardFrame.tsx` + `SessionComplete.tsx`, wired into all 6 study surfaces; StudyFlashcard/StudyAudio now skin-aware (glassy chrome under glassy skin — intended); all checks green (see LOG). Next: Speak Phase 2 curation (provider consolidation ON HOLD by owner), plan Phase 4 image-study consolidation.
- **Lens reticle enlarged again 2026-07-08 night (owner ask), committed `7ac30d21`:** `min(74vw,23rem)` → `min(90vw,28rem)` + vignette hole widened, `src/index.css` only — crop follows the DOM rect automatically. Owner should re-test framing on iPhone after deploy.
- **Curriculum bundle slimming: COMPLETE (phases 1+2), committed `26255d8f` + `aeb8cf2b` 2026-07-08 late night** per `investigations/CURRICULUM_BUNDLE_SLIMMING_PLAN_2026_07_08.md`: `publicAssetUrl()` in `publicOrigins.ts` (web no-op, native → lingwave.ai, absolute/data/blob passthrough) funnels the 4 `imageUrls.ts` helpers, 3 static builders, raw sites, guidedVibes emblems, and guidedTrophySongs audio urls (wrapped at the data source). `scripts/strip-ios-bundle.ts` in `build:ios` strips `dist/curriculum` + `dist/guided/{trophy-songs,vibes}` — measured 623.3 MiB freed, **dist 77.7 MiB** (web build untouched, trees verified present). Small guided UI assets deliberately unwrapped + bundled (offline). All checks green (typecheck, contract test, 7 guided suites run individually, eslint, check:i18n, both builds). Native device smoke pending first iOS build.
- **Speak re-test feedback pass COMMITTED + PUSHED 2026-07-08 as `c4ede7a9`** (theme restore from `profiles.theme`, header brand hairline, Live hero casting screen, tutors accordion, tutor mute; details in LOG). **Next: owner deploy re-test** — theme survives a cookie wipe after one login, header hover ramp, casting hero + accordion, mute button.
- **Speak redesign BUILT + VERIFIED 2026-07-07, committed+pushed as `3950c97f` (owner visually reviewed on the deploy 2026-07-08 and approved — revert is off the table):** owner approved D1–D6+D8 from `docs/Product/FABLE_SPEAK_REDESIGN_DECISIONS.md` (D7 quota work declined — no enforcement flip/countdown/usage UI). Implemented: Speak inherits app-wide active language (grid only for users with none) + header language-switcher dialog; ready room (one-tap resume of last tutor via `speak-last-setup-${lang}` localStorage, clickable level chip); roleplay frontend slice DELETED (roleplayScenarios.ts, toggle, wizard, hook state — API branches left inert, history panel still renders old roleplay rows); ProviderToggle + VoiceTutorPicker deleted, replaced by casting screen = LiveDoorCard (premium gold, hidden for fil + during hot-swap) + merged CharacterGrid (style tutors/characters/6 curated "Classic voices" with comic mascots in `public/characters/voices/*.webp`, Codex-generated; fil/ceb hide voxtral sections); Gemini voice stage removed from GeminiModeVoicePicker (grid owns voice pick → vibe → accent/start); grok level step skipped when level saved; conversation header = back + identity + one Settings sheet (level/study/listen/change tutor/history/new chat written out); grokPedagogy zero/beginner text synced into `api/prompts/_shared/pedagogy.ts`; i18n en/de/fr full coverage incl. 5 previously-missing fr speak keys. All checks green (typecheck, api typecheck, eslint, check:i18n, build). Related quick fixes already pushed as `99dbb499` (Decks add-language removed, welcome screen keeps LanguageCluster, opaque picker panels, mobile welcome centering).
- Video product direction: deprecated user-facing (`VIDEO_LANE_ENABLED=false`), backend pipeline kept (see [[DECISIONS]]).

## Known problems
- ~~Root-level clutter (L5)~~ SOLVED 2026-07-11; decide-list CLOSED same day (LOG (5)): worktrees/branches/venv/stale-copy/`_archive` deleted, `refactor` dropped (was merged), media kept in place. `_archive` no longer exists — un-versioned history is gone by owner decision; the 5 rescue-worthy SQL files live in `frontend/supabase/rescued-drafts/` (UNCOMMITTED, with the 3-file `/games`-fallback fix — owner commit call). `D:\CODING\ResonanceTEST\tmp\` scratch (~35 loose files) was not on the decide list, still there.
- 14 pre-existing stale frontend-contract pytest failures (assert on evolved UI copy/structure; verified present before the cleanup): music_lyrics_frontend ×6, profile_base_language ×2, gpt_premium_pricing ×2, admin_gpt_image_2 ×1, frontend_duration_fields ×1, phase1b_atomic_retry ×1, concept_lyric_levels ×1(+env). Same class as the guided-today-path-overview staleness. Needs a repoint-at-live-surfaces pass.
- Duplicated skin page pairs (classic vs PG/GO variants) — divergence risk; both skins ship for beta (decided 2026-07-08, glassy default), consolidation deferred to beta feedback (L1).
- Video/pipeline Python core frozen since ~2026-04-30 — treat as legacy-stable; don't refactor casually.
- `start_cloud.py:66-89` hard-requires video-era `POD_URL`/`POD_AUTH_TOKEN` at boot — unsetting them in Railway kills card/music generation (L4).
- The `videos` Supabase bucket holds card PNGs too (`card_worker.py:710-783`) — never "clean up" that bucket as video-only (L6).
- Admin Profiles voice/LoRA pickers call local-DAW endpoints via `src/api.ts` that 404 in production (admin-only).
- `/api/generate-imageless-tts` is referenced by `useGenerateImagelessTts.ts` but the Vercel function doesn't exist — blocks Lens Phase C generated audio (browser TTS unaffected).
- Codex sandbox on this machine cannot spawn esbuild (`tsx` → `spawn EPERM`) — Codex packets can't run tsx-based checks; Fable must re-verify locally every time.
- `scripts/test-guided-today-path-overview.ts` has 6 pre-existing stale source-string asserts (found 2026-07-08 late night, verified unrelated to the slimming work) — it sits mid-`&&`-chain in `test:guided-today`, so the 5 scripts after it never run via npm. Same class as the repaired generate-layout test; needs the same treatment.

## Open questions
- Landing experiment routes `/a`, `/b`, `/landing/*` publicly routable — gate or keep?
- ~~iOS bundle id~~ **DECIDED + APPLIED 2026-07-26: `ai.lingwave.app`.** Robert checked App Store Connect and it holds only Canta — no `pro.resonanz.app` record — which disproved the inference the 2026-07-25 "keep it" recommendation rested on and made the rename free. Applied across `capacitor.config.ts`, `project.pbxproj` ×2, `ios/App/App/capacitor.config.json`, the `Info.plist` CFBundleURLName and both AASA copies; contract test asserts all of it. `resonance://` deliberately unchanged. **Still needs an App ID registered in the developer portal (Associated Domains capability) and the App Store Connect record created under this id.**
- **NEW, gating the next build decisions — three production SQL queries** (Part 3 of `BETA_READINESS_PRODUCT_SCOPE_2026_07_26.md`): (1) `select base_language, credits from public.profiles order by created_at desc limit 5` — if every row says `'English'`, `Onboarding.tsx` is dead code and the app asks a new user nothing; (2) `select enforcement_enabled from public.api_quota_settings` — if false, provider spend is uncapped right now; (3) `select language, count(*) from public.static_tts_playback group by language` — rows for de/es/fr/ko mean already-paid Library audio can be switched on in one line.
- **NEW — should web signup at lingwave.ai stay open during an invite-only beta?** "Invite-only" currently describes the iOS distribution channel only; any email can create an account and codes merely grant credits. The cheapest gate is the Supabase signup toggle, not code.
- ~~TestFlight testers: internal or external?~~ **DECIDED 2026-07-25: external, by invite** (≤10,000; Beta App Review on the first build of each version). Robert owes the review inputs: beta description, feedback email, **a working demo account with credits**, privacy answers + policy URL.
- ~~Native Stripe surface?~~ **DECIDED + FIXED 2026-07-25:** hidden on native at the source (`isBillingTester()` returns false before reading env), because the env flag baked `true` into the bundle and would have shown it to everyone. Invite-code redemption unaffected.
- Background audio: `UIBackgroundModes` is absent, so AI music stops on lock/background. Declare it (and genuinely use it) or accept it for the beta? Owner must decide.
- Lens name final sign-off + eventual tab promotion — owner decides at the 2F gate after dogfood.

## Next actions
0h. **Home orchestrator:** D-02 LOCKED + `design/DESIGN_SPEC.md` v2.1 FINAL. Robert: give the implementation go (fresh session recommended — spec §14 has the build order; verification = typecheck/lint/check:i18n/build + §13 fixture matrix + WordTide.tsx hash equality).
0. **B1 (2026-07-18): TTS batch DONE (958/0, verified, ids frozen) — owner: commit call on the uncommitted pipeline files + migration + germanB1.ts header.** Device check of P2–P10 still open (owner deferred it).
0b. **Wave Rider (wave-rider workstream):** owner — ElevenLabs `sound_generation` permission + run `scripts/generate-surf-sfx.ts`; iPhone device-test `/games/surf` (picker, due mode with real words, `study_mode='surf'` rows in recall_attempts); commit call for the surf files (scoped add — the tree also holds main's un-integrated guided drafts). Code-side v2 backlog lives in `docs/Product/FABLE_WAVE_RIDER_PLAN.md` (reversed direction, audio prompts, colors/numbers packs, dashboard preset entry).
1. **TestFlight (scoped 2026-07-25 — "everything code-side is done" was wrong; see `investigations/TESTFLIGHT_READINESS_SCOPE_2026_07_25.md`).** Owner-side: (a) set `DEVELOPMENT_TEAM` in Xcode Signing & Capabilities; (b) Supabase dashboard — add `resonance://auth/callback` to Redirect URLs, Site URL `https://lingwave.ai`; (c) confirm the Apple Developer membership on team `ZL69STWQHV` and check App Store Connect for an existing `pro.resonanz.app` record / expired build; (d) decide the four Open questions above; (e) apply migration `20260619120000_pipeline_events_usage_cost_columns.sql`. Code-side still open: **a real 1024 app icon + a dark splash** (currently the Capacitor placeholder — needs a 1024 source or a `sharp`/`@capacitor/assets` devDependency) and, if external testing is chosen, the privacy manifest + native Stripe gating. Then `npm run build:ios` **on the Mac** (never Windows), open Xcode, bump `CURRENT_PROJECT_VERSION`, archive, upload.
2. **Owner re-tests:** NEW — SRS flashcard feel on the deploy (`526dfbaa`: swipe-to-grade physics, instant grading, direction exits) + Tier 1 press states/glass fallbacks (`6cec01ef`); Speak feedback pass (`c4ede7a9`); iPhone Lens 2G (`ae8ace4e`) + 14 UX-polish fixes (`e3fce8f0`); then the 2E device matrix on the actual shell (camera permission/preview, orientation, torch, backgrounding, offline mid-scan).
3. Capacitor fast-follow before wider beta: ~~slimming phases 1+2~~ (COMPLETE 2026-07-08, `26255d8f` + `aeb8cf2b`, dist 77.7 MiB — only native device smoke remains). Owner skim of `src/cost_logger.py` UNVERIFIED rates before trusting cost dashboards.
4. UX pass follow-ups: ~~StudyCardFrame + SessionComplete extraction~~ (done 2026-07-08 night, uncommitted); gate landing experiment routes; home-page loading perf (owner noted slowness 2026-07-06); plan Phase 4 image-study consolidation.
5. Script Lab follow-ups: owner mobile QA of /alphabet (now incl. Russian); next Russian tiers on owner request via `.claude/skills/add-target-language`. ~~Pre-existing test breakage~~ fixed 2026-07-08 night: the failing script was `test:generate-responsive-layout` (not `test:i18n-display-labels` as previously recorded) — stale asserts for deleted/evolved generate UI removed/updated, green now.
6. Cleanup ladder `docs/Refactors/FABLE_SAFE_CLEANUP_PATCH_PLAN.md`: L5 EXECUTED 2026-07-11 (plus DAW deletion, one-off archive, docs restructure — see LOG (4)); decide-list closed + Codex adversarial review clean (LOG (5)). Remaining rungs L1–L4/L6–L8 unchanged (triggers not met). **Owner commit call pending:** 3-file `/games`-fallback fix + `supabase/rescued-drafts/`.
7. Deferred but rising: Stripe/Supabase purchase config (owner flagged 2026-07-08 as "probably next"); tutor-catalog Phase 3, Study 2×2 consolidation, PG/classic consolidation (parked until beta skin feedback), further Supabase schema changes. ~~Guided listen-audio plan-first~~ → PLAN WRITTEN 2026-07-12: `docs/Product/FABLE_GUIDED_TTS_BATCH_PLAN.md`, awaiting the 4 owner decisions inside it.
8. **Language buildout queue:** A2 Phase 4 Polish/Indonesian/Cebuano P1–P10 drafts are now on disk. Next: run the language-filtered validator for the final batches, complete Fable's 300-lesson read-through, commission and arbitrate three adversarial reviews, apply fixes, integrate after each A1, run full suites, ask for the paid TTS batch, freeze ids, and make a scoped commit. After Phase 4: Tagalog/Dutch/Hindi/Arabic guided tiers (Russian owner-gated), voice-sample allow-set alignment, and the ≥20-language proposal including the Japanese decision.
9. ~~Owner calls~~ ALL RESOLVED 2026-07-13 (push ×2, full A2+Korean buildout, TTS seed+batch+push approved via AskUserQuestion). Next owner touchpoint: **device-check Spanish A2 (P2–P10) and Korean A1 (P2–P10) on the deploy** — path directories show the new paths, two-turn A2 lessons, rotated voices per path. Animations sidequest parked: owner's separate agent should start from `investigations/APPLE_DESIGN_SKILL_AUDIT_2026_07_10.md` (emil-design-eng skill already installed).
